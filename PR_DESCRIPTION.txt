## Summary

Removes plaintext database master passwords from documentation files.

## Context

During F-AUTH-1 keystone closure audit and Phase 3 credential 
rotation, plaintext credentials were discovered in committed 
documentation. Credentials have been rotated (2026-05-10 and 
2026-05-11) and old values are no longer valid for authentication.

This PR closes the documentation exposure surface as a hygiene 
measure.

## Scope

- Docs only (PR A of staged redaction work)
- Scripts requiring refactoring → deferred to PR B
- CI workflow inspection → deferred to PR C

## Risk

- Low: mechanical text replacement in markdown docs
- No code changes, no CI changes, no runtime impact
- All replaced credentials are already invalidated

## Verification

- [ ] git grep returns no matches for "Ayanna123!!" or 
      "EpisodeControl2024!Dev" in tracked files
- [ ] Diff inspection: no credential values visible in changes
- [ ] CI passes

## PEs addressed

PE #23 (amended), PE #25 (partial), PE #26, PE #28