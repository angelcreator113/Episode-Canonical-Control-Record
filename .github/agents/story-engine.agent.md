---
description: "Use when working on Story Engine auto-generation, WriteMode AI writing, voice system, texture layers, context loaders, AI prompt engineering, or any creative writing AI pipeline. Expert in the Before Lala narrative system. Use for: fix AI output, improve prose, add context loader, change prompt, voice drift, scene planner, rewrite options, story quality."
tools: [read, edit, search, execute]
---
## This file is stale

It names a deleted `frontend/src/pages/StoryEngine.jsx` and refers to a
"12K-line memories.js" — `src/routes/memories.js` was split by PR #328
into 10 files under `src/routes/memories/`.

**This notice records staleness. It does not rule on the Story Engine
backend's fate.** `src/routes/storyEvaluationRoutes.js` is still present
with no disposition document — whether it is retired, kept as an
API-only surface, or given a rebuilt UI is still owed (`PROJECT_CONTEXT.md`
§10 item 9).

For the current file layout and conventions, read `PROJECT_CONTEXT.md`
§4.2 and §5.
