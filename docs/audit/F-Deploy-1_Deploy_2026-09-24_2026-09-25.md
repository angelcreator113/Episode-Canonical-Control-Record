| **PRIME STUDIOS** **F-DEPLOY-1 DEPLOY RECORD** *Six production deploys from the night of 2026-09-24 through the afternoon of 2026-09-25, Deploys X through AC, all performed personally by Evoni, outside any agent session.* |
| --- |

**Document version**

New record, not a Fix Plan revision, and not an amendment of
`F-Deploy-1_Deploy_Afternoon_Evening_2026-09-24.md` (the P–W record).
This document follows that one rather than editing it. Basis:
`origin/main` at `79b9a5e6b02426edd38327333cb981e53132e620`, measured
2026-09-25. That basis is the tree Deploy AC (§8) moved production to.

**Author**

JAWIHP / Evoni — Prime Studios

**Status**

RECORD. Two standings appear below, each marked on its own claim and
never upgraded:

- **ATTESTED** covers what only Evoni's own account of the production
  host or database states. It cannot be reproduced from a clone.
- **MEASURED** covers what this repository itself shows: a
  `git log`/`diff`/`grep` any clone can reproduce.

This document closes no keystone, discharges no owed item, mints no FD,
XK or PE number, and rules on nothing.

The deploys are lettered X, Y, Z, AA, AB and AC, continuing after
Deploys P–W of the P–W record.

**The list is taken as given.** Evoni attests (2026-09-25, #1863) that
the list of Deploys X–AC is complete and contiguous: Deploy W left
production at `b0274fd6` and Deploy X starts there. This record takes
that list as given. Where the repository measures differently, both are
stated side by side, each with its own standing (§3, §9.3, §10, §11.1).
Neither overwrites the other. No deploy the list does not name is
inferred, lettered or dated here, and this record does not rule on which
account is right.

## §1. Identity — Deploys X, Y, Z, AA, AB and AC

**ATTESTED.**
- Instance `i-02ae7608c531db485`; tree `~/episode-metadata`.
- `git status` showed only the four known untracked `.bak` files
  throughout. That matches the P–W record §1 and the records it cites.
- `episode-worker` was stopped throughout.
- No package or lockfile change appeared in any of the six deploys.

**MEASURED.** No package or lockfile changes anywhere in the six
attested ranges combined:

```
$ git diff --name-only b0274fd61 79b9a5e6b -- package.json package-lock.json frontend/package.json frontend/package-lock.json
$ echo "exit=$?"
exit=0
```

Empty output. Of the six ranges, only Deploy X's touches
`src/migrations/` (§3); from Deploy X's end to the basis, nothing does:

```
$ git diff --name-only cc9129383 79b9a5e6b -- src/migrations/
$ echo "exit=$?"
exit=0
```

## §2. Continuity

**MEASURED.** Each attested range begins at the tree the one before it
ended at, and each end is an ancestor of the next:

```
$ git rev-parse b0274fd6 cc912938 ba4b4e77 ed59b3af 9d2cb528 8b90c0de 79b9a5e6 80c3e26bd
b0274fd61bbc626f4990ebb92bd6c0c1f016b1eb
cc912938309134beb483d1738d2a211be1a6f6d3
ba4b4e77ec8e6823a1dd2ebf4d4d9db2e53357c0
ed59b3aff430cbbbc9ab88996aa5207fcb4d06de
9d2cb528683929004990a6aeed7d8849044bd2ea
8b90c0de0b5751c243daa3df205023bc45601329
79b9a5e6b02426edd38327333cb981e53132e620
80c3e26bd13b8eb4fa23d218433e39f1521cb258

$ git merge-base --is-ancestor b0274fd61 cc9129383 ; echo "b0274fd61 -> cc9129383 exit=$?"
b0274fd61 -> cc9129383 exit=0
$ git merge-base --is-ancestor cc9129383 ba4b4e77e ; echo "cc9129383 -> ba4b4e77e exit=$?"
cc9129383 -> ba4b4e77e exit=0
$ git merge-base --is-ancestor ba4b4e77e ed59b3aff ; echo "ba4b4e77e -> ed59b3aff exit=$?"
ba4b4e77e -> ed59b3aff exit=0
$ git merge-base --is-ancestor ed59b3aff 9d2cb5286 ; echo "ed59b3aff -> 9d2cb5286 exit=$?"
ed59b3aff -> 9d2cb5286 exit=0
$ git merge-base --is-ancestor 9d2cb5286 8b90c0de0 ; echo "9d2cb5286 -> 8b90c0de0 exit=$?"
9d2cb5286 -> 8b90c0de0 exit=0
$ git merge-base --is-ancestor 8b90c0de0 79b9a5e6b ; echo "8b90c0de0 -> 79b9a5e6b exit=$?"
8b90c0de0 -> 79b9a5e6b exit=0
```

Deploy W ends at `b0274fd61` (P–W record §10); Deploy X begins there.

**ATTESTED.** Restarts 36 → 37 through 41 → 42 are the six deploys
recorded here. Restart 31 → 36 belongs to the P–W record's range. The
P–W record's own last restart is different; both are in §10.

## §3. Deploy X — 2026-09-25, after 00:16 UTC, backend and frontend

**ATTESTED.**
- Evoni moved the tree from `b0274fd6` to `cc912938`: two commits
  (#1829, #1830), two backend files.
- `node -c` passed on both.
- Backup `pre1830`.
- The served entry changed from `index-BPKWdqin.js` to
  `index-BPcC7ylO.js`.
- `pm2 restart`: restart count 36 → 37. `/health` returned 200.

**MEASURED**, `git log --oneline --first-parent b0274fd61..cc9129383`:

```
cc9129383 fix(feed): guard bulk delete against locked, crossed, entangled profiles [skip-automerge] (#1830)
39c4aca72 fix(shows): count only live episodes on show list [skip-automerge] (#1829)
80c3e26bd fix(sync): tier-based sync runs after evaluation, once per event [skip-automerge] (#1822)
207f5f00e feat(events): record deliverable fulfilment [skip-automerge] (#1823)
d1c3c0d8b feat(events): carry, edit and snapshot opportunity terms [skip-automerge] (#1821)
91a128bc7 fix(career): completing an episode completes its opportunity [skip-automerge] (#1820)
973c0056d fix(stats): one character key, lala, per F-Sec-3 [skip-automerge] (#1819)
f6ef8842b docs(flow): record the deliverables doctrine [skip-automerge] (#1813)
0590d943f docs: read why failures reach no log [skip-automerge] (#1811)
bd22e7037 fix: honest labels for the brand-deal fee and the requirements heading [skip-automerge] (#1810)
eddf0d1ac docs: read what deliverables are and where they live [skip-automerge] (#1807)
4862ff811 fix(services): opportunity-pipeline guests use the shared selection [skip-automerge] (#1805)
195232c09 docs(audit): file the 2026-09-24 afternoon and evening deploy record [skip-automerge] (#1803)
```

```
$ git rev-list --count --first-parent b0274fd61..cc9129383
13
$ git rev-list --count b0274fd61..cc9129383
13
```

`git diff --stat b0274fd61 cc9129383 -- src/`:

```
 src/app.js                                       |  10 +
 src/migrations/20260924000000-add-event-terms.js | 115 ++++++++
 src/models/CharacterState.js                     |   3 +-
 src/models/EventDeliverable.js                   |  62 +++++
 src/models/WorldEvent.js                         |  40 ++-
 src/models/index.js                              |   7 +
 src/routes/careerGoals.js                        |   6 -
 src/routes/episodes.js                           |   2 -
 src/routes/evaluation.js                         |   7 +-
 src/routes/eventDeliverables.js                  | 314 ++++++++++++++++++++++
 src/routes/shows.js                              |   5 +-
 src/routes/socialProfileRoutes.js                |  87 +++++-
 src/routes/worldEvents.js                        |  28 +-
 src/services/careerPipelineService.js            | 136 +++++-----
 src/services/characterSyncService.js             | 326 +++++++++++++++++------
 src/services/episodeCompletionService.js         |  72 ++++-
 src/services/episodeGeneratorService.js          |  54 +++-
 src/services/episodeScriptWriterService.js       |   2 +-
 src/services/eventAutomationService.js           |   7 +-
 src/services/eventTermsService.js                | 275 +++++++++++++++++++
 src/services/feedEventPipelineService.js         |  89 +++++--
 src/services/financialTransactionService.js      |   4 +-
 22 files changed, 1427 insertions(+), 224 deletions(-)
```

`git diff --stat b0274fd61 cc9129383 -- frontend/`:

```
 .../components/EventPackage/EventTermsSection.jsx  | 475 +++++++++++++++++++++
 .../EventPackage/EventTermsSection.test.jsx        | 215 ++++++++++
 frontend/src/pages/EventPackagePage.css            | 190 +++++++++
 frontend/src/pages/EventPackagePage.jsx            |  40 +-
 frontend/src/pages/SocialProfileGenerator.css      |  82 +++-
 frontend/src/pages/SocialProfileGenerator.jsx      |  43 +-
 frontend/src/pages/WorldAdmin.jsx                  |   9 +-
 frontend/src/utils/eventStakes.js                  |   3 +-
 frontend/src/utils/eventTerms.js                   | 277 ++++++++++++
 frontend/src/utils/eventTerms.test.js              | 133 ++++++
 10 files changed, 1445 insertions(+), 22 deletions(-)
```

`git diff --stat b0274fd61 cc9129383 -- src/migrations/`:

```
 src/migrations/20260924000000-add-event-terms.js | 115 +++++++++++++++++++++++
 1 file changed, 115 insertions(+)
```

The narrower range that is exactly #1829 and #1830:

```
$ git log --oneline --first-parent 80c3e26bd..cc9129383
cc9129383 fix(feed): guard bulk delete against locked, crossed, entangled profiles [skip-automerge] (#1830)
39c4aca72 fix(shows): count only live episodes on show list [skip-automerge] (#1829)
$ git diff --stat 80c3e26bd cc9129383 -- src/
 src/routes/shows.js               |  5 ++-
 src/routes/socialProfileRoutes.js | 87 ++++++++++++++++++++++++++++++-----
 2 files changed, 80 insertions(+), 12 deletions(-)
$ git diff --stat 80c3e26bd cc9129383 -- frontend/
 frontend/src/pages/SocialProfileGenerator.css | 82 ++++++++++++++++++++++++++-
 frontend/src/pages/SocialProfileGenerator.jsx | 43 +++++++++++++-
 2 files changed, 123 insertions(+), 2 deletions(-)
$ git merge-base --is-ancestor 80c3e26bd b0274fd61 ; echo "80c3e26bd ancestor of b0274fd61? exit=$?"
80c3e26bd ancestor of b0274fd61? exit=1
$ git merge-base --is-ancestor b0274fd61 80c3e26bd ; echo "b0274fd61 ancestor of 80c3e26bd? exit=$?"
b0274fd61 ancestor of 80c3e26bd? exit=0
$ TZ=UTC git log -1 --format='%cd' --date=iso-local 80c3e26bd
2026-09-24 23:04:09 +0000
```

**Where the repository measures differently — both stated, not
resolved.**

- **Commit count.** ATTESTED: two commits (#1829, #1830). MEASURED:
  `b0274fd61..cc9129383` is 13 first-parent commits (#1803, #1805,
  #1807, #1810, #1811, #1813, #1819, #1820, #1821, #1822, #1823, #1829,
  #1830).
- **Backend file count.** ATTESTED: two backend files. MEASURED: the
  same range touches 22 files under `src/`, one of them the new
  migration `src/migrations/20260924000000-add-event-terms.js` (#1821),
  and 10 files under `frontend/`.
- **Where Evoni's counts do match the repository.** MEASURED:
  `80c3e26bd..cc9129383` is exactly #1829 and #1830 and two `src/`
  files. `80c3e26bd` (#1822, committed 2026-09-24 23:04:09 UTC) is not an
  ancestor of `b0274fd61`; `b0274fd61` is an ancestor of it.
- **The time.** Not measured. Evoni's ~23:20 UTC was an estimate
  (she said so, 2026-09-25). It is recorded as after 00:16 UTC on
  2026-09-25, the merge of #1830 (`cc9129383`, the deploy's end commit),
  and before Deploy Y (§11.1).

This record states these side by side. It does not infer from them any
deploy between Deploy W and Deploy X, does not letter or date one, does
not re-count Deploy X, and does not rule on which account is right.

## §4. Deploy Y — ~2026-09-25 01:55 UTC, backend and frontend

**ATTESTED.**
- Evoni moved the tree from `cc912938` to `ba4b4e77`: five commits
  (#1831, #1835, #1836, #1837, #1839), three backend files.
- `node -c` passed on all three.
- Backup `pre1839`.
- The served entry changed from `index-BPcC7ylO.js` to
  `index-DKapwGXl.js`.
- `pm2 restart`: restart count 37 → 38. `/health` returned 200.
- Immediately before this deploy, two schema changes were made by hand
  as `postgres`, and the JustAWoman seeder was run (§9.1, §9.2).
- After the deploy, `GET /api/v1/roles/stats` returned 400 rather than
  the 500 the migration's header records.

**MEASURED**, `git log --oneline --first-parent cc9129383..ba4b4e77e`:

```
ba4b4e77e fix(feed): delete confirms say profiles are hidden, not permanently deleted [skip-automerge] (#1839)
724f4eb2f feat(feed): autofill draft for Create New Creator form [skip-automerge] (#1837)
eba35e985 fix(models): CharacterState is not paranoid [skip-automerge] (#1836)
f9c4dea67 fix(routes): an opportunity milestone no longer writes JustAWoman's locked record [skip-automerge] (#1835)
26e25c377 docs: read the migration drift [skip-automerge] (#1831)
```

Five commits (`git rev-list --count --first-parent`: `5`), matching
Evoni's count. `git diff --stat`, scoped to `src/`:

```
 src/models/CharacterState.js      |   1 +
 src/routes/opportunityRoutes.js   |  11 ---
 src/routes/socialProfileRoutes.js | 166 +++++++++++++++++++++++++++++++++++++-
 3 files changed, 165 insertions(+), 13 deletions(-)
```

Three files, matching Evoni's count. `git diff --stat`, scoped to
`frontend/`:

```
 frontend/src/pages/SocialProfileGenerator.css      | 39 +++++++++++
 frontend/src/pages/SocialProfileGenerator.jsx      | 75 +++++++++++++++++++++-
 frontend/src/pages/SocialProfileGenerator.test.jsx |  8 +++
 3 files changed, 119 insertions(+), 3 deletions(-)
```

That is consistent with the changed served entry. `src/migrations/`:
no output.

- **The 500 in the migration's header.** MEASURED:
  `src/migrations/20260902000001-add-role-key-to-assets.js` reads, in its
  header comment, "Found live: GET /api/v1/roles/stats 500s with "column
  Asset.role_key does not exist"". The 400 is ATTESTED; this record does
  not read the route and does not rule on what the 400 means.

## §5. Deploy Z — ~2026-09-25 02:20 UTC, backend only

**ATTESTED.**
- Evoni moved the tree from `ba4b4e77` to `ed59b3af`: four commits
  (#1840, #1845, #1846, #1850), three backend files.
- `node -c` passed.
- No frontend build and no bundle backup.
- `pm2 restart`: restart count 38 → 39. `/health` returned 200.

**MEASURED**, `git log --oneline --first-parent ba4b4e77e..ed59b3aff`:

```
ed59b3aff fix(feed): generated profile fields fit their column lengths [skip-automerge] (#1850)
cf992c87c fix(tooling): audit-file skill footer no longer says Prod FROZEN [skip-automerge] (#1846)
13c76dc96 fix(models): StoryTaskArc is not paranoid [skip-automerge] (#1845)
64708f9c8 fix(wardrobe): browse-pool's event lookup includes soft-deleted events [skip-automerge] (#1840)
```

Four commits (`git rev-list --count --first-parent`: `4`), matching
Evoni's count. `git diff --stat`, scoped to `src/`:

```
 src/models/StoryTaskArc.js        |  1 +
 src/routes/socialProfileRoutes.js | 88 +++++++++++++++++++++++++++++++++++++--
 src/routes/wardrobe.js            |  3 +-
 3 files changed, 88 insertions(+), 4 deletions(-)
```

Three files, matching Evoni's count. `git diff --stat`, scoped to
`frontend/` and to `src/migrations/`: no output. That is consistent with
no frontend build.

## §6. Deploy AA — 2026-09-25, after 06:11 UTC, backend only

**ATTESTED.**
- Evoni moved the tree from `ed59b3af` to `9d2cb528`: four commits
  (#1852, #1853, #1854, #1855), two backend files.
- `node -c` passed.
- `pm2 restart`: restart count 39 → 40. `/health` returned 200.

**MEASURED**, `git log --oneline --first-parent ed59b3aff..9d2cb5286`:

```
9d2cb5286 fix(tooling): tooling states the lifted freeze correctly [skip-automerge] (#1855)
e4e2f035f fix(lint): catch comment-only catches and value fallbacks [skip-automerge] (#1854)
4725660a9 fix(story-engine): "Arc saved" only when saved; log default arc context [skip-automerge] (#1853)
c4250381b fix: three .unscoped() calls include deleted rows as they were written to [skip-automerge] (#1852)
```

Four commits (`git rev-list --count --first-parent`: `4`), matching
Evoni's count. `git diff --stat`, scoped to `src/`:

```
 src/routes/evaluation.js      |  7 +++++--
 src/routes/memories/engine.js | 29 +++++++++++++++++++++++------
 2 files changed, 28 insertions(+), 8 deletions(-)
```

Two files, matching Evoni's count. `git diff --stat`, scoped to
`frontend/` and to `src/migrations/`: no output.

**The time.** Not measured. Evoni's ~02:35 UTC was an estimate (she
said so, 2026-09-25). It is recorded as after 06:11 UTC, the merge of
#1855 (`9d2cb5286`, the deploy's end commit), and before Deploy AB
(§11.1).

## §7. Deploy AB — 2026-09-25 13:31 UTC, backend only

**ATTESTED.**
- Evoni moved the tree from `9d2cb528` to `8b90c0de`: two commits
  (#1857, #1858), six backend files.
- `node -c` passed on all six. `src/utils/fitToModel.js` loaded with ten
  exports.
- `pm2 restart`: restart count 40 → 41. `/health` returned 200.

**MEASURED**, `git log --oneline --first-parent 9d2cb5286..8b90c0de0`:

```
8b90c0de0 fix(ci): deploy stops when a migration fails [skip-automerge] (#1858)
9732fbde3 fix(feed): every generated-profile write fits column lengths and enums [skip-automerge] (#1857)
```

Two commits (`git rev-list --count --first-parent`: `2`), matching
Evoni's count. `git diff --stat`, scoped to `src/`:

```
 src/routes/characterGenerationRoutes.js |  10 ++-
 src/routes/socialProfileBulkRoutes.js   | 121 ++++++++++++++++++------------
 src/routes/socialProfileRoutes.js       | 100 +++----------------------
 src/services/feedAutoGeneration.js      |  15 +++-
 src/services/feedScheduler.js           |  78 +++++++++++---------
 src/utils/fitToModel.js                 | 127 ++++++++++++++++++++++++++++++++
 6 files changed, 277 insertions(+), 174 deletions(-)
```

- **Six files**, matching Evoni's count. `git diff --stat`, scoped to
  `frontend/` and to `src/migrations/`: no output.
- **Ten exports.** `git show 8b90c0de:src/utils/fitToModel.js` opens
  `module.exports` at line 116 and lists ten names:
  `VALID_TRAJECTORIES`, `VALID_ARCHETYPES`, `VALID_FOLLOWER_TIERS`,
  `sanitizeEnum`, `stringLengthLimits`, `fitToLength`,
  `fitRecordToModel`, `warnTruncated`, `logValueTooLong` and `asText`.
  That matches Evoni's count.
- **Outside `src/`**, #1858 changes `.github/scripts/deploy-production.sh`
  and `.github/workflows/deploy-dev.yml`. No workflow was dispatched by
  the filing session.

## §8. Deploy AC — 2026-09-25 ~14:55 UTC, backend only

**ATTESTED.**
- Evoni moved the tree from `8b90c0de` to `79b9a5e6`: three commits
  (#1864, #1865, #1867), two backend files.
- `node -c` passed on both.
- No frontend build and no bundle backup.
- `pm2 restart`: restart count 41 → 42. `/health` returned 200.

**MEASURED**, `git log --oneline --first-parent 8b90c0de0..79b9a5e6b`:

```
79b9a5e6b fix(events): guest selection's relationship stage queries the real columns [skip-automerge] (#1867)
5c2a6495a fix(feed): the bulk generate timeout timer is cleared [skip-automerge] (#1865)
daea0d8a9 feat(scripts): a manual deploy stops on pending migrations [skip-automerge] (#1864)
```

Three commits (`git rev-list --count --first-parent`: `3`), matching
Evoni's count. `git diff --stat`, scoped to `src/`:

```
 src/routes/socialProfileBulkRoutes.js  | 28 +++++++++++++++++--------
 src/services/eventAutomationService.js | 37 +++++++++++++++++++++++++---------
 2 files changed, 47 insertions(+), 18 deletions(-)
```

Two files, matching Evoni's count. `git diff --stat`, scoped to
`frontend/` and to `src/migrations/`: no output. Outside `src/`, the
range adds `scripts/check-pending-migrations.js` (175 lines, #1864).

### §8.1 The first run of `scripts/check-pending-migrations.js`

**ATTESTED.** Before the restart, the new check ran for the first time:

```
NODE_ENV=production node scripts/check-pending-migrations.js --report-only
```

- **The target it named:** `episode-control-dev…/episode_metadata` as
  `episode_app_dev`.
- **The result:** 3 pending of 215 files:
  - `20260818000000-add-deleted-at-to-decision-logs.js`
  - `20260902000000-create-asset-roles.js`
  - `20260922000000-add-category-format-to-world-events.js`
- **What it settles** (Evoni's reading, recorded as ATTESTED):
  - `episode_app_dev` has `SELECT` on `SequelizeMeta`, because the check
    read it;
  - `20260902000001-add-role-key-to-assets.js` is recorded in
    `SequelizeMeta`, because it no longer appears as pending. That
    confirms the hand-run before Deploy Y (§9.1) was recorded.

**MEASURED**, only as far as the script and the tree go:
- `scripts/check-pending-migrations.js` at the basis reads
  `SELECT name FROM "SequelizeMeta"` (line 32), logs the target as
  `NODE_ENV=<env> → <host>:<port>/<database> as <username>` (line 105),
  and with `--report-only` exits 0 despite pending files (lines 15, 92).
- The tree at the basis has 215 migration files:

```
$ git ls-files src/migrations | wc -l
215
```

The background of the three pending files is in
`docs/MIGRATION_DRIFT_READ.md` §1 (rows 1, 2 and 4 of its table). It is
cited, not restated. This record does not rule on whether or how they
should run.

## §9. Hand-run schema changes

### §9.1 `role_key` (20260902000001), before Deploy Y

**ATTESTED.** Immediately before Deploy Y, as `postgres`, because
`episode_app_dev` owns no tables:
- `assets.role_key VARCHAR(100)` was added, with index
  `idx_assets_role_key`;
- the `SequelizeMeta` row for `20260902000001-add-role-key-to-assets.js`
  was inserted.
- Verified afterwards: the `role_key` column and index are both present.

No grant is attested for this change.

### §9.2 The JustAWoman seeder (20260314100000), before Deploy Y

**ATTESTED.** Then the JustAWoman seeder (`20260314100000`) was run with
`--seed`, creating `social_profiles` id 445. Verified afterwards: one
`@justawoman` row, not deleted.

**MEASURED.** The seeder file is
`src/seeders/20260314100000-justawoman-lalaverse-profile.js`.

### §9.3 `add-event-terms` (20260924000000), 2026-09-24

**ATTESTED.** On 2026-09-24, `20260924000000-add-event-terms.js` was run
by hand as `postgres`:
- `world_events.restrictions` and the table `event_deliverables` were
  created;
- `SELECT`, `INSERT`, `UPDATE` and `DELETE` were granted to
  `episode_app_dev`;
- its `SequelizeMeta` row was inserted.

Evoni attests this was earlier in the same period and already covered by
the P–W record's basis.

**MEASURED.** The migration file is not in the P–W record's basis
`b0274fd61`; it enters with #1821, inside Deploy X's measured range
(§3). The P–W record does not mention it.

```
$ git cat-file -e b0274fd61:src/migrations/20260924000000-add-event-terms.js ; echo "exit=$?"
fatal: path 'src/migrations/20260924000000-add-event-terms.js' exists on disk, but not in 'b0274fd61'
exit=128
$ git cat-file -e cc9129383:src/migrations/20260924000000-add-event-terms.js ; echo "exit=$?"
exit=0
$ git log --diff-filter=A --format='%h %ad %s' --date=short -- src/migrations/20260924000000-add-event-terms.js
d1c3c0d8b 2026-09-24 feat(events): carry, edit and snapshot opportunity terms [skip-automerge] (#1821)
$ git merge-base --is-ancestor d1c3c0d8b b0274fd61 ; echo "d1c3c0d8b ancestor of b0274fd61? exit=$?"
d1c3c0d8b ancestor of b0274fd61? exit=1
$ grep -c -e '20260924000000' -e 'event_deliverables' -e 'event-terms' docs/audit/F-Deploy-1_Deploy_Afternoon_Evening_2026-09-24.md
0
```

**Both stated, not resolved.** ATTESTED: the hand-run is covered by the
P–W record's basis. MEASURED: the file is not in that basis and the P–W
record does not name it. This record does not rule on which is right and
does not amend the P–W record.

## §10. Restarts

**ATTESTED (Evoni).**

| Restart | Deploy |
|---|---|
| 36 → 37 | X |
| 37 → 38 | Y |
| 38 → 39 | Z |
| 39 → 40 | AA |
| 40 → 41 | AB |
| 41 → 42 | AC |

- Restart 31 → 36 belongs to the P–W record's range.
- No restarts outside deploys are known.

**Beside it, from the register.** The P–W record's last restart is
Deploy W, 30 → 31
(`F-Deploy-1_Deploy_Afternoon_Evening_2026-09-24.md` §10):

```
$ grep -n -e '30 → 31' -e '31 →' docs/audit/F-Deploy-1_Deploy_Afternoon_Evening_2026-09-24.md
310:- `pm2 restart`: restart count 30 → 31. `/health` returned 200.
```

**Both stated, not resolved.** ATTESTED: 31 → 36 belongs to the P–W
record's range. The P–W record records no restart after 30 → 31. This
record does not assign the five restarts between 31 and 36 to any deploy
or any other event, and it does not letter or date any deploy for them.

## §11. Basis statement

**MEASURED.** After Deploy AC, production's tree is this record's basis,
`79b9a5e6b`, which is `origin/main` at filing:

```
$ git rev-parse origin/main
79b9a5e6b02426edd38327333cb981e53132e620
```

No commit on `origin/main` at `79b9a5e6b` is undeployed, on Evoni's
attested list.

- **The CharacterState note.** `F-Stats-1_CharacterState_Paranoid_Note_2026-09-25.md`
  §3 says the v1.17 line was inaccurate as a description of the model
  "until the fix is deployed", and that "the period ends at the first
  production deploy whose SHA contains Task #1832's model commit".
  MEASURED: Task #1832's model commit is `eba35e985` (#1836, body
  `Task: #1832`). It is in Deploy Y's range (§4), not Deploy X's:

  ```
  $ git merge-base --is-ancestor eba35e985 cc9129383 ; echo "eba35e985 in cc9129383 (Deploy X)? exit=$?"
  eba35e985 in cc9129383 (Deploy X)? exit=1
  $ git merge-base --is-ancestor eba35e985 ba4b4e77e ; echo "eba35e985 in ba4b4e77e (Deploy Y)? exit=$?"
  eba35e985 in ba4b4e77e (Deploy Y)? exit=0
  ```

  So the first production deploy whose SHA contains it is **Deploy Y,
  `ba4b4e77e`**, and that deploy ends the note's period. Issue #1863 named
  `9d2cb5286` (Deploy AA); that was an error in the issue's instructions,
  not an attested fact, and is corrected here. This record does not edit
  the note.
- **StoryTaskArc (#1845).** `13c76dc96` is first deployed by Deploy Z,
  `ed59b3aff`:

  ```
  $ git merge-base --is-ancestor 13c76dc96 ba4b4e77e ; echo "13c76dc96 in ba4b4e77e (Deploy Y)? exit=$?"
  13c76dc96 in ba4b4e77e (Deploy Y)? exit=1
  $ git merge-base --is-ancestor 13c76dc96 ed59b3aff ; echo "13c76dc96 in ed59b3aff (Deploy Z)? exit=$?"
  13c76dc96 in ed59b3aff (Deploy Z)? exit=0
  ```

- **`assembleGuestList`.** Deploy AC (#1867) is the first deploy in which
  `assembleGuestList`'s relationship stage in
  `src/services/eventAutomationService.js` can return guests. MEASURED:
  #1867's message says the stage queried `profile_a_id` /
  `profile_b_id`, "names the table never had", and now queries
  `source_profile_id` / `target_profile_id`. At the basis, the stage's
  where clause names `source_profile_id` and `target_profile_id` (lines
  406–407).

### §11.1 Commit times beside the attested times

**MEASURED.** Committer dates (`%cd`) on `origin/main`, UTC:

```
$ TZ=UTC git log --first-parent --format='%h %cd %s' --date=iso-local b0274fd61..79b9a5e6b
79b9a5e6b 2026-09-25 14:02:55 +0000 fix(events): guest selection's relationship stage queries the real columns [skip-automerge] (#1867)
5c2a6495a 2026-09-25 13:59:28 +0000 fix(feed): the bulk generate timeout timer is cleared [skip-automerge] (#1865)
daea0d8a9 2026-09-25 13:55:43 +0000 feat(scripts): a manual deploy stops on pending migrations [skip-automerge] (#1864)
8b90c0de0 2026-09-25 10:20:40 +0000 fix(ci): deploy stops when a migration fails [skip-automerge] (#1858)
9732fbde3 2026-09-25 10:17:45 +0000 fix(feed): every generated-profile write fits column lengths and enums [skip-automerge] (#1857)
9d2cb5286 2026-09-25 06:11:04 +0000 fix(tooling): tooling states the lifted freeze correctly [skip-automerge] (#1855)
e4e2f035f 2026-09-25 02:43:26 +0000 fix(lint): catch comment-only catches and value fallbacks [skip-automerge] (#1854)
4725660a9 2026-09-25 02:39:47 +0000 fix(story-engine): "Arc saved" only when saved; log default arc context [skip-automerge] (#1853)
c4250381b 2026-09-25 02:35:26 +0000 fix: three .unscoped() calls include deleted rows as they were written to [skip-automerge] (#1852)
ed59b3aff 2026-09-25 02:17:10 +0000 fix(feed): generated profile fields fit their column lengths [skip-automerge] (#1850)
cf992c87c 2026-09-25 02:13:27 +0000 fix(tooling): audit-file skill footer no longer says Prod FROZEN [skip-automerge] (#1846)
13c76dc96 2026-09-25 02:09:35 +0000 fix(models): StoryTaskArc is not paranoid [skip-automerge] (#1845)
64708f9c8 2026-09-25 01:59:29 +0000 fix(wardrobe): browse-pool's event lookup includes soft-deleted events [skip-automerge] (#1840)
ba4b4e77e 2026-09-25 01:35:17 +0000 fix(feed): delete confirms say profiles are hidden, not permanently deleted [skip-automerge] (#1839)
724f4eb2f 2026-09-25 01:26:45 +0000 feat(feed): autofill draft for Create New Creator form [skip-automerge] (#1837)
eba35e985 2026-09-25 01:24:04 +0000 fix(models): CharacterState is not paranoid [skip-automerge] (#1836)
f9c4dea67 2026-09-25 01:21:05 +0000 fix(routes): an opportunity milestone no longer writes JustAWoman's locked record [skip-automerge] (#1835)
26e25c377 2026-09-25 01:02:27 +0000 docs: read the migration drift [skip-automerge] (#1831)
cc9129383 2026-09-25 00:16:49 +0000 fix(feed): guard bulk delete against locked, crossed, entangled profiles [skip-automerge] (#1830)
39c4aca72 2026-09-24 23:43:39 +0000 fix(shows): count only live episodes on show list [skip-automerge] (#1829)
80c3e26bd 2026-09-24 23:04:09 +0000 fix(sync): tier-based sync runs after evaluation, once per event [skip-automerge] (#1822)
207f5f00e 2026-09-24 23:00:24 +0000 feat(events): record deliverable fulfilment [skip-automerge] (#1823)
d1c3c0d8b 2026-09-24 22:18:18 +0000 feat(events): carry, edit and snapshot opportunity terms [skip-automerge] (#1821)
91a128bc7 2026-09-24 21:56:57 +0000 fix(career): completing an episode completes its opportunity [skip-automerge] (#1820)
973c0056d 2026-09-24 21:44:19 +0000 fix(stats): one character key, lala, per F-Sec-3 [skip-automerge] (#1819)
f6ef8842b 2026-09-24 21:15:52 +0000 docs(flow): record the deliverables doctrine [skip-automerge] (#1813)
0590d943f 2026-09-24 20:16:02 +0000 docs: read why failures reach no log [skip-automerge] (#1811)
bd22e7037 2026-09-24 20:03:31 +0000 fix: honest labels for the brand-deal fee and the requirements heading [skip-automerge] (#1810)
eddf0d1ac 2026-09-24 19:48:30 +0000 docs: read what deliverables are and where they live [skip-automerge] (#1807)
4862ff811 2026-09-24 18:15:25 +0000 fix(services): opportunity-pipeline guests use the shared selection [skip-automerge] (#1805)
195232c09 2026-09-24 17:53:27 +0000 docs(audit): file the 2026-09-24 afternoon and evening deploy record [skip-automerge] (#1803)
```

| Deploy | ATTESTED time | MEASURED: committer date of the attested end commit |
|---|---|---|
| X | after 2026-09-25 00:16 (estimate withdrawn) | `cc9129383` 2026-09-25 00:16:49 (and #1829, `39c4aca72`, 2026-09-24 23:43:39) |
| Y | ~2026-09-25 01:55 | `ba4b4e77e` 01:35:17 |
| Z | ~2026-09-25 02:20 | `ed59b3aff` 02:17:10 |
| AA | after 2026-09-25 06:11 (estimate withdrawn) | `9d2cb5286` 06:11:04 |
| AB | 2026-09-25 13:31 | `8b90c0de0` 10:20:40 |
| AC | 2026-09-25 ~14:55 | `79b9a5e6b` 14:02:55 |

For X and AA, Evoni's first times (~23:20 and ~02:35 UTC) were
estimates that fell before the merge of each deploy's end commit. At her
direction (2026-09-25) they are recorded as bounds from those merges, as
the P–W record handled Deploy P's time, not as figures nobody measured.

## §12. Database reads — facts Evoni obtained, not ruled on

**ATTESTED (Evoni).** Read during the period. Recorded as the results
she obtained. This record does not rule on what any of them means.

- The 01:52 "value too long for type character varying(100)" error
  predates the field-length fix deployed at 13:31 (Deploy AB).
- After that fix, the log shows truncation working: `post_frequency`
  201 → 100, `engagement_rate` 99 → 50, `geographic_cluster` 224 → 100,
  `relationship_status` 236 → 100.
- A profile-creation attempt at ~13:19 produced no row. The newest
  `social_profiles` rows are 445 `@justawoman` (2026-09-25) and 444
  `@morningdewsage` (2026-03-16).
- `social_profile_relationships` has 13 columns, including
  `source_profile_id` and `target_profile_id`, and neither
  `profile_a_id` nor `profile_b_id`. This read is the evidence behind
  #1860, which #1867 (Deploy AC) carries (`Task: #1860` in its message).

No data is re-derived here.

## §13. What this document does not do

This document:

- does not resolve any place where the repository measures differently
  from Evoni's attestation (§3, §9.3, §10, §11, §11.1). Each is stated
  with its standing, and none is ruled on;
- does not infer, letter or date any deploy the attested list does not
  name;
- does not restate `docs/MIGRATION_DRIFT_READ.md` or rule on the three
  pending migrations;
- does not edit `F-Stats-1_CharacterState_Paranoid_Note_2026-09-25.md`
  or the P–W record;
- does not rule on what any database result in §12 means, and
  re-derives no production data;
- does not discharge any owed item recorded in `PROJECT_CONTEXT.md` §6.5
  or any Fix Plan revision, and closes no keystone;
- makes no fix, and mints no FD, XK or PE number;
- amends no filed document. Every document named above is cited, not
  edited;
- performs no deploy, database read or change, workflow dispatch, or
  credential change of its own, and makes no host, AWS, database or
  Cognito contact. Every ATTESTED claim above is Evoni's own account,
  taken outside any agent session. Every MEASURED claim is a repository
  read this filing session performed itself, against `origin/main`, not
  against any host;
- records no secret anywhere above.

## §14. Tails — re-derived, not carried

```
$ ls docs/audit/ | grep -E '^FD-[0-9]+_' | sort -t- -k2 -n
FD-66_Model_Migration_Contract_Mismatch_2026-08-18_DRAFT.md
FD-69_Unauthenticated_Token_Issuance_2026-08-22_DRAFT.md

$ ls docs/audit/ | grep -E '^XK-[0-9]+_'
XK-2_Extent_Census_2026-09-05.md

$ grep -oE 'PE #[0-9]+' docs/audit/Session_PE_Roster.md | sort -t'#' -k2 -n | tail -1
PE #68
```

Unchanged from the P–W record §15. Nothing minted here.

## §Standing

- §3–§8 each carry an ATTESTED clause and a MEASURED clause, marked
  separately and never merged into one standing:
  - ATTESTED: Evoni's own account of actions taken personally on the
    production host, not reproducible from a clone;
  - MEASURED: a read of this repository, reproducible by anyone with a
    clone.
- Where they differ (Deploy X's counts, §3; the event-terms basis, §9.3;
  restarts 31 → 36, §10; the CharacterState period, §11; the times of X
  and AA, §11.1), both are stated and neither overwrites the other.
- §1 and §2 carry MEASURED checks and ATTESTED host facts.
- §8.1 and §9 are ATTESTED for what was run on the database. Their
  MEASURED clauses read only the repository.
- §12 is ATTESTED for the database results.
- Nothing in this document is labelled RULED, and nothing is INFERRED.
- No host, AWS, database or Cognito contact was made by the agent
  session that filed it.
- Production's freeze is lifted (`F-Deploy-1_Fix_Plan_v1.53.md` §1);
  agent sessions still never touch hosts, AWS, RDS or Cognito
  (`CLAUDE.md`).

*Type: deploy record. Rules: nothing. Mints: nothing. Discharges:
nothing. Host/AWS/DB/Cognito contact by the filing session: none.
Task: #1863.*
