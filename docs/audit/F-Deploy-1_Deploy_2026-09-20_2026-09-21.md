| **PRIME STUDIOS** **F-DEPLOY-1 DEPLOY RECORD** *Three production deploys, 2026-09-20 to 2026-09-21 — one frontend-only deploy each on the 20th and the 21st, one frontend-and-backend deploy on the 21st with an API restart — the 2026-09-21 log check, and Evoni's own post-deploy episode deletion.* |
| --- |

**Document version**

New record, not a Fix Plan revision. Basis: `origin/main` at
`24073cb54d3ce9c266d6a74033bf6d8d4399a8ee`, measured 2026-09-21.

**Author**

JAWIHP / Evoni — Prime Studios

**Status**

RECORD. Two standings appear below, each marked on its own claim, never
upgraded: **ATTESTED** for what only Evoni's own account of the production
host states, not reproducible from a clone, and **MEASURED** for what this
repository itself shows — a `git log`/`diff` any clone can reproduce. This
document closes no keystone, discharges no owed item, and mints no FD, XK,
or PE number.

## §1. Deploy 1 — 2026-09-20, frontend only

**ATTESTED.** Evoni attests: instance `i-02ae7608c531db485` (confirmed via
the metadata service), shell prompt `ip-172-31-26-1`, deploy tree
`~/episode-metadata`, remote
`git@github.com:angelcreator113/Episode-Canonical-Control-Record.git`. `git
status` before the deploy showed only four untracked files —
`ecosystem.config.js.bak-20260619-a2cfg`,
`ecosystem.config.js.bak-20260706`, `ecosystem.config.js.bak-20260706-b`,
`src/routes/auth.js.bak-2026-08-22` — unchanged from the prior deploy
record's own observations (`F-Deploy-1_Deploy_2026-09-20.md` §6). `pm2`
showed `episode-api-prod-hotfix` (id 13, cluster) online and
`episode-worker` (id 1) stopped, before any action this record covers.

Evoni moved the tree from `f08440fcb5a361896cc91d1f3d79705f235bc51e` to
`c8c4ec8b6a600be2f70f8621630878e6a02d8211` (`git merge --ff-only`), four
commits, no `src/` changes; no API restart was performed. `node_modules`
was already present (468M); no `npm install` was run. Disk was at 931M
free (88% used) before the deploy.

The served bundle at `/var/www/html` was dated Sep 19 14:14 **before**
this deploy — a write this record does not account for (see §4 below).
Evoni made a backup, `/var/www/html.bak-20260920-pre1584` (5.7M); a
rollback command was run in error immediately after and restored an
identical copy, so the backup was re-made. The build's `dist` was dated
2026-09-20 22:42; rsynced to `/var/www/html`; served entry
`index-DblLTMGT.js` (was `index-jhOcWKuV.js`); a request with
`Host: primepisodes.com` returned HTTP 200.

Evoni also observed, this deploy: two enabled nginx site files
(`episode-prod`, `episode`), both with root `/var/www/html` and not
symlinks of each other; `episode-prod`'s `server_name` line contains a
markdown-link-formatted `www.primepisodes.com`; a request to localhost
with no `Host` header returns 404; a service worker (`sw.js`) is served.
None of these is characterized as a defect here.

**MEASURED**, `git log --oneline
f08440fcb5a361896cc91d1f3d79705f235bc51e..c8c4ec8b6a600be2f70f8621630878e6a02d8211`:

```
c8c4ec8b6 fix(frontend): open What's next on wrap, not on episode load [skip-automerge] (#1584)
cbcd38296 docs(audit): file the 2026-09-20 branch-tip census [skip-automerge] (#1582)
e55917089 docs(context): record the 2026-09-20 deploy and v2.77 §8(b) [skip-automerge] (#1580)
a9b17cef0 docs(audit): file the 2026-09-20 production deploy record [skip-automerge] (#1578)
```

Four commits, matching Evoni's own count. PRs #1578, #1580, #1582, #1584.
`git diff --stat` over the same range touches no file under `src/`,
`src/migrations/`, or `package.json` — consistent with Evoni's account
that no API restart was needed.

## §2. Deploy 2 — 2026-09-21 ~14:45 UTC, frontend only

**ATTESTED.** Evoni moved the tree from
`c8c4ec8b6a600be2f70f8621630878e6a02d8211` to
`c3ecff3ea5a579fa6169442603fca3530883c72f`; no `src/`, dependency, or
migration changes; no API restart.

Evoni found pre-existing backups already on the box:
`/var/www/html.bak-20260918`, `-20260919`, `-20260919b`, `-20260919c`,
`-20260920-pre1584`. Three are dated 2026-09-19 — consistent with §1's
unexplained Sep 19 14:14 bundle timestamp, but this record takes no
position on what wrote them, when, or by what process; it records the
coincidence in date only. All five were left in place. The login page
carried a "System restart required" banner; no reboot was performed.

Evoni made a backup, `/var/www/html.bak-20260921-pre1606`; served entry
`index-DkC3FXJA.js`; HTTP 200.

**MEASURED**, `git log --oneline
c8c4ec8b6a600be2f70f8621630878e6a02d8211..c3ecff3ea5a579fa6169442603fca3530883c72f`:

```
c3ecff3ea refactor(frontend): separate Episode Run Sheet from To-Do Overlays, rename Preview Phone [skip-automerge] (#1606)
b102201f4 docs: census the thumbnail system [skip-automerge] (#1604)
4117b86ca refactor(frontend): trim Episode Detail header to owned actions [skip-automerge] (#1602)
5e6a79b25 docs: refresh PROJECT_CONTEXT.md — basis, citation convention, URL-only count, §0 item 5 [skip-automerge] (#1600)
9ffffc3f7 docs: record the citation convention for living and audit documents [skip-automerge] (#1598)
6b43cb975 docs(flow): fix stale post-#1590 references in PAGE_INVENTORY.md [skip-automerge] (#1596)
434c9c81a docs(flow): fix stale post-#1590 references in EVENT_EPISODE_FLOW.md and CultureEvents.jsx [skip-automerge] (#1594)
21ca06261 docs(flow): add the calendar-driven EVENT path and its host-recording asymmetry [skip-automerge] (#1592)
a29c1d8c2 refactor(frontend): merge Feed Events and Events Library into one Events tab [skip-automerge] (#1590)
360449704 docs(flow): inventory frontend pages against the event-episode lifecycle [skip-automerge] (#1588)
5da8586fa docs(flow): record the canonical event to episode lifecycle [skip-automerge] (#1586)
```

Eleven commits. PRs #1586, #1588, #1590, #1592, #1594, #1596, #1598,
#1600, #1602, #1604, #1606. `git diff --stat` over the same range touches
no file under `src/` or `package.json` — consistent with Evoni's account.

## §3. Deploy 3 — 2026-09-21 ~21:31–21:38 UTC, frontend and backend

**ATTESTED.** Evoni moved the tree from
`c3ecff3ea5a579fa6169442603fca3530883c72f` to
`24073cb54d3ce9c266d6a74033bf6d8d4399a8ee`, nine commits; her own `git
diff` showed no package or migration changes, eight `src/` files changed.
`node -c` passed on all eight changed backend files; requiring
`src/constants/canonicalBeats` and `src/utils/scriptOverwriteGuard`
succeeded.

Backup `/var/www/html.bak-20260921-pre1625`; served entry
`index-VzSReGOP.js`; HTTP 200. `pm2 restart episode-api-prod-hotfix`:
restart count 8 → 9, online; `/health` returned 200; the startup log
showed the database authenticated, Redis connected, and the server ready
at 21:38:34 UTC.

**MEASURED**, `git log --oneline
c3ecff3ea5a579fa6169442603fca3530883c72f..24073cb54d3ce9c266d6a74033bf6d8d4399a8ee`:

```
24073cb54 docs(audit): file a note on episode_scripts column drift [skip-automerge] (#1625)
a3ba18cc9 fix(routes): refuse silent script overwrites [skip-automerge] (#1624)
8fea73227 fix(services): guard scene plan inserts against duplicates [skip-automerge] (#1621)
218518ccc docs: census the script pipeline [skip-automerge] (#1618)
1d8993a6a feat(services): canonical beats for Lala's persistent phone [skip-automerge] (#1616)
aa311070a refactor(services): generate episodes on the canonical SAL beats [skip-automerge] (#1614)
17bd88770 feat(services): complete canonical SAL beat table, correct beat 5 [skip-automerge] (#1612)
fe7a22e2f refactor(services): one canonical SAL beat structure [skip-automerge] (#1610)
78e547f5e docs(flow): record SAL production decisions [skip-automerge] (#1608)
```

Nine commits, matching Evoni's own count. PRs #1608, #1610, #1612,
#1614, #1616, #1618, #1621, #1624, #1625. `git diff --stat` over the same
range confirms exactly eight `src/` files changed and none under
`src/migrations/` or `package.json`:

```
src/constants/canonicalBeats.js         | 329 ++++++++++++++++++++++++++++++++
src/routes/episodeBriefRoutes.js        |   9 +-
src/routes/episodeScriptWriterRoutes.js |   6 +
src/routes/worldEvents.js               |  39 +++-
src/services/episodeGeneratorService.js | 159 +++++++++------
src/services/feedMomentsService.js      | 169 ++++++++--------
src/services/scenePlannerService.js     |  22 +--
src/utils/scriptOverwriteGuard.js       |  48 +++++
8 files changed, 624 insertions(+), 157 deletions(-)
```

## §4. The 2026-09-19 backups and bundle timestamp — not ruled

**MEASURED/ATTESTED, carried forward from §1 and §2 above, not
re-derived here.** §1's served bundle predates this record's own first
deploy by one day (Sep 19 14:14, deploy on Sep 20); §2 found three
`.bak-20260919*` directories already on the box. Together these are
consistent with an unrecorded frontend write on 2026-09-19, but this
record does not identify who or what performed it, whether it was a
deploy, a manual copy, or something else, or whether it reached
production traffic. No agent session or Fix Plan revision known to this
record's author accounts for a 2026-09-19 write. This is recorded as an
open question, not ruled on.

## §5. Log check — 2026-09-21, after Deploy 3

**ATTESTED.** Evoni attests: `logs/error-13.log` begins 2026-09-18
15:13:03 — the log's own coverage window does not reach before that date.
Within that window, no `EpisodeScript`, `episode_scripts`, `script_text`,
or `script_json` errors appear anywhere in `logs/`; the only matches for
those terms were `ENOENT` errors for `.js.map` files, unrelated. No
`"column … does not exist"` error appears in any file under `logs/`,
which Evoni's search included `dev-error-5`, `-7`, `-9`, `-10`, `-11`,
`-12` and their matching `dev-out` files — logs from development
processes that have run on this box.

Cited by relation only: `docs/audit/Canon_EpisodeScripts_ColumnDrift_2026-09-21.md`
documents that canon's live `episode_scripts` table matches neither of
its own migrations and lacks the `script_text`/`script_json` columns the
`EpisodeScript` model's one writer depends on — a schema mismatch that,
if triggered, this record's log check would be positioned to catch. It
did not catch one in this window. This record does not use the absence
of an error to rule the drift note's open question either way — a clean
log over a three-day window is not proof the code path was ever
exercised in that window, and this record makes no claim that it was.

Cited by relation only, on the `dev-*` log files themselves:
`PROJECT_CONTEXT.md`'s "Deployments" row (§0) records that
`dev.primepisodes.com`'s DNS "still points at the prod box" — the
presence of development-process logs on this production box is
consistent with that standing fact. This record does not investigate
further and does not characterize it as a defect.

## §6. Evoni's own action — 2026-09-21, after Deploy 3

**ATTESTED, and explicitly not a deploy.** After Deploy 3, Evoni deleted
all episodes of *Styling Adventures with Lala* through the application UI,
to start the season fresh. This did not move any tree, restart any
process, or touch any file on the production host outside the
application's own database writes. It is recorded here, separated from
§1–§3, so a future reader who observes zero episodes in production knows
the cause was Evoni's own deliberate action, not a deploy failure or data
loss.

## §7. What this document does not do

This document:

- does not rule on §4's 2026-09-19 discrepancy — who or what wrote the
  bundle, or whether it reached traffic;
- does not rule on `docs/audit/Canon_EpisodeScripts_ColumnDrift_2026-09-21.md`'s
  open schema question, and does not treat §5's clean log check as
  resolving it;
- does not discharge any owed item recorded in `PROJECT_CONTEXT.md` §6.5
  or any Fix Plan revision — whether any of §1–§6 above discharges
  something is for that document's own refresh, or a future Fix Plan
  revision, to state;
- mints no FD, XK, or PE number;
- amends no filed document — `F-Deploy-1_Deploy_2026-09-20.md`,
  `docs/audit/Canon_EpisodeScripts_ColumnDrift_2026-09-21.md`, and
  `PROJECT_CONTEXT.md` are cited above, not edited;
- performs no deploy of its own and makes no host, AWS, database, or
  Cognito contact — every ATTESTED claim above is Evoni's own account,
  taken outside any agent session; every MEASURED claim is a repository
  read this filing session performed itself, against `origin/main`, not
  against any host.

## §8. Tails — re-derived, not carried

```
$ ls docs/audit/ | grep -E '^FD-[0-9]+_' | sort -t- -k2 -n
FD-66_Model_Migration_Contract_Mismatch_2026-08-18_DRAFT.md
FD-69_Unauthenticated_Token_Issuance_2026-08-22_DRAFT.md

$ ls docs/audit/ | grep -E '^XK-[0-9]+_'
XK-2_Extent_Census_2026-09-05.md

$ grep -oE 'PE #[0-9]+' docs/audit/Session_PE_Roster.md | sort -t'#' -k2 -n | tail -1
PE #68
```

Unchanged from the last register tail check this session found (#1625's
own basis). Nothing minted here.

## §Standing

§1, §2, §3, §5, and §6 each carry an ATTESTED clause (Evoni's own account
of actions and reads made personally, on the production host, not
reproducible from a clone) and, where a commit range is involved, a
MEASURED clause (a `git log`/`diff` read against this repository,
reproducible by anyone with a clone), marked separately, never merged
into one standing. §4 carries both prior sections' standings forward
without re-deriving them, and states plainly what it does not rule on.
Nothing in this document is labelled RULED. No host, AWS, database, or
Cognito contact was made by the agent session that filed it — every
MEASURED claim above reads this repository only. Production's freeze is
lifted (`F-Deploy-1_Fix_Plan_v1.53.md` §1); agent sessions still never
touch hosts, AWS, RDS, or Cognito (`CLAUDE.md`), unchanged by that lift or
by this record.
