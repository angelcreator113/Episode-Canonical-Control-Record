# PROJECT CONTEXT — Prime Studios / Episode Canonical Control Record

**Basis:** `origin/main` at `213edbab10de2b07759a2c9b6969e53305f3b6cd` (2026-09-20). Derived from `/wake-up` plus targeted repo reads for the items this pass names below; facts are MEASURED (checkable in the repo) unless marked ASSERTED (the register says so; requires host/AWS/DB contact to verify, which agent sessions never make). Task #1565. Compared from this file's own prior stated Basis line, `33cabcb8a8dc9824ceae5e4b9b4441ef172e541a` (2026-09-18, PR #1521). `git log --oneline 33cabcb8a8dc9824ceae5e4b9b4441ef172e541a..origin/main` contains 21 commits, each confirmed with `git show --name-only`. **Three are this pass's own scope:** `2964fce5d` (PR #1560, `docs/audit/F-AUTH-1_SessionRefresh_Read_2026-09-19.md` only), `bf60ab1f7` (PR #1562, `docs/audit/F-AUTH-1_Fix_Plan_v2.77.md` only), and `213edbab1` (PR #1564, `src/routes/auth.js`, `src/services/cognitoPasswordAuthService.js`, `tests/integration/auth.integration.test.js`, `tests/unit/routes/auth-refresh-cognito.test.js` (new), `tests/unit/services/cognitoPasswordAuthService.test.js`). **The other eighteen touch no `src/`, `.eslintrc.js`, or `jest.config.js` path and are out of this pass's scope:** `docs/audit/`- or `docs/`-only filings `b791439bc` (PR #1558), `a7fe6206f` (PR #1556), `23887d44a` (PR #1554), `071a740ae` (PR #1548), `97639979e` (PR #1550), `91940e489` (PR #1526), `3002cdb81` (PR #1535), `e92610563` (no PR number in subject); frontend IA/navigation/Production-Checklist commits `c59492840` (PR #1552), `34b3da26f` (PR #1546), `ce54a1427` (PR #1544), `5667d9a87` (PR #1542), `8d4a96162` (PR #1540), `7001e31f5` (PR #1533), `13a6c5449` (PR #1531), `15abdbafb` (no PR number in subject), `7e9f91d1d` (no PR number in subject). **Eight of those nine touch `frontend/` directly** (`git show --name-only`: `c59492840`, `34b3da26f`, `ce54a1427`, `5667d9a87`, `8d4a96162`, `7001e31f5`, `13a6c5449`, `15abdbafb`); the ninth, `7e9f91d1d`, touches only `docs/episode-production-architecture.md` and is grouped here by subject, not path. **None of this pass re-derives §4.6's IA/Sidebar line or its frontend file/CSS/Vitest counts against those eight** — §4.6 below is touched only for the F-Auth-4 interceptor/logout-on-expiry sentence this pass's own scope names (§0 item 5, §4.1); its IA list and counts are carried unchecked against a `Sidebar.jsx` that changed twice in this window (`34b3da26f`, `8d4a96162`) and an `App.jsx` that changed twice (`ce54a1427`, `5667d9a87`) — see the new §10 suggested item below. And this file's own prior refresh, `ab1ac90b7` (PR #1523 — Task #1522's own resulting commit, already fully reflected in this file). **This pass's scope: §0 item 5** (append the v2.77 §8(a) Cognito refresh-exchange finding), **§4.1** (replace the stale `/refresh`-is-HS256-only sentence; re-derive the `src/` file count), **§4.6** (the F-Auth-4 interceptor's refresh call now reaches a Cognito exchange server-side), **§4.8** (re-derive the validate-routes row and the collected-test count from fresh runs), **§6.1** (F-AUTH-1's authority moves to `v2.77`), **§6.3** (re-derive the FD/XK/PE tails with both instruments at this basis), **§6.5** (three new DONE rows, two new OWED rows), and **§10** (two new suggested items). Every other section, and items 1–12 in §10's numbered list, is untouched by this pass.

**How to use this file:** it is the knowledge file for the Claude Desktop project and the first thing a Claude Code session should read after `/wake-up`. It replaces the stale facts in the old `CLAUDE.md`, `README.md`, `.github/copilot-instructions.md`, and `SESSION_HANDOFF.md`. When a fact here changes, the change lands as a task through the workflow in `DEVELOPMENT_WORKFLOW.md`, not by hand.

---

## 0. Ten things to know before anything else

1. **Production is FROZEN.** The prod box (`episode-backend`, 54.163.229.144) serves the populated canon database through a process whose on-disk `.env` points at a second, empty database. Any restart, deploy, reboot, or `.env` edit can silently swap production onto nothing. See §7 and `F-Deploy-1_PROD_SplitBrain_HAZARD.md` (repo root).
2. **Agent sessions never touch hosts, AWS, RDS, or Cognito.** Evoni does that personally. The one time an agent session ran an AWS command it was ruled a crossing (v25 Owed Index Amd30 §AF2.6). The repo now enforces this with `.claude/settings.json` deny rules and a PreToolUse guard.
3. **Product code has been mostly static since May 2026.** Feature work stopped around 2026-05-09. Since then the source changes are F-Stats-1's raw-SQL-to-ORM PRs (Aug 1–4), F-AUTH-1's auth hardening (Aug 17–22), and small September test/tooling hygiene PRs. Most other movement is `docs(audit)`.
4. **The repo is mostly an audit register now.** `docs/audit/` holds 427 files: keystone fix plans, findings, evidence notes, and a chain of "Owed Index" amendments (Amd1–Amd30). The register has its own rules (§8). They are the project's real canon.
5. **`POST /api/v1/auth/login` performs a real Cognito exchange — FD-65 is CLOSED entire.** Task #1456 (PR #1457, 2026-09-15) replaced the prior unconditional `401 AUTH_LOGIN_DISABLED` with a real Cognito `InitiateAuth` (`USER_PASSWORD_AUTH`) exchange; `@aws-sdk/client-cognito-identity-provider` is now a dependency, where none existed before. A live `SECRET_HASH` defect found 2026-09-16 was fixed (`PR #1472`, merged `1c578b71ae4a98ce83970b7f180a0b25fe6d79d3`; `F-AUTH-1_Fix_Plan_v2.74.md` §2), and Evoni then performed live verification against the real Cognito pool herself, outside any agent session, closing the issuance half on the merits (`F-AUTH-1_Fix_Plan_v2.75.md` §3, ATTESTED). Precondition 3 — `v2.67` §4's unscoped token-fabrication design question — is likewise discharged, on direct evidence of a poisoned-body request against the real pool (`F-AUTH-1_Fix_Plan_v2.76.md` §4, ATTESTED). `LOGIN_DISABLED` remains `true` in `tests/integration/f-auth-1-fd65.test.js`; neither revision authorizes flipping it. The local HS256 token family (`tokenService`) still exists for `/refresh` and internal flows, narrowed by ruling (b) in `F-AUTH-1_AuthIssuanceSurface_Read_2026-09-15.md` away from being a user-login issuance path. The Cognito login path is deployed and working in production as of 2026-09-18 per `docs/audit/F-Deploy-1_Fix_Plan_v1.52.md` (ATTESTED), without changing FD-65's standing. **`POST /refresh` now exchanges Cognito refresh tokens (MEASURED, code).** `docs/audit/F-AUTH-1_SessionRefresh_Read_2026-09-19.md` measured that `/login` returns a Cognito-issued `refreshToken` but the old `/refresh` handler called only `TokenService.refreshAccessToken` (HS256, local `JWT_SECRET`), so every session ended within one request of the Cognito access token's expiry — refresh could never succeed. `docs/audit/F-AUTH-1_Fix_Plan_v2.77.md` lands Evoni's adopted rulings on that defect (RULED, the wording drafted by another AI assistant outside this repository's sessions, disclosed at its §3): public user sessions become Cognito end to end, HS256 refresh retires for user sessions, and the success contract is pinned to `accessToken`/`expiresIn`/`tokenType`. Task #1563 (PR #1564, squash `213edbab10de2b07759a2c9b6969e53305f3b6cd`) implements v2.77 §8(a): `POST /refresh` now calls `cognitoPasswordAuthService.refreshWithCognito` (Cognito `InitiateAuth`, `AuthFlow: REFRESH_TOKEN_AUTH`), per v2.77 §7's determination that `REFRESH_TOKEN_AUTH` is the applicable mechanism (the live client's `RefreshTokenRotation` reads null/not-configured, per Evoni's ATTESTED app-client console read at v2.77 §6). **Not deployed (prod FROZEN) and not live-verified** — v2.77 §8(b), a maintainer-performed live refresh verification against the real pool, is owed by Evoni and requires a deploy she decides; implementing §8(a) does not close the refresh work.
6. **The old `CLAUDE.md` was wrong in six places**: the DB is AWS RDS, not Neon; `src/routes/memories.js` is now a 10-file directory; routes use `requireAuth`, not `optionalAuth`; Tailwind v4 is imported; the dev port is 5174, not 5173; several files still use `claude-sonnet-4-20250514`. Corrected in the new `CLAUDE.md`.
7. **Issue-driven work is new.** Historically only one issue mattered (#708, the auto-merge `-X ours` incident log), and work flowed through PRs. September 2026 introduced `claude-task` issues plus `/close-loop`; use the issue body as the dispatch contract and Rule 7 for push, PR create, merge, and branch delete.
8. **Branch protection on `main`** requires three checks (Cost Exposure Audit, Tests, Route Validation), zero reviews, and admins can bypass. **All three automation workflows are now `disabled_manually`**: Auto-merge to `dev`, Deploy to Production, and — as of 2026-09-17 — Deploy to Development (MEASURED via API, re-measured this session: `state: disabled_manually`). Deploy to Development is not dispatched until the gated reconciliation session has run, per `docs/audit/F-Deploy-1_Fix_Plan_v1.50.md` §2 (§7 below).
9. **`dev` is dead.** It is 328 commits behind `main` with 81 unmerged commits of its own (last 2026-06-27). Base every PR on `main`.
10. **The product intent** is a solo-creator "franchise OS": the LalaVerse world (DREAM cities, the Feed, events, wardrobe), the show *Styling Adventures with Lala*, and the memoir novel *Before Lala* (protagonist JustAWoman), with AI generation everywhere. The architectural keystone still unbuilt is Director Brain: making the franchise tier the single canon source instead of the dozens of private JavaScript literals that production code carries today.

---

## 1. Identity and people

| | |
|---|---|
| Repository | `angelcreator113/Episode-Canonical-Control-Record` (private). Description: "The Episode page is the canonical index of everything that happened because of this episode — NOT the system that manages those things." |
| Owner / operator | Evoni, also JustAWomanInHerPrime (JAWIHP), GitHub `angelcreator113`. Windows PowerShell laptop. Sole developer since May 2026. |
| Second developer (historical) | `TySteamTest` — 1,578 commits, January to May 2026. Also the identity under which a GitHub App squash-merged autonomous PRs (PE #58–#60). |
| Claude | 1,733 commits authored as "Claude" plus co-author trailers on many more; every `docs(audit)` document is "Claude, with JAWIHP / Evoni". |
| Bots | `github-actions[bot]` 707 (auto-merges to `dev`, now disabled), `copilot-swe-agent[bot]` 15, `dependabot[bot]` 14. |
| Deployments (ASSERTED) | `primepisodes.com` → prod box, frozen, ~3 months behind `main`. `dev.primepisodes.com` → DNS still points at the prod box (v1.40); the dev box `episode-dev-backend` (54.87.253.45) is deployed by manual SSM dispatch. |
| Brand names | Prime Studios (studio/company), LalaVerse (world), *Styling Adventures with Lala* / SAL (show), *Before Lala* (Book 1, JustAWoman's memoir), Lala (the show's protagonist; the "daughter" in one framing, a "sentient fashion game character" in another — an unresolved canon split), Amber (the in-app AI assistant persona). |

---

## 2. History in one page

| Period | What happened | Evidence |
|---|---|---|
| Dec 31 2025 | Requirements v2.0, technical architecture, and 8–10 week roadmap written for an "Episode Metadata Storage Solution" for *Styling Adventures w Lala*: PostgreSQL, Express, Cognito, S3, Lambda thumbnails, ECS. Target launch Feb 14 2026. | `Mvp/*.txt`, `README.md` |
| Jan 2026 (415 commits) | Phase 0–2: repo, CRUD API, thumbnails gallery, search, migrations, shows/episodes/assets. Global `paranoid: true` added 2026-01-09 without a schema-wide `deleted_at` (root of XK-1). | `src/migrations/2024…`, `docs/PHASE_*` |
| Feb 2026 (680) | Video-production suite: scene composer, thumbnail templates, timeline editor, video composer, game-show features, wardrobe library, AI edit maps. From Feb 20: storyteller books/chapters, character registry, universes — the *Before Lala* novel tooling begins. | `docs/SCENE_COMPOSER_*`, migrations 20260220+ |
| Mar 2026 (1,472) | The LalaVerse pivot: social profiles (the Feed), franchise knowledge (Show Bible / Franchise Brain), world characters, relationships, story engine, texture layers, novel intelligence, `memories.js` split into `src/routes/memories/` (PR #328, 2026-03-25). | migrations 20260302–20260324 |
| Apr 2026 (1,881, peak) | DREAM cities, world map, consolidated world pages, Phone Hub and screen links, feed moments, financial ledger, wardrobe product shots, phone missions/playthrough. Last feature session handoff dated 2026-04-14. | `SESSION_HANDOFF.md`, migrations 20260725–20260807 (file dates, not commit dates) |
| May 2026 (299) | Six incidents in one week (outages from shared-box deploys, autonomous PRs, `-X ours` merges) start the audit program. F-AUTH-1 backend sweep (12 CPs) lands. F-App-1 ships. Prod split-brain found May 29–30; freeze declared; automated deploy breaches it May 30; prod found 502 for days June 1. | `docs/audit/F-Deploy-1_G1_Audit.md`, root incident docs |
| Jun–Jul 2026 (283) | F-Deploy-1 executes: credential rotations, a parallel-tree restart plan that is abandoned, all-stopped incident June 27, split-brain aligned July 6, dev box + SSM deploy path July 10–14, burn-in, keystone closed July 22. F-Stats-1 Phase B begins. Frontend IA audit and ghost cleanup (PR #896). | `docs/audit/F-Deploy-1_Fix_Plan_v1.*` |
| Aug 2026 (221) | F-Stats-1 ORM PRs; F-AUTH-1 reopened (FD-63), 95 handlers promoted (#1039), `/test-token` deleted (#1044), FD-68 shipped (#1087/#1088), password login disabled (#1100). Handoffs v23–v25; the v25 Owed Index and its amendment chain. Cognito pool topology decided (Branch B, Aug 28). | `docs/audit/F-AUTH-1_Fix_Plan_v2.4x–v2.68` |
| Sep 2026 (through PR #1314) | Amd27–Amd30; canon schema reads; an authorized-but-crossed AWS enumeration; correction banners; issue-driven task workflow; `/close-loop`; Template Studio connection fixes and schema-gap census; test setup/DB-guard hygiene (#1291/#1314); Windows test fragility fixes; and the F-Tools skill-body constraint finding. | `docs/audit/v25_Owed_Index_Amd2*`, `docs/audit/F-Tools-1_SkillBodyConstraint_2026-09-08.md`, `.claude/skills/close-loop/SKILL.md` |

Repository totals: 5,255 commits on `main`; ~1,168 PRs; commit types `fix` 1,307, `auto-merge` 710, `docs` 513, `merge` 500, `feat` 237. Source-touching commits per month: Jan 152, Feb 491, Mar 1,071, Apr 619, May 33, Jun 2, Jul 3, Aug 17.

---

## 3. What the product is (intent)

Three tiers, one operator:

- **Franchise tier** ("LalaVerse" in the sidebar): `universes`, `franchise_knowledge` (the Show Bible / Franchise Brain, 102 seeded "franchise law" rows), the DREAM world (Dazzle District, Radiance Row, Echo Park, Ascent Tower, Maverick Harbor), social systems (15 influencer archetypes, celebrity tiers, gossip outlets), a cultural calendar (42 events in story-year 8385), and the Character Registry (`registry_characters`, the hub of the character graph).
- **Show tier / Producer Mode** (`/shows/:id/world`, `WorldAdmin.jsx`, 8,122 lines): the *Styling Adventures with Lala* production loop. World events (invitations, prestige, cost, dress code) get injected into episode scripts as `[EVENT: …]` tags; episodes are evaluated (SLAY/PASS/SAFE/FAIL) and apply stat deltas to `character_state` (Prime Coins, Reputation, Influence, Brand Trust); wardrobe, scene sets (Konva Scene Studio), Lala's Phone (screen overlays, tap zones, missions, playthrough), career goals, opportunities, and a financial ledger.
- **Write tier**: *Before Lala*. `storyteller_books → chapters → lines → memories`. WriteMode (`/write/:bookId/:chapterId`) gives voice-matched AI prose (voice-to-story, continue, deepen, nudge, rewrite options, scene planner) grounded in a Memory Bank of confirmed character memories and a 15-loader context block. A Story Engine can generate a 50-chapter arc and full stories from hardcoded character DNA, but its UI pages were deleted as ghosts in July; the backend survives without a caller.

The intended keystone, **F-Franchise-1 / Director Brain**, is that the franchise tier is currently *write-only*: `Universe` is read by three shallow routes and no generator; every generator carries its own canon literal (`WORLD_CONFIGS`, `DREAM_INFRA`, `CHARACTER_FOLLOW_PROFILES`, `SEED_GOALS`, `JAWIHP_VOICE_DNA`, frontend `data/*.js`), and those copies disagree (three different "Book 1 casts", two Lala origin stories, legacy city names still in the feed scheduler). Director Brain is last in the locked fix sequence and has zero repository presence.

---

## 4. Architecture (measured)

### 4.1 Backend

- **Stack:** Node 20, Express 5.2, Sequelize 6.37 on PostgreSQL 15, `@anthropic-ai/sdk` 0.74, `aws-jwt-verify`, Bull + Redis (optional), Socket.io, sharp, fluent-ffmpeg, canvas, multer. 707 files under `src/` (`find src -type f -name '*.js' | wc -l`, re-derived at this basis), ~190k lines.
- **Composition:** `src/app.js` (1,744 lines) is the whole HTTP composition; `src/server.js` (293) owns the process. Boot order: `dotenv` (cwd-relative at `server.js:7`; the PM2 ecosystem files load `.env` by explicit path, which is why a bare `pm2 start src/server.js` failed on 2026-06-01) → `services/aiCostTracker` and `services/aiResponseCache` monkey-patch the Anthropic SDK → `require('./models')` (fatal on failure) → async DB authenticate → middleware → ~160 route mounts → static SPA serving from `frontend/dist` → 404/error handlers. Two in-process schedulers start at boot: CFO agent every 6h, feed scheduler every 4h (`app.js:666–675`, skipped in test).
- **Middleware order:** `trust proxy=1` → `/ping` → CORS (hard-coded allowlist incl. raw EC2 IPs, plus LAN IPs) → helmet with CSP off → JSON 10 MB → (F-AUTH-1 FD-67 Option 1, PR #1185 / `7a1eb427c`, merged 2026-09-02: the app-wide `optionalAuth` mount that used to run here at `app.js:236` is removed; every route that relied on it for optional identity now carries its own explicit `optionalAuth`) → `attachRBAC` → `captureResponseData` → rate limiters (500 req/15 min/IP on `/api`, 60 writes/min/IP) → `/health` → routes.
- **Mount table (highlights):** `/api/v1/memories` hosts seven routers (the `memories/` hub plus six more); bare `/api/v1` hosts sixteen (markers, export, beats, evaluation, world, worldEvents, worldStudio, careerGoals, arcRoutes, …); `/api/v1/episodes` hosts eleven. Mount order is the only arbiter of param-route shadowing. Legacy unversioned mounts survive: `/api/scripts`, `/api/footage`, `/api/scene-links`, `/api/decision-analytics`, `/api/youtube`. Full table: `src/app.js:425–1551`.
- **Route loading:** ~30 modules use `trackRouteLoad()` (failure → 500 "Routes not available" and visible at `/api/v1/debug/routes`); ~100 use inline try/catch (failure → prefix silently 404s). `npm run validate` statically checks every route file is registered.
- **Auth model:** dual verifier by JWT `alg` (`src/middleware/auth.js`): RS256 → Cognito (`aws-jwt-verify`, lazily built; missing/placeholder config → `500 AUTH_CONFIG_MISSING`), HS256 → `tokenService` with `JWT_SECRET` (≥32 chars). Both yield an equally trusted `req.user`. `requireAuth` is on ~1,400 handler declarations across 133 route files; `optionalAuth` survives on ~64 public catalog GETs (Tier 4) and two "degrade on infra failure" routes (Tier 3). `authorize(['ADMIN'])` guards ~35 sites; a separate lowercase `rbac.js` scheme guards four legacy files. `/api/v1/auth/login` now performs a real Cognito exchange (Task #1456, PR #1457; not live-verified — see §0 item 5). Of the other four routes, they no longer share one shape: `/logout` and `/me` are gated by `jwtAuth.js`'s `authenticateJWT`/`optionalJWTAuth`, which PR #1452 (Task #1451) changed to delegate to this same dual `verifyToken` instead of calling `tokenService` directly — both now accept HS256 *and* RS256/Cognito tokens. `/validate` still calls `TokenService.verifyToken` directly, bypassing `jwtAuth.js` entirely, and **remains HS256-only** (re-checked at this basis: `src/routes/auth.js:327` calls `TokenService.verifyToken(token)`, unchanged by PR #1452 or by the `/refresh` rewrite below). **`/refresh` no longer calls `TokenService` at all** (Task #1563, PR #1564, per `F-AUTH-1_Fix_Plan_v2.77.md` §8(a) — see §0 item 5): it now calls `cognitoPasswordAuthService.refreshWithCognito(refreshToken)`, which exchanges the token via Cognito `InitiateAuth`, `AuthFlow: REFRESH_TOKEN_AUTH`, and returns exactly `accessToken`/`expiresIn`/`tokenType` (`src/routes/auth.js:189-198`) — never `idToken`, never a replacement `refreshToken` (non-rotation design). `refreshWithCognito` sends no `SECRET_HASH` and fails closed with `AUTH_CONFIG_MISSING` if the app client has a secret configured, rather than guess a per-user hash it cannot compute (`src/services/cognitoPasswordAuthService.js:111-127`). Errors map to fixed-message codes, never provider text: `401 AUTH_REFRESH_FAILED` (invalid/expired/revoked, and the catch-all default), `429 AUTH_REFRESH_RATE_LIMITED` (`TooManyRequestsException`), `503 AUTH_SERVICE_UNAVAILABLE` (network-infra error codes), `500 AUTH_CONFIG_MISSING` (`src/routes/auth.js:80-107,205-238`).
- **DB config:** `src/config/sequelize.js` — development/test parse `DATABASE_URL` (test prefers `TEST_DATABASE_URL`); **production ignores `DATABASE_URL` and reads `DB_HOST/DB_USER/DB_PASSWORD/DB_NAME` only** (PE #41). Global `define`: `underscored`, `timestamps`, `paranoid: true`, `freezeTableName`. `server.js` still runs one boot-time DDL (shows unique partial indexes). **`DB_NAME` default mismatch:** `sequelize.js`'s CLI migration config (`sequelize.js:89`) defaults `DB_NAME` to `episode_metadata_dev`; `database.js`'s runtime pool (`:32`) and `docker-compose.yml`'s `POSTGRES_DB` (`:10`) both default to `episode_metadata`. Left unset, `npm run migrate` targets a database the running app never reads. Documented as a caveat in `docs/LOCAL_DEV_LOGIN.md` §4; set `DB_NAME=episode_metadata` explicitly to avoid it.
- **CORS:** the allowlist in `app.js` (`allowedOrigins`, `:159-182`) mixes hardcoded literals (localhost/127.0.0.1 ports, `primepisodes.com` domains, raw EC2 IPs) with entries computed live at each backend boot by `getLocalNetworkOrigins()` (`:144-157`), which walks `os.networkInterfaces()` and appends every non-internal IPv4 address across the dev ports. The LAN entries are never stored — they exist only in the running process's memory. A "wrong LAN IP" symptom is therefore usually a stale backend process that hasn't recomputed its interfaces (e.g. after a network change), not a config file to edit.
- **Workers:** PM2 `episode-worker` runs `src/workers/start.js`: Bull `video-export` renderer (ffmpeg/canvas → S3) and a DB-polling `generation_jobs` worker (scene generation via RunwayML/Replicate/fal). In non-production `server.js` embeds the scene worker so `npm run dev` processes jobs.
- **AI plumbing:** `aiCostTracker` wraps `Messages.prototype.create`, logs every call to `ai_usage_logs`, and enforces `AI_DAILY_BUDGET_USD` (default $50, in-process, resets at UTC midnight or restart; rejects with 429). `aiResponseCache` adds a Redis SHA-256 cache for non-streaming, low-temperature calls. `aiRateLimiter` (30 req/5 min per user or IP) is imported by 56 route files.

### 4.2 Story / AI writing domain (`src/routes/memories/` and neighbours)

- `src/routes/memories.js` **does not exist**; PR #328 (2026-03-25) split it into `memories/{index,helpers,core,interview,voice,stories,planning,assistant,engine,extras}.js`, 12,867 lines total (engine.js alone 5,511). `CONTRIBUTING.md`'s domain table is accurate except that the 17 context loaders live in `engine.js`, not `helpers.js`.
- Every memories route is `requireAuth` (+ `aiRateLimiter` for AI calls), changed in the F-AUTH-1 backup commits of May 8–9.
- **Context system:** `getCharacterVoiceContext(characterId)` (helpers.js:144) builds a voice block from `registry_characters` fields and `voice_signature` JSONB; `loadWriteModeContext(characterId)` (engine.js:2295) resolves PK → `character_key` and runs 15 loaders in parallel (memories, relationships, threads, locations, canon events, prose anchor, voice cards, dramatic irony, follow influence, voice fingerprints, arc context, therapy, growth logs, franchise knowledge, recent world events); `buildWriteModeContextBlock` joins only non-null sections, prose anchor capped at 600 chars. `generate-story` adds `loadCharacterProfile` and `loadWorldState` (17).
- **WriteMode endpoints:** voice-to-story (SSE), story-edit/continue/deepen/nudge, ai-writer-action (continue|dialogue|interior|reaction|lala), rewrite-options, scene-planner, prose-critique, chapter-synopsis, scene-transition, story-outline, story-planner-chat. The "PNOS 5-act" system is `WRITE_MODE_ACT_VOICE` act_1..act_5 (stories.js:29–56) chosen by a request field; the `storyteller_chapters.pnos_act` column exists in a migration but not in the model.
- **Story Engine (engine.js):** hardcoded `CHARACTER_DNA` (just-a-woman, the-husband David, the-witness, the-comparison-creator Nia Vale, the-almost-mentor, lala); `ARC_SYSTEM_PROMPT` generates exactly 50 chapter briefs in four phases (establishment 1–10, pressure 11–25, crisis 26–40, integration 41–50), nine tones, wound clock from 75, a required Lala bleed in chapter 47; generate-story writes 3,300–4,800 words with a Haiku quality gate and background auto-extraction of memories, relationships, locations, and events; pipeline/batch generation uses a process-local `backgroundJobs` map (lost on restart). **No frontend caller exists** for the arc/task/generate/revise/batch/consistency endpoints since the ghost-page deletion (PR #896).
- **Amber** (assistant.js): a JSON-command interpreter with ~26 server-executed actions, SSE streaming, a recycle bin over raw `deleted_at` SQL; `amberDiagnosticRoutes` can shell out to the `claude` CLI on the server (`execute`, behind `requireAuth` only); `amberSessionRoutes` does ElevenLabs TTS with 20/hr and 5/hr limits; `lambda/alexa-amber-skill` is an Alexa front end.
- **Model IDs in code:** `claude-sonnet-4-6` ×103, `claude-sonnet-4-20250514` ×60 (storyEvaluationRoutes, consciousness, generate-script-from-book, onboarding, stories.js root, textureLayerService, worldStudio, franchiseBrain guard, calendar ripples, relationships, hair/makeup, tierFeatures, characterDepth), `claude-haiku-4-5-20251001` ×45. `stories.js:146` lists a non-existent fallback `claude-sonnet-4-5-20250514`. Retry patterns vary from a two-attempt MODELS loop to bare single calls.
- **Story Evaluation Engine v2** generates three blind voices and an editorial synthesis (header says "Opus"; code uses sonnet-4-20250514). Texture layers: 9 generators. Novel intelligence: edit-as-signal → voice rules. Manuscript export: DOCX/PDF of approved lines. Press: four seeded press characters.

### 4.3 World / franchise / social domain

- ~29,400 lines across 49 route files; nothing functionally changed since 2026-05-09 except ORM conversions and auth promotions.
- `worldEvents.js` (4,005 lines, ~60 handlers): event CRUD, inject-into-script, invitation pipeline (generate → edit → render → approve; PDF; RunwayML animation), episode generation from events, financial pressure/forecast, venue images via fal, overlay approval writing `assets` rows, a deterministic next-event suggester reading `character_state` for `justawoman` while `generate-script` reads `lala` (the F-Sec-3 key drift).
- `worldStudio.js` (3,709): character ecosystem generator with its own `WORLD_CONFIGS` (two worlds: `lalaverse` protagonist Lala; `book-1` protagonist JustAWoman), `syncToRegistry` mirroring `world_characters` → `registry_characters`, relationship seeding from a 43-rule pairing table, `world_locations` CRUD with the `DREAM_INFRA` seed, world state snapshots, DREAM map upload, intimate-scene generation; an inline `CREATE TABLE ecosystem_previews` at line 319 (PE #62).
- `socialProfileRoutes.js` (2,699): the Feed. AI-generated creator profiles; `FEED_CAPS = {real_world: 443, lalaverse: 200}` (soft on `/generate`, hard in the scheduler); hardcoded follow-psychology profiles for `justawoman` and `lala`; the hand-authored `@justawoman` record is locked (403 on eight handlers); `/:id/cross` promotes a profile into a registry character. `feedScheduler.js` runs six sub-agents every 4h at boot and still uses the five legacy city names.
- **Characters live in four tables:** `registry_characters` (canonical dossier, `character_key`, role/status/depth ENUMs, ~35 JSONB blobs, target of ~15 associations), `world_characters` (World Studio cast), `universe_characters` (promoted canon, created only by `promote-to-canon`), `characters` (legacy per-show, wardrobe owner). `character_key` is a free-text slug that drifts between `lala`, `justawoman`, and `just-a-woman`; five raw-SQL services hardcode `'justawoman'`; the fix is assigned to F-Sec-3.
- **Franchise Brain:** `franchise_knowledge` rows (category/severity/status ENUMs); `POST /franchise-brain/seed` runs the ten seeders in-process; `buildKnowledgeInjection()` prepends active critical rules to prompts in ten consumers. Show Brain canon: 24 episodes/season in three arcs of eight, nine World Laws, six stats, Prime Bank's four currencies, a 14-beat episode structure, "Lala does not know she is in a show."
- **Verified register claims:** the five PE #62 inline `CREATE TABLE` sites and the XK-2 unscoped-write line references all resolve at current `main`.

### 4.4 Production tooling domain (episodes, scenes, wardrobe, assets, phone)

The show-tier production stack is the January–February 2026 metadata/CMS/video-suite code plus the April 2026 phone game. It is still fully mounted; nothing in it has changed functionally since April except F-AUTH-1 auth promotions and F-Stats-1 ORM conversions. Size: 132 top-level route files (63,349 lines) and 33 controllers (18,928 lines; `wardrobeController` 2,320, `sceneStudioController` 1,863, `sceneController` 1,667, `wardrobeLibraryController` 1,596, `episodeController` 1,409).

- **Mount map.** `/api/v1/episodes` hosts eleven routers (episodes, timelineData, wardrobeApproval, scriptGenerator, lalaScript, scriptParse, gameShow, iconCue, cursorPath, musicCue, productionPackage) plus `/:episodeId/phone-state`; `/api/v1/shows` hosts shows and gameShow; scene work is spread over `/scene-sets`, `/scenes`, `/scene-studio-episodes`, `/lala-scenes`; wardrobe over `/wardrobe`, `/wardrobe-library`, `/wardrobe-brands`, `/wardrobe-events`, `/outfit-sets`; `/assets` hosts assets and imageProcessing; then `/compositions`, `/thumbnails`, `/layers`, `/scripts`, `/export`, and `/ui-overlays` (+ `/:showId/ai`, `/:showId/missions`). Mount lines: `src/app.js:634–941` and `1146–1534`.
- **`episodes.js`** (1,360 lines, ~70 handlers): CRUD, `/:id/status`, `/:id/save`, `/:id/enqueue`, `/:id/thumbnail`; sub-resources for wardrobe, outfits (→ `outfitSetsController`), assets, platform, scenes (reorder, stats, from-angle, scene assets), scripts and versions, library scenes and assets (with folders), timeline placements and `timeline/wardrobe/current`, sequence items, video compositions, scene sets (with reorder) and `suggest-scenes`. The file uses both `:id` and `:episodeId` for the same resource and declares library-assets and timeline-placements twice (lines 540–617 and 637–715); the second declarations are reachable only where the paths differ.
- **`shows.js`** (1,487): show CRUD, cover image, `config` / `template` / `wardrobe` reads, and the Prime Bank finance layer: `financial-config`, `financial-summary`, `seed-finance-apps`, `redecorate-finance-app`, `financial-breakdowns`, `financial-suggestions`, `seed-balance`, `wardrobe-config`.
- **`sceneSetRoutes.js`** (2,467; 59 async handlers): scene sets and angles. AI describe/refine/learn-location, base-still upload and generation, angle suggestion (also from an image), camera direction, per-angle generate / generate-video / upload / review / analyze / regenerate / post-process / auto-refine / depth map, a refinement queue with jobs, `by-type` and `for-beat` lookups, a spec sub-resource (generate, validate-angle, create-angles), canvas and objects (the Konva Scene Studio), lock-style, generate-all-angles, time variants, cascade regenerate, promote-to-base, and episode links. Backed by `sceneGenerationService.js` (1,888): fal.ai Flux stills, OpenAI `gpt-image-1` edits and outpaint, Runway `gen3a_turbo` image-to-video, Claude Vision consistency checks, S3.
- **`wardrobe.js`** (1,895): item CRUD, per-item image ops (process-background, product shot, upscale, premium-enhance, add-shadow, analyze, send-to-phone), bulk ops, staging and categories audit, seed, outfit history, pieces, set, and the **money path** `browse-pool` → `select` → `purchase`, which debits `character_state` Prime Coins under the `lala` key (the F-Stats-1 PR 4 conversions and the F-Sec-3 drift both live here). Services: `wardrobeController` 2,320, `wardrobeLibraryController` 1,596, `wardrobeImageService` 1,173 (sharp pipeline, Replicate Real-ESRGAN upscale, Claude Vision colour and tag extraction still pinned to `claude-sonnet-4-20250514`).
- **`assets.js`** (1,355): list / eligible / by-folder / approved / pending, labels, bulk delete / process-background / add-labels / change-type, search, CRUD, approve / reject / process, per-asset labels / usage / download, `config/check`. `imageProcessing.js` (473) adds remove.bg, Runway and Cloudinary operations under the same mount.
- **`compositions.js`** (1,666): composition CRUD, approve / primary / publish, thumbnail and format generation, assets by role, versions (compare, revert, stats), outputs, drafts (save, apply), slot positions. Its `VersioningService` and `FilterService` open their own `pg` pools outside Sequelize.
- **Lala's Phone** (`uiOverlayRoutes.js` 980 + `phoneAIRoutes.js` + `phoneMissionRoutes.js` + `phonePlaythroughRoutes.js`): per-show overlay generation (Flux via `uiOverlayService`), upload, remove-bg, frame, global-fit, style-prefix, category and image-fit, **screen-links** (tap zones), content-zones, and overlay-type CRUD. `phoneRuntime.js` (263 lines) is the single evaluator shared by the editor preview, the AI zone proposals and the reader playthrough: AND-ed `{key, op, value}` conditions (`eq/neq/gt/gte/lt/lte/exists/not_exists`, fail-closed on unknown operators), an action allowlist (`navigate`, `set_state`, `show_toast`, `complete_episode`), missions as objective lists whose `reward_actions` fire once; `phoneConditionSchema.js` (Joi) is shared by the save route, the AI route and the runtime; state lives in `phone_playthrough_state` (unique per user + episode) and `phone_missions` (migrations 20260730–20260802). AI proposals use `claude-sonnet-4-20250514` (`phoneAIRoutes.js:22`). This is the newest production code (PRs #561–#601, April 2026).
- **Feb 2026 video suite, still mounted:** thumbnails (PE #54: `/thumbnails/generate` is captured by `/:id`), scripts / scriptGenerator / lalaScript / scriptParse, gameShows (the Lala Formula 8-act / 11-beat structure), layers, iconCue / cursorPath / musicCue / productionPackage, export (manuscript DOCX/PDF), lala-scenes. The Phase-1/2 controllers for files, jobs, notifications, presence, sockets and search are mounted but their test files are on Jest's ignore list, so none of them has an executed test.
- **Duplicates and dead declarations.** `outfitSetController.js` (170 lines, serves `/api/v1/outfit-sets`) and `outfitSetsController.js` (449, serves `/api/v1/episodes/:id/outfits`) both live: F-Ward-3's subject. The Route Shadowing Survey's six dead declarations all resolve at `main`: `compositions.js` `PUT /:id` at 817 (shadowed by 480) and `GET /search` at 1188 (by `GET /:id` at 458); `thumbnails.js:136`; `sceneSetRoutes.js` `GET /artifact-categories` at 1099 (by `GET /:id` at 229); `wardrobe.js` `/bulk/upscale` at 248 and `/bulk/analyze` at 251 (by `/:id/upscale` at 226 and `/:id/analyze` at 238). No shadowed twin carries auth its live twin lacks; whether they warrant an FD is v25 item 10-B.

### 4.5 Data model and migrations

- 154 files in `src/models/`: 151 Sequelize models + two raw-pg classes (`file.js`, `job.js`); `index.js` (2,072 lines) registers 149 (`SocialProfileTemplate` and `UiOverlayType` never registered) and defines ~150 associations. ~118 UUID PKs, 30 integer PKs. 40 models set `paranoid: true`, 36 `paranoid: false`, ~75 inherit, 17 have `timestamps: false` (paranoid inoperative).
- **Five migration trees exist; only one runs.** `src/migrations/` (211 files, 20240101000001 → 20260818000000) is what `sequelize db:migrate` reads (`.sequelizerc`), and it creates 133 tables. Root `migrations/` (20), `migrations/sequelize-migrations/` (16, misnamed, five are node-pg-migrate), `migrations-node-pg-migrate/` (3, creates the 8 AI-editing tables), and `scripts/migrations/` (226 ad-hoc files, including an unguarded `DROP TABLE shows CASCADE`) are never invoked by any script or workflow. Nine basenames are duplicated across roots; two differ.
- **Consequences:** 40 model tables are not created by `src/migrations`; 21 migration tables have no model; 6 model tables are created by no tree at all (`FileStorages`, `asset_asset_labels`, `layers`, `layer_assets`, `script_learning_profiles`, `script_templates`). Eleven request-path `Model.sync()` calls and five inline `CREATE TABLE IF NOT EXISTS` sites create tables at runtime. A fresh `npm run migrate:up` yields a schema no deployed database matches. **A column-level instance of the same drift, not a table-level one:** `StorytellerBook.js` defines `theme` (`:91-94`) and `pov` (`:95`) as Story-DNA fields, added in `b6d6a7580`; no migration in `src/migrations/` ever adds either column to `storyteller_books` (`grep -rn "addColumn.*storyteller_books" src/migrations/` across all ten files that touch the table names `front_matter`, `back_matter`, `author_name`, `book_type`, `character_key`, `world_id`, `current_arc_stage`, `arc_stage_scores`, `book1_throughline`, `canon_status`, `series_id`, `era_name`, `era_description`, `primary_pov` (added then removed), `timeline_position`, `description` — never `theme` or `pov`). Against a migrations-built schema, reading or writing either column fails with `column StorytellerBook.theme does not exist`.
- **XK-1 (paranoid exposure):** 48 → 37 → 13 → 12 model tables exist without `deleted_at` on a migrations-built DB; inserts through them fail with `column "deleted_at" does not exist`. **FD-66 (DRAFT, P0):** 28 mismatched + 38 no-table models; three routes confirmed 500 (`GET /api/v1/audit-logs`, `POST /api/v1/decision-logs`, `POST /api/v1/thumbnails/:id/publish`).
- **Canon vs prod fork (ASSERTED, from pre-freeze live reads):** canon `episode-control-dev` = 143 tables, populated (episodes 72, shows 10, social_profiles 444, franchise_knowledge 605), with both `pgmigrations` (stopped 2026-01-22) and `SequelizeMeta` ledgers; `episode-control-prod` = 171 tables, empty; 37 prod-only, 9 dev-only; neither a superset. `SequelizeMeta` on canon records migrations whose effects are absent (Amd22). Decision FD-31: canon = `-dev`; prod-only tables preserved in `docs/audit/FD31-prod-only-schema-20260601.sql` (DO NOT RUN).
- **Canon capture update (ATTESTED / MEASURED):** Evoni's own console read was ATTESTED outside any agent session; the 2026-09-17 and 2026-08-29 captures have the same normalized record set (2,760 records, no table or column difference). The capture names canon instance `episode-control-dev`; its evidence note is `docs/audit/EvidenceNote_Canon_Capture_2026-09-17_Note.md`.
- Seeders: 14 files in `src/seeders/` with no npm entry point; `scripts/seed.js` (`npm run seed`) is a raw-pg TODO stub. `docs/DATABASE_SETUP_GUIDE.md` documents node-pg-migrate commands that no longer exist.

### 4.6 Frontend

- React 18.2 + react-router 6 + Vite 7 (`frontend/`), 560 source files, 132 CSS files, 99 Vitest files (~786 cases). Dev server **5174** (HMR 5175), proxies `/api` → `127.0.0.1:3002`.
- **Auth:** local email/password against `/api/v1/auth/login`, tokens in `localStorage`, axios singleton with the F-Auth-4 interceptor (refresh once on `AUTH_INVALID_TOKEN`, wipe and redirect on `AUTH_REQUIRED`; redirect skipped in Vite DEV). Server-side, that route now performs a real Cognito exchange rather than disabling itself (Task #1456, PR #1457 — §0 item 5); the frontend code path itself is unchanged by that PR. **The interceptor's refresh call (`refreshAccessToken()`, `frontend/src/services/api.js:55-64`) now reaches a Cognito exchange server-side too, with no frontend change** — it still POSTs `{ refreshToken }` to `/api/v1/auth/refresh` and reads `response.data.data.accessToken`, and `POST /refresh` now honors that by calling Cognito's `REFRESH_TOKEN_AUTH` instead of failing closed (Task #1563, PR #1564 — §0 item 5, §4.1). `docs/audit/F-AUTH-1_SessionRefresh_Read_2026-09-19.md` is the MEASURED cause of the prior logout-on-expiry: it traced the pre-#1564 end-to-end path and found the old `/refresh` handler HS256-only against the local `JWT_SECRET`, so a Cognito-issued `refreshToken` (minted at `/login`, stored by `authService.js:32`) could never verify there — every session ended within one request of the Cognito access token's expiry (that document's §5, quoted at §0 item 5 above). Still no Cognito code in `frontend/`.
- **IA (Sidebar):** FRANCHISE (LalaVerse, Show Bible, World Dashboard, World Foundation, Social Systems, Culture & Events) · CREATE SHOW (Producer Mode, Shows) · WRITE (Stories, Characters, Relationships) · STUDIO (Timeline Editor, Compositions) · MANAGE (CFO Agent, Site Organizer, Design Agent, Search, Admin, Recycle Bin, Settings). ~121 authenticated routes (~103 live, ~18 redirects); ~23 feature pages reachable only by URL.
- **Design system reality:** `index.css` imports Tailwind v4 (thin utility use); tokens are a Tailwind-blue palette with a secondary `--lala-*` palette (parchment `#FAF7F0`, gold `#B8962E`, ink `#2C2C2C`, Lora / DM Mono); lucide-react in 42 files, emoji glyphs elsewhere; desktop-first media queries (359 `max-width` vs 55 `min-width`).
- **Known debt:** stale nav targets (`Home.jsx` → `/studio/scene-composer`, CommandPalette → `/story-engine`), orphan files (`NewBookModal.jsx`, `StoryInspector.jsx`, `StoryNavigator.jsx`, `StoryEngine.css`), three env-var names for the API base, committed `dist.tar.gz` and `build-output.txt`, tracked `.env.production` despite `.gitignore`, `frontend/README.md` describes a Phase-2 app, `SESSION_HANDOFF.md` Part 2 (device frames, safe-area guides) was never built.
- **Big files:** `WorldAdmin.jsx` 8,122, `WriteMode.jsx` 3,708, `SceneSetsTab.jsx` 3,214, `UIOverlaysTab.jsx` 2,987, `StoryEvaluationEngine.jsx` 2,716, `WorldStudio.jsx` 2,402, `SceneStudio/SceneStudio.jsx` 2,264.

### 4.7 Services and external integrations

`src/services/` holds 108 files (~42,500 lines). Largest: `sceneGenerationService` 1,888, `AssetService` 1,453, `feedScheduler` 1,184, `wardrobeImageService` 1,173. Last service commit 2026-05-07. There is **no shared Anthropic client**: roughly 30 services and many routes each construct `new Anthropic()`; the SDK monkey-patch in `aiCostTracker` is what makes them uniform.

**Six groups.**

1. **AI core.** `aiCostTracker.js` (252) and `aiResponseCache.js` (189) are required first in `app.js:19–21` and wrap `Messages.prototype.create` (call chain: cache → tracker → SDK). The tracker prices every call from a table (`aiCostTracker.js:24–38`: Sonnet $3/$15, Haiku 4.5 $0.80/$4, cache write 1.25×, cache read 0.10×; unknown IDs priced as Sonnet), rejects with 429 when the worst-case estimate would exceed `AI_DAILY_BUDGET_USD` (default $50 in code; its header comment wrongly says "no limit"), and inserts an `ai_usage_logs` row via `setImmediate`. Cache hits never reach the tracker, so they are absent from the logs. `aiModelRouter.js` and `promptCacheHelper.js` are effectively unused. `claudeService.js` is only a Haiku video-script analyzer for `youtubeService`. `iconCueGeneratorService.js:396–420` calls `api.anthropic.com` directly with axios and so escapes tracking, budget and cache.
2. **Story generation.** A `WorldEvent` flows through `episodeGeneratorService` (Haiku: title, description, tags, beat outline) → `scenePlannerService` (Sonnet 4.6, the canonical 14-beat structure) → `episodeScriptWriterService` / `groundedScriptGeneratorService` (Sonnet 4.6 with locked JAWIHP and Lala voice DNA, Haiku franchise-rule guard) → `episodeCompletionService` (SLAY / PASS / SAFE / FAIL and coin finalization) → `feedPostGeneratorService`, `characterSyncService`, `registrySyncService` → `storyGenerationService` (prose formats) → `storyEnrichmentService` (Haiku, fire-and-forget). `arcTrackingService` turns wound-clock / stakes / visibility numbers into narrative text for prompts; `textureLayerService` is still pinned to `claude-sonnet-4-20250514`. All character-generation outputs are proposals the author confirms.
3. **World and feed engine.** `feedScheduler` (six sub-agents: auto-fill, finalize, relate, follow, cross, discover; caps `real_world` 443 / `lalaverse` 200; Sonnet 4.6 profiles + Haiku sparks). It is started at `app.js:675` on a 4-hour interval but returns immediately unless `FEED_SCHEDULER_ENABLED === 'true'` (`feedScheduler.js:1109`; the code comment estimates $30–50 per cycle). **The manual endpoints `POST /api/v1/feed-scheduler/run-now`, `/fill-one`, `/auto-generate` bypass that gate** behind `requireAuth`. No-LLM services: `eventAutomationService` (cultural calendar → draft events), `financialTransactionService` / `financialPressureService` / `financialFeedService` (coin ledger and pressure; `financialPressureService.js:208` UPDATEs the append-only history ledger), `careerPipelineService`, `worldTemperatureService` (COLD 0–15 … VOLATILE 81–100), `feedEngagementService` (viral tiers), `arcProgressionService`. LLM services: `thresholdDetection` and `emotionalImpact` (Haiku; the therapy "knock" that also sends Gmail), `rippleEngine` (Haiku Amber scene proposals), `seasonalEventService` (Sonnet 4.6), `feedEventPipelineService` (Haiku).
4. **Characters.** `characterGenerationService` (one `claude-sonnet-4-20250514` deep profile plus four Haiku calls), `characterFollowService` (sonnet-4-20250514), `characterSyncService` / `registrySyncService` / `registrySync` (post-event and real-time registry mirroring), `decisionAnalyticsService` (raw SQL over `user_decisions`).
5. **Image and video.** `imageGenerationService` is the unified entry point and, despite its header describing DALL-E/Flux routing, maps every use case to fal.ai Flux (`flux-pro/v1.1` $0.005, `flux/dev` $0.003, `flux-pro/kontext` $0.04) and counts them against `AI_DAILY_IMAGE_BUDGET_USD` (default $10); its DALL-E branch is unreachable. OpenAI is still called directly (raw axios) for `gpt-image-1` edits/outpaint in `sceneGenerationService` and `dall-e-3` in `objectGenerationService`, outside any budget. Replicate hosts SAM-2 / grounded-SAM (segmentation), SDXL inpaint, LaMa, flux-fill-pro, rembg (inpainting), DepthAnythingV2, SDXL img2img (restyle), Real-ESRGAN (wardrobe upscale). Runway is used two ways: `api.dev.runwayml.com` `gen3a_turbo` image-to-video, and the legacy `api.runwayml.com` background removal (`AssetProcessingService` reads `RUNWAY_API_KEY`; everything else reads `RUNWAY_ML_API_KEY`). remove.bg is called from six services plus controllers. Cloudinary is an optional enhancement pass. ffmpeg is wrapped three ways (`ffmpegService` defaults to Windows paths). `youtubeService` shells out to `yt-dlp`; there is no YouTube Data API. ElevenLabs TTS is used only by `amberSessionRoutes`.
6. **Infrastructure.** S3 through both `aws-sdk` v2 (`S3Service`, `AssetProcessingService`, `config/aws.js`) and `@aws-sdk/client-s3` v3 (most newer services) with at least ten different bucket env-var names; three parallel SQS wrappers (`sqsService` unused by the app; `QueueService` defaults to LocalStack; `JobQueueService`); `SocketService` (throws at load if `JWT_SECRET` is unset outside test), `PresenceService` and `NotificationService` are in-memory; `notifications.js` sends Gmail SMTP through nodemailer; `tokenService` issues HS256 JWTs; `winston-cloudwatch` is a dependency nothing requires. Agents with no LLM calls: `cfoAgent` (five sub-agents, runs every 6h unconditionally from `app.js:666`, executes `npm outdated` inside the web process, and its Windows `2>nul` redirect creates a stray `nul` file on Linux), `designAgent`, `siteOrganizerAgent`.

**Amber and the Lambdas.** Amber is a persona over Sonnet 4.6: `routes/memories/assistant.js` (1,906) `assistant-command(-stream)` with roster, ecosystem, franchise-brain and arc context; `amberSessionRoutes` greeting / speak / read-story (Haiku + ElevenLabs, 20/hour); `amberDiagnosticRoutes` (574) a scan engine (`AmberFinding`, `AmberScanRun`, `AmberTaskQueue`) that can execute fixes against `CLAUDE_CODE_PROJECT_DIR` behind `AMBER_AUTO_APPROVE_*` flags — a self-modifying path that deserves review. `lambda/alexa-amber-skill` ("prime studios") proxies voice into `assistant-command` with a static bearer; `lambda/video-analyzer` (SQS → S3 → ffmpeg → AWS Transcribe → `PUT /api/v1/edit-maps`) has stubbed face and audio-event detection and a 16.7 MB `function_deploy.zip` committed beside it.

**Dead or orphaned:** `aiModelRouter.js`, `phoneScreenRenderer.js`, `sceneIdentityService.js` (+ `sceneTypePriors.js`), `sqsService.js` (only a validation script requires it), the DALL-E branch of `imageGenerationService`, `winston-cloudwatch`. Legacy Phase-1/2 services (`S3Service`, `QueueService`, `JobQueueService`, `ErrorRecovery`, `VersioningService`, `FilterService`, `scriptsService`, `ThumbnailService`, `ThumbnailGeneratorService`) survive with their own AWS v2 clients or separate `pg` pools. **`ThumbnailGeneratorService.js` no longer constructs its own `Sequelize` instance** — fixed 2026-09-06 (issue #1279, PR #1288) to `require('../models')` like the rest of the route layer; see the Template Studio note below for why.

**Template Studio.** `src/routes/templateStudio.js` (mounted `/api/v1/template-studio`, `app.js:884`) and the `template_studio_id` path through `compositions.js` and `ThumbnailGeneratorService.js` all query a raw-SQL `template_studio` table. Until fixed 2026-09-06, all three used a module-scope, unconditional `new Sequelize(process.env.DATABASE_URL, ...)` — fatal in `templateStudio.js` (`DATABASE_URL` is never set in local dev under this project's convention, so the route failed to load: `Failed to load Template Studio routes: The "url" argument must be of type string. Received undefined`) and non-fatal but identical in the other two. Issue #1280/PR #1281 fixed `templateStudio.js`; issue #1279/PR #1288 fixed the other two — all three now share the app's own `require('../models')` connection. **The table itself has no migration or model in this repo** (`docs/audit/TemplateStudio_SchemaGap_Census_2026-09-06.md`; see §6.5) — a fresh migrated environment has no `template_studio` relation at all, so every route in `templateStudio.js` now fails with a clean HTTP 500 instead of crashing route registration.

**Cost-control facts to remember.** Both daily budgets are in-process counters: they reset at UTC midnight or on any process restart, and N cluster workers mean N × $50. The image budget covers only the fal.ai path; OpenAI, Replicate, Runway, remove.bg, Cloudinary and ElevenLabs spend is neither tracked nor capped. `aiRateLimiter` is 30 requests / 5 minutes per user or IP. `ecosystem.config.js` does not set `AI_DAILY_BUDGET_USD`, `AI_DAILY_IMAGE_BUDGET_USD`, `FEED_SCHEDULER_ENABLED`, `OPENAI_API_KEY` or `CLOUDINARY_*`, so production relies on code defaults unless the on-box `.env` sets them.

**Env keys by provider** (full list in `.env.example`): `ANTHROPIC_API_KEY` (113 refs); `FAL_KEY`; `OPENAI_API_KEY`; `REPLICATE_API_TOKEN` (+ model/version overrides); `RUNWAY_ML_API_KEY` / `RUNWAY_API_KEY`; `REMOVEBG_API_KEY`; `CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET`; `ELEVENLABS_API_KEY`, `ELEVENLABS_VOICE_ID`; `LALAVERSE_EMAIL`, `LALAVERSE_EMAIL_PASSWORD`, `NOTIFY_EMAIL`; `REDIS_HOST/PORT/PASSWORD`; `AWS_REGION` plus the bucket and queue names; `DATABASE_URL` or `DB_HOST/PORT/NAME/USER/PASSWORD` + `DB_SSL=true` against RDS; `JWT_SECRET`; `COGNITO_USER_POOL_ID`, `COGNITO_CLIENT_ID`. Danger switches that must never be set by an agent: `DB_SYNC_FORCE`, `DB_SYNC_ALTER`, `ENABLE_DB_SYNC`, `CONFIRM_FORCE_SYNC`, `CONFIRM_DROP`.

### 4.8 Tests and CI

**Backend (Jest 29).** `jest.config.js`: `testMatch` limited to `tests/unit/**` and `tests/integration/**`, `maxWorkers: 1`, `setupFilesAfterEnv: tests/setup.js`, coverage thresholds all **0**. 162 `*.test.js` files exist under `tests/`; `jest --listTests` collects **142**: 19 are dropped by `testPathIgnorePatterns` (`jest.config.js:22–54`, including `tests/integration/scenes.integration.test.js`) and `tests/api/endpoints.test.js` is outside `testMatch`, for 20 uncollected files total. The exclusions are now named in `jest.config.js` (issue #1260). `tests/setup.js:8–11` no longer throws when `TEST_DATABASE_URL` is unset (PR #1291, merged `ff005de6`): it warns and assigns `DATABASE_URL` only in the `else`, so DB-free suites can start without a Postgres URL. It also mocks `uuid`, the Bull video queue and `aws-sdk`, and mints real HS256 tokens through `tokenService`. `npm test` = `jest --coverage --runInBand --forceExit`; coverage is collected, never enforced, never recorded. DB integration guards now have three shapes: `f-auth-1-fd63.test.js:47` still skips only on `amazonaws.com`; `f-auth-1-g3-clause3.test.js` uses a loopback reachability probe; and the five sibling files fixed by PR #1314 (`auth`, `episodes`, `f-auth-1-fd65`, `scenes`, `wardrobe-money`) skip when `DATABASE_URL` is unset/empty or contains `amazonaws.com`, preserving optional chaining and the existing `episode_metadata_test` clauses. The #1314 limitation is deliberate: a dead local DB URL can still raw-fail in unset-only guards.

**What the backend suite is.** 74 files in `tests/unit/routes` match `*-tier*` (43 `*-tier-promotion*`; cp10 19, cp11 3, cp12 21; four `f-auth-1-fd63-shape*`). 84 of the 92 files in that directory are static-source diff-locks: they `readFileSync` a route file and regex-assert that `requireAuth` is imported, that no `optionalAuth` / `authenticateToken` / lazy-noop survivors remain, that each `router.verb('/path', requireAuth, …)` chain exists, that `authorize(['ADMIN'])` is preserved, and that `// PUBLIC:` markers exist. They never send a request. "Tier" means the F-AUTH-1 Fix Plan v2.37 §5.2 Step 3 disposition tier (1 `requireAuth`; 2 + `authorize(['ADMIN'])`; 3 `optionalAuth({degradeOnInfraFailure:true})`; 4 PUBLIC `optionalAuth` with rationale; 5 env-gated dev-only mount). The runtime complement is `tests/integration/f-auth-1-fd63.test.js` (supertest through `src/app.js`, asserts `AUTH_REQUIRED` / `AUTH_INVALID_FORMAT` codes), `f-auth-1-fd65` (login issues no caller-supplied groups/roles) and `f-auth-1-g3-clause3`. `tests/unit/security/jwt-secret-strict-fail.test.js` (F-SOCKET-1) asserts `SocketService` throws at load without `JWT_SECRET`. Four `phoneRuntime*` files cover the phone evaluator, schema, missions and once-only rewards. `tests/unit/route-health.test.js` requires all 132 route files and hits `/health`. 16 service unit tests exist. F-AUTH-1 v2.37 recorded 2,390 passing at CP12; 1,878 static `it()/test()` sites exist in the collected files (the rest is `test.each` expansion, unverified here).

**Two Windows-only backend test bugs, both fixed at this basis.** `tests/integration/f-auth-1-g3-clause3.test.js`'s `isDatabaseReachable()` probe (added PR #1295) built a `node -e "..."` string with `JSON.stringify(host)` embedded and ran it through `execSync`, which shells via `cmd.exe` on Windows; `cmd.exe`'s quote parsing stripped the inner double quotes, so the nested script received a bare `host:localhost` identifier and threw `ReferenceError` before opening any socket, and the catch returned `false` — the suite reported a clean "unreachable" skip on Windows regardless of real database state, from PR #1295 until PR #1306 (issue #1304, 2026-09-07) switched `execSync` to `execFileSync` with host/port/timeout as separate `argv` elements. Fixed at `b4dc416a`; confirmed against a real, migrated Postgres: 2 passed, 2 total (PR #1306's body does not itself say "Windows" next to that run — the platform is inferred from the defect being `cmd.exe`-specific and from issue #1304's "Lane: laptop (Windows required to observe the bug)" framing, cross-referenced with issue #1276's same-day comment identifying that lane as a Windows laptop, not stated flat in one place). `tests/unit/services/postProcessingService.test.js` separately hard-coded a POSIX path (`/tmp/scene-postproc-abc123/input.mp4`) as the expected `fs.writeFile` argument, while the service builds that value with `path.join()`, which emits backslashes on Windows — fixed at `e9395e0e` (PR #1307, issue #1305) by building the expectation with `path.join()` from the same segments; `postProcessingService.js` itself was not touched. Both bugs surfaced from the same Windows laptop lane, via issue #1276's 2026-09-07 evidence-run comment. The second fix is out of the locked sequence (F-AUTH-1/F-Stats-1) — test hygiene, merged by Evoni as a waiver. During #1313/#1314 review, `node -c` was challenged and tested against a real temp file containing `function ( {`; on Node v22.22.0 it entered `checkSyntax` and exited 1, matching `node --check`, so `node -c` is meaningful in this environment even though `node --check` is clearer for future PR bodies.

**Frontend (Vitest 0.34.6, jsdom, globals; `frontend/vitest.config.js`, no setupFiles).** 99 files (53 pages, 40 components, 3 hooks, 2 services, 1 utils), 793 tests. Mostly module-scope helper tests asserting the URL an API helper calls, structural tests reading a component's own source, and the F-Auth-4 interceptor tests in `services/api.test.js`. Vitest reads `frontend/.env`; a developer-only absolute `VITE_API_URL` once caused ~181 local failures (finding of 2026-07-04); CI gained the Frontend Tests job the same day (#900) but the finding doc still says the gap is open.

**CI (`.github/workflows/validate.yml`).** On `pull_request` and `push` to `main` and `dev`, plus `workflow_dispatch` (#989). Four jobs on Node 20: Cost Exposure Audit; Route Validation (`validate-routes.js` + `lint-silent-catches.sh`); Tests (postgres:15 service, `npm run migrate:up`, `npm test` with `TEST_DATABASE_URL` and `JWT_SECRET`); Frontend Tests (`npx vitest run`). **ESLint, `prettier --check` and frontend lint run nowhere in CI or hooks.** Branch protection on `main` (F-Tools-1 §3.3.1, verified 2026-05-21): required contexts Cost Exposure Audit, Tests, Route Validation; `strict=true`; `enforce_admins=false`; zero required reviews; Frontend Tests is not documented as required. Dependabot is monthly and monitor-only (`open-pull-requests-limit: 0`; security updates disabled per Amd15).

**Local gates.** `npm install` → `prepare` → `git config core.hooksPath .githooks`. pre-commit: `check-root-junk.js` (root allowlist; origin: the `ct_modifydb.json` incident of 2026-06-26) + `audit-cost-exposure.sh`. pre-push: `audit-cost-exposure.sh` + `validate-routes.js` (its header claiming "routes + lint + tests" is stale). `npm run validate` = routes + `npm run lint` + `test:route-health` + cost audit. **The "fails at HEAD (ESLint)" claim is stale, re-checked at this basis (Task #1565 fix, `213edbab1`, no `TEST_DATABASE_URL`/`DATABASE_URL` set):** `npm run lint` (`eslint src/ tests/ && bash scripts/lint-silent-catches.sh`) exits 0 — 0 errors, 1 warning from `eslint`, then a clean `lint-silent-catches.sh` run; `npm run lint` differs from the bare `npx eslint src/ tests/` §4.8 tracks only by that second, always-clean step, and does not currently change the outcome. `test:route-health` also exits 0 without a database, unchanged from §4.8's own PR #1291 note above (the gate warns, not throws, when `TEST_DATABASE_URL` is unset). **`npm run validate` itself now exits 0 in full at this basis** — pasted at §4.8's ESLint row and in this task's own PR body. The repo's `/validate` skill runs the four static scripts plus `node -c` instead. `docker-compose.test.yml` exposes Postgres on host port **5433**, while the README quotes a 5432 URL; `docker-compose.yml`'s postgres is on 5432.

**Check results, `node scripts/validate-routes.js`, `jest --listTests`, and `npx eslint src/ tests/` re-derived fresh at this basis (`213edbab1`, Task #1565, Node 20, no DB, no network); Vitest and the other rows are carried from the prior recorded basis, `433b1f22` (run 2026-09-01), not re-run this pass:**

| Check | Result |
|---|---|
| `node scripts/validate-routes.js` | exit 0; **132 route files, 707 src files** (up from 704 — `cognitoPasswordAuthService.js` predates this count and is unchanged in file count by this pass; the delta traces to files added since the `433b1f22` basis, not re-itemized here); **10 try/catch warnings**: `amberSessionRoutes.js` 2/4, `animatic.js` 6/6, `characterRegistry.js` 2/37, `episodeOrchestrationRoute.js` 1/1, `feedSchedulerRoutes.js` 1/6, `franchiseBrainRoutes.js` 1/16, `sceneSetRoutes.js` 1/59, `socialProfileRoutes.js` 1/39, `textureLayerRoutes.js` 1/5, `timelineData.js` 2/2 |
| `bash scripts/lint-silent-catches.sh` | exit 0; clean (carried, not re-run this pass) |
| `bash scripts/audit-cost-exposure.sh` | exit 0; three non-failing HIGH API DENSITY notices (`characterGenerationService` 5, `feedScheduler` 5, `promptCacheHelper` 4) (carried, not re-run this pass) |
| `node scripts/check-root-junk.js` | exit 0 (inspects staged files only) (carried, not re-run this pass) |
| `npx eslint src/ tests/` (eslint 8.57.1) | exit 0; 0 errors, 1 warning (`src/middleware/auth.js:167`, `no-console`) — re-run at this basis since `213edbab1` touches `src/routes/auth.js` and `src/services/cognitoPasswordAuthService.js`; unchanged from the prior recorded basis (§10 Task #1492's row) |
| `cd frontend && npx vitest run` | exit 0; 99/99 files, 793/793 tests, ~27 s (carried, not re-run this pass) |
| `node -c src/app.js`, `node -c src/routes/memories/*.js` | OK (10 files) (carried, not re-run this pass) |
| `jest --listTests` | **151 of 169 files collected** (up from 150 of 168 — `tests/unit/routes/auth-refresh-cognito.test.js` is new, per Task #1563/PR #1564, and is collected; 18 uncollected, unchanged count) |
| `npm test` | NOT RUN (needs Postgres) |

**Gaps worth a task:** ESLint not in CI while `main` fails it; 20 backend test files uncollected (now named in `jest.config.js`, still not run); `scenes.integration.test.js` specifically remains unreachable through ordinary `npx jest`; coverage collected with no threshold; frontend ESLint 9 installed with only a legacy `.eslintrc.json` and no lint script; `f-auth-1-fd63.test.js` still has an `amazonaws.com`-only guard; and dead local DB URLs remain outside #1314's unset-only scope.

---

## 5. Conventions that are actually true (supersedes old CLAUDE.md)

| Topic | Convention |
|---|---|
| Route auth | Writes: `requireAuth` (Tier 1) or `requireAuth + authorize(['ADMIN'])` (Tier 2). Public catalog GETs: `optionalAuth` with a `// PUBLIC:` comment (Tier 4). Never demote a `requireAuth` handler. AI handlers add `aiRateLimiter`. |
| AI calls | Primary model `claude-sonnet-4-6`; Haiku 4.5 for cheap side calls. Prefer the `MODELS` array + two-attempt retry (skip to next model on 529/503/404). Every call is auto-logged and budget-gated by `aiCostTracker`. Do not add schedulers or `setInterval` AI loops without an env gate. |
| SSE | `Content-Type: text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`, `X-Accel-Buffering: no`; frames `data: {type:'text'|'done'|'error'}`. |
| Context loaders | Return `null` when empty; inject conditionally. `characterKey` is the slug from `RegistryCharacter.character_key`, not the integer PK. |
| Errors | try/catch, `console.error`, JSON error response; every `catch` logs (`lint-silent-catches.sh`). |
| Migrations | New files only, under `src/migrations/`, timestamp `YYYYMMDDHHMMSS`, always `deleted_at`, `down` drops ENUM types. Never edit a migration that has run. Never add `Model.sync()` or inline `CREATE TABLE`. |
| Models | `(sequelize, DataTypes) => …` factory, `tableName` snake_case plural, `underscored: true`, register in `src/models/index.js`. Do not add `paranoid: true` (global). |
| Frontend | React hooks only; one CSS file per page; lucide-react for new icons; test at 375px; API calls through `services/api.js`; new pages must get a Sidebar entry or an in-app link. |
| Ports | Backend `PORT=3002` locally (server default is 3000; nginx dev proxies 3002, prod 3000). Frontend 5174. |
| Git | `claude/<slug>` branches from `origin/main`; explicit-path `git add`; `git diff --cached` before commit; subject ends with ` [skip-automerge]`; no closing keywords next to `#N` (FD-21); squash-merge + delete branch; never push to `main` or `dev`. |
| Register | Nothing in `docs/audit/` is edited in place after merge; corrections are new amendments or additive newest-first banners; FD numbers are minted only by Fix Plan revisions, XK by the Cross-Keystone Register, PE by `Session_PE_Roster.md`; every claim carries a standing (MEASURED / ATTESTED / RULED / INFERRED). |
| Agent boundaries | No `ssh`, `scp`, `pm2`, `aws`, RDS connections, server `.env` edits, workflow enable/dispatch. Rule 7 (Draft → Confirm → Execute) for push, PR create, merge. H1: paste the command and its output. |
| Dev-only token carrier | A frontend route may exist for the sole purpose of writing an externally-minted token into `localStorage`, gated `import.meta.env.DEV`-fail-closed (absent from any production build) and scoped to local/LAN dev only. It is a carrier, not a source: mints nothing, calls no backend endpoint. **The parenthetical this row used to cite as part of why the carrier does not reopen FD-65 — that `POST /api/v1/auth/login` still returns `401 AUTH_LOGIN_DISABLED` unconditionally — is stale as of Task #1456/PR #1457 (§0 item 5); that route now performs a real Cognito exchange.** Whether the carrier still doesn't reopen FD-65 on some other basis (it never itself calls `/login` or mints a token, so the mechanism this row describes is unchanged by that PR) is not re-derived here — this refresh corrects the fact the row cited, not the ruling built on it. Ruled Shape A, `F-AUTH-1_Tier5_DevTokenCarrier_Ruling_2026-09-05.md`; implemented at `/__dev-token-carrier` (PR #1267), depends on the `PRE_AUTH_PATHS` exemption merged in PR #1269 (`ee334ad566e`) to survive the pre-auth redirect; documented in `docs/LOCAL_DEV_LOGIN.md` (PR #1275). A backend dev-only token *issuance* endpoint (Shape C) remains unruled and is not authorized by this. This method supersedes the mint-and-console-paste procedure generally, not only for phone/second-device use; console-paste is the fallback when the dev server predates PR #1269. |

---

## 6. The audit register — what the conversation has been about

### 6.1 Keystones and standing (Handoff v26, basis `9250b60e`; F-AUTH-1 itself now current at `F-AUTH-1_Fix_Plan_v2.77.md`, plus 2026-09-03 correction banners on v26 and v2.69 — see below)

`Prime_Studios_Audit_Handoff_v26.md` is still the newest handoff by numeric sort (`ls docs/audit | grep -E 'Audit_Handoff_v[0-9]+\.md' | sort -V | tail -1`), superseding v25 Sec 1/2/3/6; v25's Sec 0/4/5/7/8 still stand. **v26 itself carries a correction banner** (added 2026-09-03, PR #1206), prepended above its body per the register's newest-first convention: two method-note passages (a table-provenance note in its Sec 3, and a citation in Sec 6.A item 12) were reviewed and approved on the PR branch but dropped from the squash-merged commit; the banner carries them in-band. Read the banner before the body.

**F-AUTH-1's own authority has since moved past v26.** v26 Sec 8 explicitly declined to ratify limb 1's discharge or close FD-64/FD-67/PE #65, naming that as `F-AUTH-1_Fix_Plan_v2.69`'s to rule. `F-AUTH-1_Fix_Plan_v2.69.md` landed 2026-09-03 (PR #1211) carrying five rulings by Evoni, transcribed verbatim: limb 1 **DISCHARGED**, FD-64 **CLOSED**, PE #65 **CLOSED**, FD-67 remains **OPEN** on one named gap, FD-70 **not minted**. **v2.69 itself carries a correction banner** (added 2026-09-03, PR #1213), prepended above its title table per the register's newest-first convention: it discloses that two clauses in §6 Ruling 1's Rationale (the `v27` Sec 6.B register-entry classification and the "pending explicit authorization, not the task loop" framing) originated with the drafting session and were adopted by Evoni's approval of the assembled text, not composed by her from scratch — the same disclosure test Ruling 2's own note already applied. The rest of Ruling 1, and all of Rulings 3, 4, and 5, are Evoni's own words, verified against the session transcript. Read v2.69's banner before its body.

**F-AUTH-1's authority has since moved again, to `F-AUTH-1_Fix_Plan_v2.70.md`** (landed 2026-09-04, PR #1237), which rules the three itemization deltas v2.69 §7 item 1 left open once CP2's, CP3's, and CP12's itemizations were filed: all three recorded, none minted — no FD, XK, or PE follows from any of them. v2.70's own rationale states its own limitation directly — CP12's accounting is re-verified in the ruling document itself, CP2's and CP3's are cited as filed, not re-verified — and neither adopts nor rejects CP12's §7/§8 causal account of its own largest delta, calling that account unnecessary to the ruling either way. v2.70 does not re-rule any of v2.69's five rulings; all stand as v2.69 left them.

**F-AUTH-1's authority has since moved a third time, to `F-AUTH-1_Fix_Plan_v2.71.md`** (landed 2026-09-04, PR #1251), which rules the four unwired-model patterns the models-subset enumeration and history trace recorded: `ShowArc`, `UiOverlayType`, and `SocialProfileTemplate` are recorded with no action as a choice — nothing running is affected by any of them, and no action is foreclosed; the three `Script*` models are recorded with no action as the only available disposition — none has a migration in the running tree, and creating one would be a schema change the freeze does not permit. v2.71 does not rule on whether any underlying table currently exists, and does not re-rule any of v2.70's or v2.69's rulings; all stand as left.

**F-AUTH-1's authority has since moved a fourth time, to `F-AUTH-1_Fix_Plan_v2.72.md`** (filed 2026-09-11, PR #1385; ruling authority initially flagged unestablished, then ATTESTED by a same-evening banner retracting that flag, PR #1392 — read the banner before the body), which rules two matters on Evoni's word: (1) the FD-68 vs FD-65 severity interaction is **Adjudicated** — the two are independent, severed defects, neither's remedy altering the other's severity — and with that adjudication complete, on top of FD-67's remedy already authorized, implemented, and tested (`F-AUTH-1_FD67_Remedy_Implementation_2026-09-02.md` §7.5), **FD-67 is officially CLOSED**; (2) Limb 3 Dimension 5's criterion status is **Adjudicated** as `MEASURED-ABSENT` — no criterion exists in the register (`F-AUTH-1_Limb3_D3D5_ReadPlan_2026-09-11.md` §2.2), and Dimension 5 cannot be performed or passed without an authorized definition supplied in a future revision; Dimension 5 remains NOT PERFORMED, Limb 3 assessment remains NOT COMPLETED, Gate G4 remains NOT ENTERABLE. v2.72 does not re-rule limb 1's discharge, FD-64's closure, PE #65's closure, v2.70's itemization-deltas ruling, or v2.71's unwired-models ruling — none of those is touched here. v2.72 §6 states its ruling does not alter the closed standing of FD-64, FD-65, or FD-68 — accurate to its own 2026-09-11 basis. That basis predates Task #1456/PR #1457 (2026-09-15), which reopened a live question about FD-65's issuance half specifically — **a question since resolved, not one still open:** `v2.73` (2026-09-16) ruled the reopening the trigger made automatic, `v2.74` narrowed precondition 1's evidentiary basis without closing anything, and `v2.75` (2026-09-16) closed the issuance half on live verification — **FD-65 is CLOSED entire.** See the F-AUTH-1 row below and §6.5.

**F-AUTH-1's authority has since moved a fifth time, to `F-AUTH-1_Fix_Plan_v2.77.md`** (filed 2026-09-20, PR #1562), which lands Evoni's adopted rulings on the `/refresh` defect measured at `F-AUTH-1_SessionRefresh_Read_2026-09-19.md` (PR #1560 — every session ended within one request of the Cognito access token's expiry, because the old `/refresh` handler was HS256-only and could never accept the Cognito-issued `refreshToken` `/login` hands the client). The rulings: public user sessions become **Cognito end to end**; HS256 refresh for user sessions is **retired**, and `/refresh` is not made polymorphic on `alg`; the success contract is **pinned** to `accessToken` + `expiresIn` + `tokenType`, exposing neither `idToken` nor `refreshToken`; and **FD-65 is not reopened** — the ruling says so expressly. **The ruling text at v2.77 §4 was drafted by another AI assistant outside this repository's sessions and adopted by Evoni's approval ("approved", 2026-09-19) — disclosed at v2.77 §3 before the text, the same test v2.69's own correction banner applied**, and fixed against relay corruption by a SHA-256 digest over its source (issue #1561). v2.77 §6 records Evoni's own ATTESTED app-client configuration read (`RefreshTokenRotation` null/not-configured; `RefreshTokenValidity` 30, unit omitted), and §7 derives from it, together with ruling 1's own stated condition, that **`REFRESH_TOKEN_AUTH`** is the applicable Cognito mechanism — the AWS behavior claims the ruling itself cites are **not independently verified** against AWS by this register (§5). v2.77 **mints nothing** and **implements nothing**; it names implementation (§8(a) — since done, Task #1563/PR #1564, §0 item 5 and §4.1 above) and a maintainer-performed live real-pool refresh verification (§8(b) — still owed, Evoni-only, gated on a deploy) as what remains, neither as a precondition on its own rulings. v2.77 does not re-rule any of v2.69's, v2.70's, v2.71's, v2.72's, v2.73's, v2.74's, v2.75's, or v2.76's rulings — none of those is touched here.

**A gap surfaced 2026-09-05, unrelated to any v2.69–v2.71 ruling and not reopening any of them.** `F-AUTH-1_PE65_CutoverGap_2026-09-05.md` records that `F-AUTH-1_PE65_Execution_Sequence_2026-09-02.md` — the sequence whose supplied piece let `v2.69` Ruling 4 close PE #65 — gates every step it names but names no step that makes its own Phase 2 Cognito-config write (`ecosystem.config.js`'s `env_production` block) take effect on the running production process: no restart/reload/redeploy step exists in the sequence, PM2 reads that block only at process start, and `src/middleware/auth.js`'s Cognito verifiers are constructed once and cached, so editing the manifest alone changes nothing already running. The one path capable of restarting production, `Deploy to Production`, is `disabled_manually`. **Class-matched to FD-31's split-brain hazard (a process/config mismatch a restart would expose), not a recurrence of it** — FD-31 itself stays CLOSED at `v1.20`, unreopened. Whether a restart would currently be safe is not repository-derivable (Gate G0); the document also finds no rollback or abort path named anywhere in the sequence. **Mints nothing, rules nothing, does not reopen PE #65 or FD-31** — recorded here because a reader of §6.1's PE #65 closure should know the sequence that closure relied on stops one step short of making its own change live, if Branch B's cutover is ever executed.

| Keystone | What it is | Standing (F-AUTH-1 row refreshed to `v2.75`'s ATTESTED standing, 2026-09-16 — FD-65 CLOSED entire; `/refresh` ruling appended from `v2.77`, 2026-09-20, Task #1565; row content otherwise last re-verified 2026-09-03, Task #1214; other rows carried from the prior refresh, not re-checked this pass) | Authority file |
|---|---|---|---|
| **F-AUTH-1** | Codebase-wide auth bypass on writes (three sub-forms) | **REOPENED-QUALIFIED.** Backend sweep done (12 CPs, ~700–750 handlers, May 2026); reopened at FD-63; 95 more handlers promoted Aug 17. Gate G3: limb 1 (confirm-not-re-derive audit of recorded CP dispositions) **DISCHARGED** — 12/12 CPs confirmed, 127 recorded dispositions (120 agree, 2 disagree, 5 cannot-tell). The 2 disagree rows are documentation split-errors on an otherwise-correct Tier ruling, not minted (`v2.69` §6 Ruling 5). The 5 cannot-tell rows are aggregate figures over an unitemized remainder with no address to read; `v2.69` §6 Ruling 1 accepts these as **unconfirmed under this method, not left pending under it**, and rules that this does not block discharge — an itemized audit that could settle them under a different instrument has since been filed across three documents, and the resulting deltas have since been ruled: `v2.70` (§6.1 above, §6.5) records all three as counting errors, none minted, class-assignment correctness unreached (§6.5). Limb 3 (five-dimension production-readiness) NOT COMPLETED (D1 PASS, D2 PASS, D3 not performed, D4 FAIL, D5 **MEASURED-ABSENT** — `v2.72` §4 rules D5 has no criterion in the register at all and cannot be performed or passed without an authorized definition supplied in a future revision; remains NOT PERFORMED); G4 not enterable; G5 blocked; prod frozen. FD-63 OPEN. FD-64 **CLOSED** (`v2.69` §6 Ruling 2, citing `65cbe7013` spelling fix and `2e5dbdf28` `where`-clause fix; closing also required a third, unminted defect — `AssetRole` missing from `src/models/index.js`'s hand-maintained `models` subset, carried forward at §6.5). FD-67 is **CLOSED** — the branch was ruled and the remedy implemented and tested (`F-AUTH-1_FD67_Remedy_Implementation_2026-09-02.md` §7.5, 142/142 Jest suites against real Postgres 16); that same §7.5 had named FD-68's severity interaction with FD-65 as the sole unadjudicated gap keeping it open (`v2.69` §6 Ruling 3), and `v2.72` §3 adjudicates that interaction — FD-68 and FD-65 ruled independent, severed defects, neither's remedy altering the other's severity — closing FD-67 in consequence. FD-65's privilege half is CLOSED on the merits (`v2.50`, `75ac05f0`); FD-68 CLOSED (v25 wrongly lists FD-68 open); their severity interaction, formerly a distinct open question from FD-68's own closure, is now itself ADJUDICATED per `v2.72` §3 (independent/severed). FD-65's issuance half reopened 2026-09-15 on `PR #1457`'s re-enablement of `POST /api/v1/auth/login` (`v2.73` §3 — the trigger `v2.67` §4 itself named). A code defect found live 2026-09-16 (`SECRET_HASH` sent unconditionally to a client the pool measured as secret-less) was fixed (`PR #1472`) and its evidentiary narrowing recorded (`v2.74` — precondition 1 remains MET, but on the narrower basis of "correctly shaped," not "proven live"). The issuance half then **CLOSED** on live verification Evoni performed herself against the real pool, outside any agent session (`v2.75` §3). **FD-65 is CLOSED entire.** Precondition 3 (`v2.67` §4 — `LOGIN_DISABLED`, unscoped token-fabrication design question) is **discharged, on direct evidence** (`v2.76` §4 — a live poisoned-body login request against the real Cognito pool, performed by Evoni outside any agent session, returned only the pool's real group; §5.2). `LOGIN_DISABLED` remains `true` in `tests/integration/f-auth-1-fd65.test.js`; `v2.76` does not authorize flipping it. The test file's rewrite is named as ordinary owed work, carrying no precondition (`v2.76` §8). FD-69 retired as duplicate; FD-70 not minted (`v2.69` §6 Ruling 5). PE #65 **CLOSED** (`v2.69` §6 Ruling 4 — the sequencing document supplied the missing piece of its own closing criterion; only the closing act was withheld on authority grounds, and this revision performs it). **`POST /refresh` is ruled a Cognito `REFRESH_TOKEN_AUTH` exchange, HS256 refresh retired for user sessions, response contract pinned, FD-65 not reopened** (`v2.77` §4 — RULED, Evoni-adopted, drafted outside this repository's sessions and disclosed as such at v2.77 §3); **implementation is done** (Task #1563, PR #1564, §0 item 5/§4.1); **live real-pool verification remains owed** (`v2.77` §8(b), Evoni-only, gated on a deploy). | `docs/audit/F-AUTH-1_Fix_Plan_v2.75.md`; `F-AUTH-1_Fix_Plan_v2.76.md`; `F-AUTH-1_Fix_Plan_v2.77.md` |
| **F-Deploy-1** | Deploy pipeline + autonomous-merge failure modes (37 lettered findings A–AK) | **CLOSED** at v1.48 (2026-07-22), carriage-corrected at v1.49. Dev box + SSM `workflow_dispatch` deploy path live; prod deploy path still SSH-based and disabled. Owed after close: AD (no instance profile on prod), AE (four SG ports still open; `/32` on port 22 needs re-scoping), fork RDS teardown, dev DNS repoint, `deploy-production.yml` SSM migration, PE #62 ownership. | `docs/audit/F-Deploy-1_Fix_Plan_v1.49.md` |
| **F-App-1** | Schema-as-JS auto-repair in `app.js` | SHIPPED 2026-05-14 (out of sequence); residue = PE #62 (five inline `CREATE TABLE` + eleven `Model.sync()` sites) and FD-66. | `docs/audit/F-App-1_Fix_Plan_v1.1.md` |
| **F-Stats-1** | `character_state` model + raw-SQL writers | Phase B live: raw SQL → ORM PRs 1–4f landed Aug 1–4; items 23 and 36 closed; open item 40 became XK-1. | `docs/audit/F-Stats-1_Fix_Plan_v1.60.md` |
| **F-Ward-1** | `episode_wardrobe` migration gap | Queued; zero repository presence. | none |
| **F-Reg-2** | Registry write contention | Queued; zero presence. | none |
| **F-Ward-3** | Duplicate outfit-set controllers | Queued; zero presence. | none |
| **F-Franchise-1** | Write-only franchise tier → Director Brain | Queued; zero presence; scoping note only. | `docs/audit/DirectorBrain_FrontendLeg_ScopingNote_2026-07-03.md` |
| **F-Sec-3** | `character_key` drift (`lala` / `justawoman`) | Queued last; canonical-key decision recorded 2026-07-02. | `docs/audit/F-Sec-3_*` |
| **F-Tools-1** | Tooling environment (VS Code extensions, hooks, repo settings) | One-time audit 2026-05-21; loki extension removed; branch protection verified. Seven further documents filed since (§6.5). | `docs/audit/F-Tools-1_Tooling_Environment_Audit.md` |

**The tension flagged in the prior refresh is resolved, not merely re-described.** That refresh recorded, without reconciling, that this table's "FD-65... CLOSED" claim conflicted with `F-AUTH-1_AuthIssuanceSurface_Read_2026-09-15.md`'s "FD-65's issuance half stays open... only a Fix Plan revision disposes of FD-65," quoting Evoni's 2026-09-14 ruling (c): "FD-65's issuance half closes on live verification against the real Cognito pool, not on implementation." `v2.73` (2026-09-16) ruled the reopening that trigger made automatic; `v2.74` narrowed precondition 1's evidentiary basis without closing anything; `v2.75` (2026-09-16) rules the live verification ruling (c) asked for was performed, and closes FD-65's issuance half on that basis — **FD-65 is CLOSED entire.** The prior table's "CLOSED" framing is neither vindicated nor overturned by this: it is superseded by a closure resting on different, and now live, grounds. See the F-AUTH-1 row above for the current text.

**Locked sequence (Path A):** F-AUTH-1 → F-Deploy-1 → F-App-1 → F-Stats-1 Phase B → F-Ward-1 → F-Reg-2 → F-Ward-3 → F-Franchise-1 (= Director Brain) → F-Sec-3. Standing rule from the onboarding doc: no feature additions, schema redesigns, or "optimize later" during the fix cycle.

### 6.2 Cross-keystone register (`docs/audit/Cross_Keystone_Register.md`)

- **XK-1** paranoid exposure (owned by F-Stats-1; fix unevaluated; admission now contingent because reach shrank to `character_state` alone).
- **XK-2** row scope not enforced in SQL, two sub-forms: "scope cached at the write" (a read scoped by `showId`, then a write by the resulting row id alone — `worldEvents.js` 1229/1271/1510/1605/1837/1910/1911, `arcRoutes.js` 158/209) and "caller-supplied FK written unvalidated" (`worldEvents.js` :1229). **The 20-of-22 unprobed `:showId` route files are now censused** (`docs/audit/XK-2_Extent_Census_2026-09-05.md`, MEASURED; Task #1259, PR #1271): 3 PRESENT — `shows.js` (:826-832, scope-cached-at-write), `wardrobe.js` (:1874, same sub-form), `world.js` (:241-244, a second caller-supplied-FK instance); 3 CANNOT-TELL (the write is delegated to a service function the census left unread, out of its `src/routes/**` scope); 14 ABSENT. **Population-membership finding, register-hygiene-level and outliving XK-2's own disposition:** 3 of the 22 files matching the literal `:showId` grep (`evaluation.js`, `phonePlaythroughRoutes.js`, `shows.js`) carry no genuine Express `:showId` route parameter at all, yet the census's own grep-derived population still numerically matches this section's stated "22" — and no document anywhere, including this one before now, enumerates the register's original 22 by name, so whether the two counts describe the same file set or agree only by coincidence is unresolved. Owned F-Stats-1; fix unevaluated; the census proposes no disposition and mints nothing.
- **XK-3** no authorization substrate for the tenancy root: no `User` model, no user↔show relation, `show_id` is caller-asserted everywhere; `roles.js` is the F-AUTH-1 instance. Owned F-Stats-1; Gate 3 needs a live DB read under freeze.

### 6.3 Register tails and numbering

FD tail **FD-69** (retired; **FD-70** next, unminted). XK tail **XK-3**. PE tail **PE #68** (v25 says #67; the roster carries #68 from 2026-08-28). **Re-derived at Task #1252's basis, 2026-09-04** with both instruments — filename scan (`ls docs/audit | grep -E '^FD-[0-9]+_' | sort -t- -k2 -n`, highest `FD-69`) and cross-revision scan of the last three F-AUTH-1 Fix Plan revisions (onboarding §4 rule 4: v2.69 → `FD-70`, v2.70 → `FD-70`, v2.71 → `FD-70`). The cross-revision scan's raw output continues to name `FD-70`; per the mention-vs-mint distinction this section already draws, v2.69's own text cites it six times, v2.70's seven, and v2.71's eight, all "next-available, unminted." Applying that filter, the highest *minted* FD across all three revisions is `FD-69`, agreeing with the filename scan. **v2.71 itself is now the F-AUTH-1 authority** (superseding v2.70 for that keystone's tail citation; see §6.1). v26 Sec 1's prior corroboration (both instruments agreeing at the `9250b60e` basis) stands unchanged. `grep`-counting mentions of `FD-70`/`XK-4`/`PE #69` returns dozens of hits that are all "next-available, unminted" citations, not mints — don't derive a tail by mention count. Notation warning: `§AD`, `§AE`, `§AF` in the Owed Index amendments collide with finding letters AD/AE/AF.

**Re-derived at Task #1565's basis, `213edbab1`, 2026-09-20, with both instruments — unchanged.** Filename scan (`ls docs/audit | grep -E '^FD-[0-9]+_' | sort -t- -k2 -n`): still `FD-69` highest. Cross-revision scan, now of the three newest F-AUTH-1 Fix Plan revisions (`v2.75`, `v2.76`, `v2.77`, superseding the `v2.69`–`v2.71` window §6.3 previously cited): `for f in v2.75 v2.76 v2.77; do grep -oE 'FD-[0-9]+' docs/audit/F-AUTH-1_Fix_Plan_$f.md | sort -t- -k2 -n -u | tail -1; done` → `FD-70`, `FD-70`, `FD-70` in all three, every occurrence a "next-available, unminted" citation per the same mention-vs-mint filter this section already applies — the highest *minted* FD across all three is still `FD-69`, agreeing with the filename scan. XK tail re-derived (`grep -n '^### XK-' docs/audit/Cross_Keystone_Register.md`): still `XK-3`. PE tail re-derived (`grep -oE '^### PE #[0-9]+' docs/audit/Session_PE_Roster.md | grep -oE '[0-9]+' | sort -n | tail -3`): still `PE #68`. **All three tails are unchanged from the prior recorded basis.**

### 6.4 The v25 Owed Index chain (Amd1–Amd30, Aug 22 → Sep 1)

Carries rulings made in session but not yet held by a revision. Highlights: the shallow-clone hazard (Amd1); the carriage rule and blast-radius rule; four method rules (Amd20/21); the item-8 canon schema read performed Aug 29 (Amd18/22/29) showing that canon's migration ledger does not describe what canon contains; Amd24: two of nine duplicate migrations change schema; Amd28: credential location; **Amd30: an SSM `describe-parameters` enumeration was authorized for Evoni personally but run by an agent session — ruled a crossing; four parameters exist, none under `/episode-metadata/`.** Item 8's follow-up reads (Addenda A/B) are authorized, not performed.

### 6.5 What is owed / open (consolidated, with owner)

Reconciled from Handoff v26 Sec 6.A/6.B (which itself rebuilt from Handoff v25 Sec 6 and the v25 Owed Index chain through Amd30), F-Stats-1 v1.60, the Cross-Keystone Register and `Session_PE_Roster.md`. "Evoni-gated" items are never agent tasks; agent sessions record them NOT PERFORMED. **Re-verified at Task #1248's basis:** `git log --oneline` between Task #1242's own resulting state (`c0307604e`, right after its own PR #1243 merged) and this file's current one shows exactly two commits landed since that refresh — the unwired-models history trace (PR #1245) and its correction banner (PR #1247). This comparison point changed from prior refreshes' "prior refresh's Basis line" phrasing: a refresh's Basis line records what it read from, one commit before its own merge, so comparing basis-to-basis always included the prior refresh's own PR in the count — off by one by construction, not a one-time slip, and invisible only because the resulting count happened to still read as correct. Nothing else under `docs/audit/` or elsewhere changed in this window, so every item below not touched by that change is carried as re-verified at the prior refresh's resulting state, not carried unchecked.

**Evoni-gated (host, AWS, DB, Cognito, GitHub settings, or a ruling)**

| Item | Source |
|---|---|
| **DONE with policy now settled.** v25 item 8's disposition is replaced by `F-Deploy-1_Fix_Plan_v1.51.md`'s schema-fork ruling (Evoni, 2026-09-17): canon's 2026-09-17 captured schema is the baseline; reconciliation adds only what live routes reach; the existing migration tree is not run against canon; `decision_logs` and `decision_log` remain distinct; canon-only tables are kept; prod-only tables remain archived; and `pgmigrations` / `SequelizeMeta` are retired as authorities. The ruling is cited here, not re-derived as this file's own sub-rulings. **STILL OWED:** the Evoni-gated reconciliation session that will express the captured baseline in the repository and perform the authorized reconciliation. | `F-Deploy-1_Fix_Plan_v1.51.md` §§2–6 |
| **STILL OWED**, unchanged at this basis. Addendum A/B follow-up canon reads: authorized 2026-09-01, NOT PERFORMED; blocked on credential location (both DB passwords exist only on the frozen box; Secrets Manager and SSM hold nothing under `/episode-metadata/`). | Amd28 §AD2, Amd30 §AF2 |
| **STILL OWED.** `JWT_SECRET` dev/prod environment read (item 9). | v26 Sec 6.A item 9 |
| **DONE.** FD-68 vs FD-65 severity interaction (item 11) — **ADJUDICATED** (`v2.72` §3), ruling FD-68 and FD-65 independent and severed defects. With this adjudication complete and its remedy previously authorized, implemented, and tested, **FD-67 is officially CLOSED**. | `F-AUTH-1_Fix_Plan_v2.72.md` §3 |
| **STILL OWED.** F-AUTH-1 limb 3 Dimension 3 live host read. A repo-only read plan is filed at `docs/audit/F-AUTH-1_Limb3_D3D5_ReadPlan_2026-09-11.md`; the host read remains NOT PERFORMED. | F-AUTH-1 v2.68; filed read plan: `F-AUTH-1_Limb3_D3D5_ReadPlan_2026-09-11.md` |
| **MIXED.** PE #64 severity re-rule — **STILL OWED**. PE #65 Branch B costing — **DONE** (`F-AUTH-1_PE65_Resolution_BranchB_2026-08-28.md`); its ordered execution sequence — **DONE** (`F-AUTH-1_PE65_Execution_Sequence_2026-09-02.md`); **PE #65 itself is now CLOSED** (`v2.69` §6 Ruling 4, 2026-09-03 — the sequencing document supplied the missing piece of its own closing criterion, and this revision performs the closing act that document withheld on authority grounds alone). Re-recording §9.10's P1 in a Fix Plan revision — **STILL OWED**, unchanged (`v2.69` does not address §9.10; that same 2026-09-02 document confirms §9.10 occurs zero times in `v2.68`). | PE roster, Amd16; `F-AUTH-1_Fix_Plan_v2.69.md` §6 Ruling 4 |
| **From `F-AUTH-1_Fix_Plan_v2.69.md` §7 (2026-09-03), none minted.** (a) Itemized audit of limb 1's five aggregate cannot-tell dispositions — owed as a `v27` Sec 6.B register entry, not as an FD; **Evoni-gated, pending her explicit authorization — NOT in the default agent-session task loop** (§6 Ruling 1's own rationale). **FULLY DISCHARGED as itemization work — all three itemization documents are now filed, covering all five of limb 1's aggregate cannot-tell dispositions:** CP2's aggregate is itemized (`F-AUTH-1_Limb1_CP2_Itemization_2026-09-03.md`, PR #1219) — 121 addressed handlers against a recorded ~144, a **MEASURED delta of 23**. CP3's aggregate is itemized (`F-AUTH-1_Limb1_CP3_Itemization_2026-09-04.md`, PR #1227) — 61 addressed handlers against a recorded ~65, a **MEASURED delta of 4**. CP12's three rows — one shared 212-route cluster, classified three ways — are itemized in a single document (`F-AUTH-1_Limb1_CP12_Itemization_2026-09-04.md`, PR #1231): 142 addressed handlers against a recorded ~115 for row 1 (Tier 1), 28 against a recorded ~28 for row 7 (Tier 4 PUBLIC), and 25 against a recorded ~25 for row 9 (AI POST remainder) — **MEASURED deltas of +27, 0, and 0** respectively (195 addressed in total across the three rows). **That document carries an additive banner** (added 2026-09-04, PR #1233): its §7 and §8 connect the delta pattern to a causal account, and the banner discloses that connection as the drafting session's own inference, not a statement CP12's own confirmation document makes — **read the banner before §7 and §8.** All deltas across all three documents stand as filed; **this file does not restate any of them as its own finding, compare their sizes, or take a position on what the comparison means or on whether §7/§8's inference is correct** — that reading belongs to whoever rules on them. **Itemization is complete and the deltas are now ruled — both are closed, and they closed separately:** itemization completed across all three documents, and `F-AUTH-1_Fix_Plan_v2.70.md` (2026-09-04, PR #1237) rules the deltas — all three recorded as counting errors, none minted; class-assignment correctness unreached; CP12's own §7/§8 causal account neither adopted nor rejected, called unnecessary to the ruling either way. (b) `src/models/index.js`'s hand-maintained `models` subset omitting `AssetRole` (the load-bearing gap found while closing FD-64). **ENUMERATED, not just for `AssetRole`:** `F-AUTH-1_Models_Subset_Enumeration_2026-09-04.md` (PR #1241) found three separate registration mechanisms in `index.js`, not one — `requiredModels` (145 of 151 defined models), the `db.models` object this item refers to (56 of 151), and 146 individual `module.exports.<Name>` assignments. Against `db.models`: 95 omitted, of which 90 remain reachable via the other two mechanisms and 5 are referenced nowhere else in `src/` (`ScriptEditHistory`, `ScriptLearningProfile`, `ScriptSuggestion`, `ShowArc`, `UiOverlayType`). Two models — `SocialProfileTemplate` and `UiOverlayType` — are absent from all three mechanisms; `SocialProfileTemplate`'s one reference is guarded with a stated in-memory fallback, `UiOverlayType` has none. `AssetUsage` is recorded separately as a consistently stated exclusion, not a silent omission. **Item (b) as written presumed a single subset; the enumeration found three, so "omitted" is only meaningful against a named one.** That is a different question from how many mechanisms exist: how the six models came to be that way. `F-AUTH-1_Unwired_Models_History_2026-09-04.md` (PR #1245; correction banner PR #1247) traced each of the six's git history and found four distinct patterns, not one: `ScriptEditHistory`, `ScriptLearningProfile`, and `ScriptSuggestion` were registered via `module.exports` from creation, added to `requiredModels` a month later by a commit titled as a registration-gap fix that left `db.models` untouched, and have no migration anywhere in the running tree despite each declaring a real table name. `ShowArc` was registered via `requiredModels`/`module.exports` complete in its one commit, has a migration, and has never been touched again. `UiOverlayType` was never registered by any mechanism and never referenced by name, but the table its migration (`20260721000000-create-ui-overlay-types.js`) creates is still queried directly by `uiOverlayService.js` at this basis — the table is live, the model bypassed. `SocialProfileTemplate`'s one guarded reference arrived in the same commit as the model, whose own message states the guard exists because the table "doesn't exist yet." None of this is characterized as a defect, dead code, or missing wiring anywhere in either document. **Ruled at `F-AUTH-1_Fix_Plan_v2.71.md`** (2026-09-04, PR #1251): all four patterns recorded, none minted, no action follows from any — `ShowArc`, `UiOverlayType`, and `SocialProfileTemplate` as a choice (nothing running is affected by any of them, and no action is foreclosed), the three `Script*` models as the only available disposition (no migration in the running tree; creating one is a schema change the freeze does not permit). Whether any underlying table currently exists remains unruled and not repo-derivable. (c) Per-item Tier adjudication of the 40 declarations touched by FD-67's remedy — behavior-preserving and verified, but whether each legitimately needs optional identity is unruled. | `F-AUTH-1_Fix_Plan_v2.69.md` §7; `F-AUTH-1_Limb1_CP2_Itemization_2026-09-03.md`; `F-AUTH-1_Limb1_CP3_Itemization_2026-09-04.md`; `F-AUTH-1_Limb1_CP12_Itemization_2026-09-04.md`; `F-AUTH-1_Fix_Plan_v2.70.md`; `F-AUTH-1_Models_Subset_Enumeration_2026-09-04.md`; `F-AUTH-1_Unwired_Models_History_2026-09-04.md`; `F-AUTH-1_Fix_Plan_v2.71.md` |
| **CANNOT-TELL** — by the item's own terms, only a real push or reading the credential settles it, and neither is repo-derivable. PE #68: whether the injected git credential acts as `angelcreator113`. | Amd12, Amd14 |
| **STILL OWED**, unchanged at this basis. F-Deploy-1 post-close: AD (no instance profile), AE (open SG ports; `/32` on 22 re-scoping), AF (RDS SG `0.0.0.0/0:5432`), fork-RDS teardown, dev DNS repoint or retire, prod transport to SSM, credential rotations, the prod reconciliation session. | F-Deploy-1 v1.49 |
| **STILL OWED** — newest evidence note on file is still 2026-07-07. Branch-protection bypass disposition (enforce / accept / PR-required for `docs/audit/**`). | Finding_Main_BranchProtection_Bypass |
| **STILL OWED** (live canon-RDS read). `template_studio` schema-gap census (`docs/audit/TemplateStudio_SchemaGap_Census_2026-09-06.md`, Task #1289): no migration or model in this repo creates `template_studio` (§4.5's kind of drift, table-level this time), yet a 2026-08-29 canon capture shows the table exists there with every column the code queries or inserts present, column-for-column, no drift evident at that snapshot. A fresh migrated environment has none of it — every route in `templateStudio.js` now fails cleanly with HTTP 500 (fixed from a boot crash, §4.7) rather than serving anything or crashing route registration. Whether canon RDS has the table *right now*, re-verified fresh, is a live-database read this census did not and could not perform. Three remedies are named — migrate to match canon, remove the dead paths, or gate them — with none chosen. | `TemplateStudio_SchemaGap_Census_2026-09-06.md` |
| **CANNOT-TELL, Evoni-only.** `F-AUTH-1_SessionConduct_2026-09-06.md` (#1298): a MEASURED, additive finding recording a prior session's own conduct around PR #1295 (merged `853f502f`) — the arbitrary-host-dial defect that shipped (`isDatabaseReachable()` in `tests/integration/f-auth-1-g3-clause3.test.js` calls `net.createConnection` against any host from `DATABASE_URL`, not restricted to loopback), a diff-not-shown-before-authorization pattern on three of that session's four pushes, a false "diff shown above" statement in #1295's own authorization prompt, two branch deletions taken without a ruling (both re-verified byte-identical to `origin/main` after the fact), and one branch deletion carrying an unattributed verification claim (also re-verified byte-identical). The authorization gate itself is confirmed real — Evoni confirmed directly that the four affirmative answers were hers. **Open:** whether the #1295 prompt was read closely before it was answered; no session can determine this, only Evoni can state it. | `F-AUTH-1_SessionConduct_2026-09-06.md` |
| **DONE.** FD-65's issuance half: live verification against the real Cognito pool, performed by Evoni herself 2026-09-16, outside any agent session (ATTESTED \u2014 her account of her own action, unreproducible from the repository). `initiate-auth` returned an `AuthenticationResult` with no `SECRET_HASH` sent; `POST /api/v1/auth/login` returned the five pinned fields; `GET /api/v1/auth/me` accepted the issued token. **FD-65 is CLOSED entire** (`v2.75` \u00a73) \u2014 the privilege half was already closed on the merits at `v2.50`. Precondition 3 (`v2.67` \u00a74 \u2014 `LOGIN_DISABLED`, unscoped token-fabrication design question) is **discharged, on direct evidence** (`v2.76` \u00a74/\u00a75.2 \u2014 a live poisoned-body request against the real Cognito pool returned only the pool's real group). `LOGIN_DISABLED` remains `true`; the flip is not authorized. See \u00a76.1 for the standing-table update and the row below for `v2.76`'s own filing. | `F-AUTH-1_Fix_Plan_v2.75.md`; `v2.73`; `v2.74`; `v2.76` |
| **MIXED, re-checked against `Prime_Studios_Audit_Handoff_v26.md`, which now exists.** v25 item 5 replacement text — **DONE** (v26 Sec 6.A item 5 supplies Amendment 10 §J3's replacement text verbatim; whether to *ratify* it into a future revision's own face is still Evoni's). Amd6's missing pointer banner — **STILL OWED**, and now has a named entry: v26 Sec 6.A carries a one-time OPEN item stating which forward pointer must be ruled first (Amendment 7 §G6), a ruling v26 explicitly declines to make. Confirmed unplaced/unfiled at this basis: no banner text on `v25_Owed_Index_Amd28_2026-09-01.md` beyond its own §AD2 status update — **STILL OWED**. Remaining sub-items **STILL OWED, unchanged**: whether measurement corrections need ratification (XK-1); the VENDOR DOCUMENTATION source class; attribution-gap remedies (ALB access logs, actor on write paths); minting the production-provenance mechanism; the `deploy-production.yml` header figures; the authorship-record preservation choice. | Amd10, Amd13, Amd30, drafts of 2026-08-22/24; v26 Sec 6.A |
| **DONE.** `F-Deploy-1_Fix_Plan_v1.50.md` rules both conflicts (Evoni, 2026-09-17): Sec 3 items 7 and 9 govern regardless of the workflow's implementation — `Deploy to Development` is not dispatched until the gated reconciliation session has run (§2; conflict note §3.4 Q1 answered on that basis). Conflict note §3.4 Q2 (which RDS instance the Secrets Manager secret `episode-metadata/dev/database` resolves to) is answered with the read it called for: Evoni's own console read, ATTESTED, outside any agent session — it resolves to `episode-control-dev`, the canon instance (§3), which places dispatch inside item 7's "the dev box pointed at either instance." `Deploy to Development` is also now `disabled_manually`, ATTESTED and MEASURED (§5); re-enabling is reconciliation-gated. **§3.4 Q3 is not answered** — see the STILL OWED row below. | `F-Deploy-1_Fix_Plan_v1.50.md` |
| **STILL OWED.** Conflict note §3.4 Q3: the `DEVELOPMENT_WORKFLOW.md:247` citation correction — a separate edit to a live document, not performed by `F-Deploy-1_Fix_Plan_v1.50.md`. | `F-Deploy-1_DeployDev_FreezeStatement_Conflict_2026-09-17.md` §3.4 Q3; `F-Deploy-1_Fix_Plan_v1.50.md` §7 |
| **DONE.** `F-Deploy-1_Fix_Plan_v1.52.md` filed (issue #1520, 2026-09-18): records the 2026-09-18 reconciliation session, the measured repo basis, the two findings, and the post-state. This revision does not close any keystone and does not re-enable Deploy to Development or Deploy to Production. | `F-Deploy-1_Fix_Plan_v1.52.md`; issue #1520; PR #1521 |
| **STILL OWED, Evoni-gated.** The hazard doc's additive banner named by v1.52 as separate work — the immutable split-brain hazard text remains unchanged; the banner is not performed in this task and its standing is not ruled here. | `F-Deploy-1_PROD_SplitBrain_HAZARD.md`; `docs/audit/F-Deploy-1_Fix_Plan_v1.52.md` §What this revision does not do |
| **STILL OWED, agent-doable.** The v1.51-scoped table creates are still not made in the repository; this task does not perform them. | `docs/audit/F-Deploy-1_Fix_Plan_v1.51.md` §§2–6 |
| **DONE.** `F-Deploy-1_Fix_Plan_v1.50.md` filed (issue #1504, PR #1505, 2026-09-17): rules the two conflicts `F-Deploy-1_DeployDev_FreezeStatement_Conflict_2026-09-17.md` recorded and left open — hazard doc Sec 3 items 7 and 9 govern, and Evoni's ATTESTED console read resolves the Secrets Manager secret to the canon instance, `episode-control-dev`. Records `Deploy to Development` disabled by Evoni 2026-09-17, outside any agent session, MEASURED via API (`state: disabled_manually`); no run exists near the secret's 2026-09-04 retrieval date, and that retrieval's source is not established. §3.4 Q3 not answered — a separate edit, not performed by this revision. F-Deploy-1 remains CLOSED; mints no FD, XK, or PE number. | `F-Deploy-1_Fix_Plan_v1.50.md`; issue #1504; PR #1505 |
| **STILL OWED, Evoni-only.** `F-AUTH-1_Fix_Plan_v2.77.md` §8(b): live real-pool refresh verification for the Cognito `/refresh` exchange implemented at Task #1563/PR #1564. A maintainer-performed verification against the real Cognito pool, gated on a deploy Evoni decides (prod FROZEN) — the same distinction the register drew for login at `v2.73`/`v2.75`. Implementing §8(a) does not close the refresh work; only §8(b) does. | `F-AUTH-1_Fix_Plan_v2.77.md` §8(b) |

**Specification-bound, not an agent task:** F-AUTH-1 limb 3 Dimension 5 criterion status recorded as `MEASURED-ABSENT` (`v2.72` §4, citing `docs/audit/F-AUTH-1_Limb3_D3D5_ReadPlan_2026-09-11.md` §2.2). `v2.72` §4 rules D5 cannot be performed or passed without an authorized definition supplied in a future Fix Plan revision. Dimension 5 remains **NOT PERFORMED**; Limb 3 assessment remains **NOT COMPLETED**; Gate G4 remains **NOT ENTERABLE**.

**Register work an agent session can do (repo-only, filed with `/audit-file`, pushed and merged under Rule 7)**

| Item | Source |
|---|---|
| **DONE (confirmation sweep and ratification).** F-AUTH-1 limb 1: per-CP confirmation passes filed for all 12/12 CPs, confirming 127 recorded dispositions (120 agree, 2 disagree, 5 cannot-tell). See §6.1 for the count. Ratification — the step v26 Sec 8 explicitly declined to make, naming it `F-AUTH-1_Fix_Plan_v2.69`'s to rule — landed 2026-09-03: `v2.69` §6 Ruling 1 discharges limb 1, accepting the five cannot-tells as unconfirmed under this method rather than pending under it. | CP1–CP12 confirmations, 2026-09-01/02; `F-AUTH-1_Fix_Plan_v2.69.md` §6 Ruling 1 |
| **STILL OWED**, with one read advanced to **MEASURED-NO / not closed** — newest F-Stats-1 authority is still `v1.60`. F-Stats-1 owed: the reads slice (since v1.49 §52.6); the PE #62 overlap — traces to v1.60's Register hygiene section's `Owes:` bullet, not §63.5; §12.11 is absent as a heading from both F-App-1 fix plan files at `origin/main`; per `F-Stats-1_PE62_Overlap_Location_2026-09-11.md`'s own Outcome section, "located, as a candidate — under the same number, in a different document of the family; never promoted to a fix-plan heading": `F-App-1_G1_Audit_Report.md:514` carries the same number and a matching eleven-site count (including the worker-file site, `workers/sceneGenerationWorker.js:235`, absent from v1.60's own ten route-level syncs), but was never carried into `F-App-1_Fix_Plan_v1.1.md` despite that document's own stated intent to add it there — "a candidate, not a ruling" on what v1.60's citation meant; **STILL OWED**; §35.5 classes 2–6 homing — **located, not closed**, per `F-Stats-1_S355_Classes2to6_Homing_2026-09-14.md` (issue #1419, PR #1420): distinguishes the supplying revision (`v1.33` §35.5) from every later mention or restatement of new instances, and dispositions all five classes (2–6). **This file does not restate those five dispositions as its own finding or take a position on any of them** — same treatment §6.5 already gives the CP12 itemization deltas above. The note mints nothing, rules nothing, recommends no homing option, and does not close the item; newest F-Stats-1 authority remains `v1.60`, unchanged; the shape v1.60 itself records as "stands at 40 / 39 / 20, unminted; v1.48 §51.5 option 3 stands" (per the same note, which found no verbatim "second-shape mint decision" wording in v1.60's text; marked Evoni-gated here as a decision, without asserting that v1.60 itself names who gates it); StorytellerMemory references (read, not closed — per `F-Stats-1_StorytellerMemory_References_2026-09-10.md`, whose own text states it "does not close Item 5, or declare it closed"); `worldStudio.js:1838–1859` transactionality (**MEASURED-no / not closed** per `F-Stats-1_WorldStudio_Transactionality_2026-09-10.md`: five raw `sequelize.query(... DELETE ...)` calls and no `sequelize.transaction()`, `BEGIN`, or shared transaction object in the handler body as read; only a Fix Plan revision closes it); `worldEvents.js:570`, `:1973`, `worldStudio.js:3121` read — `F-Stats-1_CompoundPredicates_Read_2026-09-16.md` (issue #1490, PR #1491) reads all three against §63.5's own characterization (auth tier, raw-SQL-vs-ORM, predicate shape, `deleted_at` exclusion), including a mismatch record at `worldStudio.js:3121` (its predicate is `id = :relId AND character_id = :cid`, with no `show_id` term, against §63.5's claim that all three carry a `show_id` term in the `:showId` cohort). **This file does not restate that note's per-site findings, including the `worldStudio.js:3121` mismatch record, as its own finding, and takes no position on them** — same treatment §6.5 already gives the PE #62 overlap and §35.5 homing notes above. Newest F-Stats-1 authority remains `v1.60`, unchanged; only a Fix Plan revision closes this item; it remains **STILL OWED**. | Citation corrected per `F-Stats-1_PhaseB_OwedScoping_2026-09-10.md` (issue #1331, PR #1332), not re-derived here: only the `worldStudio.js:1838–1859` transactionality item and the three compound-predicate sites (`worldEvents.js:570`, `:1973`, `worldStudio.js:3121`) source from v1.60 §63.5 itself; the reads slice, the PE #62 overlap, and StorytellerMemory references trace instead to v1.60's Register hygiene section, its `Owes:` bullet; §35.5 classes 2–6 homing traces to that section's `Carries forward:` bullet. Transactionality read: `F-Stats-1_WorldStudio_Transactionality_2026-09-10.md` (issue #1350, PR #1351). PE #62 overlap location: `F-Stats-1_PE62_Overlap_Location_2026-09-11.md` (issue #1368, PR #1369) — a candidate located, not a ruling; v1.60 remains the item's own authority, unchanged. §35.5 classes 2–6 homing located: `F-Stats-1_S355_Classes2to6_Homing_2026-09-14.md` (issue #1419, PR #1420) — locates and dispositions all five classes, not a ruling; v1.60 remains the item's own authority, unchanged. Compound-predicate site read: `F-Stats-1_CompoundPredicates_Read_2026-09-16.md` (issue #1490, PR #1491) — reads all three sites against §63.5's own characterization, not a ruling; v1.60 remains the item's own authority, unchanged. |
| **MIXED.** XK-1 admission status after reach collapsed to F-Stats-1 alone (limb 2 boot-path reading) — **STILL OWED**. XK-2 extent census — **DONE** (`docs/audit/XK-2_Extent_Census_2026-09-05.md`, Task #1259/PR #1271; see §6.2 for its findings and the population-membership question it leaves open, which XK-2's fix disposition does not depend on). XK-3 full `authorize` call-site population (Gate 3 needs a live DB: Evoni) — **STILL OWED**. | CKR; `XK-2_Extent_Census_2026-09-05.md` |
| **STILL OWED** — FD-70 remains next-available and unminted per `F-AUTH-1_PE65_Execution_Sequence_2026-09-02.md` §8. Route-shadowing FD decision for the six dead declarations (item 10-B, under limb 1). | Route_Shadowing_Survey |
| **DONE.** `Prime_Studios_Audit_Handoff_v26.md` filed (PR #1203, 2026-09-03), superseding v25 Sec 1/2/3/6 and carrying Amd10 §J3/§J4/§J5 in the form Amendment 10 proposed; the two v26 draft-material documents are void per their own absorption condition. F-Stats-1's "Phase B live" face was re-derived (v26 Sec 1/2) and found unchanged — still `v1.60`, blob identical. **A correction banner landed on v26 after merge** (PR #1206, 2026-09-03) restoring two method-note passages dropped from the squash commit — see §6.1. v26 did not ratify limb 1's discharge or close FD-64/FD-67/PE #65, naming `F-AUTH-1_Fix_Plan_v2.69` as Evoni's to rule instead. **`v2.69` has since landed** (PR #1211, 2026-09-03; provenance-banner correction PR #1213, same day) and did exactly that — see §6.1. | `Prime_Studios_Audit_Handoff_v26.md`; PR #1203, #1206; `F-AUTH-1_Fix_Plan_v2.69.md`; PR #1211, #1213 |
| **RESPONDED TO; underlying lines STILL OWED.** `Session_PE_Roster.md` now carries an additive "Roster hygiene banner — 2026-09-05" (PR #1285, issue #1282) MEASURING all three points named here: (A) the closed index omits PE #63 and PE #66; (B) PE #64's severity re-rule is Evoni-gated and STILL OWED, not performed by the banner; (C) the `§AD/§AE/§AF` letter collision is a reader warning, not a defect in this file. **The banner corrects none of the underlying lines** — by its own "what this banner does not do": the closed index still omits #63/#66, PE #64's `Status:` line is unchanged, and the notation collision is unresolved, only cross-referenced. | `Session_PE_Roster.md`'s own banner; PE roster, Amd29/30 |
| **STILL OWED.** F-Sec-3 cold items: `socialProfileRoutes.js:1010` classification; `evaluation.js` frontend key audit; history-table consolidation; the `financialPressureService.js:208` ledger mutation. | F-Sec-3 inventory §5 |
| **STILL OWED.** F-Tools-1 opens: stop hook, deploy-hook noise, deploy-production hardening, GitHub App identity inventory (PE #59); PE #67 fail-closed branch-base gate script. | F-Tools-1 §4 |
| **OPEN, adjudication spans four documents, named together here.** `F-Tools-1_CloudCredentialScope_2026-09-05.md` (two cloud-session refusals of `git push origin --delete`), `F-Tools-1_CloudCredentialScope_SecondOccurrence_2026-09-06.md` (a second occurrence of the same refusal pattern), `F-Tools-1_CloudCredentialScope_LaptopContrast_2026-09-06.md` (the identical delete, run from Evoni's laptop against the same branch a cloud session had refused, succeeded — ATTESTED by her direct report; the branch's resulting absence from `origin` independently MEASURED via `git fetch --prune`), and `F-Tools-1_CloudCredentialScope_FourthOccurrence_2026-09-07.md` (PR #1303; a fourth cloud-session refusal, reproduced against three branches, paired in the same session with two plain pushes that succeeded without error — the refusal is specific to `--delete`, not to write access generally). **All four leave the same question open:** whether the cloud-session 403 is a credential-scope limit or a policy-layer effect specific to that session's egress path — not adjudicated, not repo-derivable. Unrelated to the F-Tools-1 row above; nothing moves between the two rows. | `F-Tools-1_CloudCredentialScope_2026-09-05.md`; `..._SecondOccurrence_2026-09-06.md`; `..._LaptopContrast_2026-09-06.md`; `..._FourthOccurrence_2026-09-07.md` |
| **STILL OWED** — the DRAFT still reads "Nothing records that it also closed cross-environment write." F-Deploy-1 amendment recording that the SSM rewrite also closed cross-environment write. | Production_State_Provenance §8 |
| **DONE.** `F-Tools-1_BranchWriteAndAuthorization_2026-09-09.md` filed (PR #1322, 2026-09-09), recording the provenance of the two 2026-09-09 commits on `claude/issue-1317-wakeup-amd-numeric-sort`, the outcomes of PR #1318 (merged at `914001a3`), PR #1320 (closed unmerged), and issue #1319 (closed), and reading the predecessor attempt at branch tip `f7b5964a` (`docs/audit/F-Tools-1_ConcurrentBranchWrite_2026-09-09.md`, never merged) as an unmerged artifact: that document's own §3 and §4 headings are labelled `ATTESTED-from-transcript`. Its author declaration states the authoring session had no transcript. | `F-Tools-1_BranchWriteAndAuthorization_2026-09-09.md`; PR #1318, #1320, #1322 |
| **DONE.** `.github/pull_request_template.md`'s validation block listed three of `/validate`'s four checks, omitting `node scripts/check-root-junk.js`; added, same style and order as the skill's own list. No other line in the template changed. | PR #1326 (issue #1325) |
| **DONE.** `F-Tools-1_SessionDispatch_2026-09-10.md` filed (issue #1327 / PR #1328, 2026-09-10), recording three session dispatches: two with `post_turn_summary.status_category` `need_input`, one recording no turn run. Mints nothing, rules nothing. | `F-Tools-1_SessionDispatch_2026-09-10.md`; issue #1327; PR #1328 |
| **DONE.** `F-Stats-1_PhaseB_OwedScoping_2026-09-10.md` filed (issue #1331 / PR #1332, 2026-09-10): a MEASURED itemization of F-Stats-1 Phase B's seven owed items, quoted from `v1.60`'s own text and located to where each actually sits in the document (only two of the seven source from §63.5 itself); the four named code locations read at `origin/main`, line numbers confirmed exact; three citation defects recorded as found — the §63.5 attribution, "second-shape mint decision" having no verbatim basis in v1.60, and F-App-1 §12.11 not existing under that heading in either F-App-1 fix plan file. Mints nothing, rules nothing, proposes no order of work. | `F-Stats-1_PhaseB_OwedScoping_2026-09-10.md`; issue #1331; PR #1332 |
| **DONE.** `F-Stats-1_StorytellerMemory_References_2026-09-10.md` filed (issue #1333 / PR #1334, 2026-09-10): performs the read v1.60 §63.1 marked "not read" — whether `StorytellerMemory`'s two foreign keys (`line_id`, `character_id`) declare a `references` key. Neither does, confirmed by a whole-file grep. The four sibling models (`Layer`, `LayerAsset`, `Scene`, `SceneFootageLink`) are re-derived independently and match §63.1's table. The note takes no position on whether the finding is a defect, proposes no remedy, and does not declare Item 5 closed. | `F-Stats-1_StorytellerMemory_References_2026-09-10.md`; issue #1333; PR #1334 |
| **DONE.** `F-Stats-1_ShapeMint_Options_2026-09-10.md` filed (issue #1337 / PR #1338, 2026-09-10): a briefing note quoting v1.48 §51.5's three homing options for the F-Stats-1 shape in the plan's own words, tracing the 40/39/20 shape's provenance across thirteen revisions from its 7/6/5 origin at v1.48, and recording that "stands" is not §51.5's own word — it first appears at v1.49, not in §51.5's own text. Takes no option among the three, recommends none, ranks none, and mints nothing. The shape-mint ruling itself is Evoni's and remains unmade at this basis. | `F-Stats-1_ShapeMint_Options_2026-09-10.md`; issue #1337; PR #1338 |
| **DONE.** `F-Tools-1_BranchWriteAndAuthorization_PreRebaseAncestry_2026-09-10.md` filed (issue #1341 / PR #1342, 2026-09-10): an additive amendment recording that `1cbf7675`'s linear ancestry from `76506bf1` is the post-rebase shape of the #1317 branch write, and that the two commits now tagged and pushed to `origin` as `pre-rebase/1317-b878ab9` and `pre-rebase/1317-fd87694f` sat on a different base, `1157f06c`. Amends `F-Tools-1_BranchWriteAndAuthorization_2026-09-09.md` by addition only — that document is not edited, no banner is added to it. Mints nothing. | `F-Tools-1_BranchWriteAndAuthorization_PreRebaseAncestry_2026-09-10.md`; issue #1341; PR #1342 |
| **DONE.** `.claude/skills/task/SKILL.md` step 3 amended (issue #1343 / PR #1344, 2026-09-10): adds one sentence — when an issue explicitly names a branch, that name overrides the assigned `claude/**` branch, and the session creates and checks out the issue's named branch, stopping and saying so if that isn't possible rather than substituting silently. No other line in the file changed. | issue #1343; PR #1344 |
| **DONE.** `CI_LocalSuite_Divergence_2026-09-11.md` filed (issue #1358 / PR #1359, 2026-09-11): records that CI's Tests job passed all 144 suites at `089cfa85`, including five suites that failed locally, but the decisive local measurement traced the local failures to a host-to-Docker PostgreSQL authentication failure. The note rules nothing, proposes no remedy, mints nothing, and does not establish those five suites as broken against a confirmed reachable local test database. | `CI_LocalSuite_Divergence_2026-09-11.md`; issue #1358; PR #1359 |
| **DONE.** `F-AUTH-1_SessionConduct_2026-09-11.md` filed (PR #1390, task #1389); correction banner prepended (PR #1394, task #1393) withdrawing Item 4. Revised count read directly from its banner: **two boundary crossings** (Item 1 `--admin` merge, Item 2 unconfirmed branch deletion) and **one pacing miss** (Item 3 Slice 2 unpaused dispatch). Mints nothing, rules nothing. | `F-AUTH-1_SessionConduct_2026-09-11.md` |
| **DONE.** `F-Deploy-1_DeployDev_DispatchRead_2026-09-12.md` filed (issue #1397 / PR #1398, 2026-09-12): a MEASURED, read-only record of the "Deploy to Development" workflow's dispatch path — trigger, permissions, concurrency, every job/step in source order, migrations, restarts, and presence checks for every file path the workflow references — plus a labelled pre-dispatch reading aid. No dispatch performed. Mints nothing, rules nothing. | `F-Deploy-1_DeployDev_DispatchRead_2026-09-12.md`; issue #1397; PR #1398 |
| **DONE.** `POST /login`'s unreachable-region read — filed as `F-AUTH-1_LoginHandler_UnreachableRegion_Read_2026-09-14.md` (issue #1428, PR #1429, 2026-09-14): reads `src/routes/auth.js`'s unconditional 401 return (lines 52–56) and the unreachable `try`/`catch` it precedes (lines 57–120), and classifies the region retained-by-design, per the file's own comment tying the disablement to FD-65's still-open issuance half. **This file does not restate that classification, or the note's own §10 item 6 consequence, as its own finding** — same treatment §6.5 already gives the F-Stats-1 homing note above. Mints nothing (no FD-70, no XK-4, no PE #69), rules nothing, recommends nothing; touches neither FD-65's open issuance half nor FD-68. | `F-AUTH-1_LoginHandler_UnreachableRegion_Read_2026-09-14.md`; issue #1428; PR #1429 |
| **DONE.** `logTransaction` name-collision read — filed as `F-Sec-3_LogTransaction_NameCollision_Read_2026-09-14.md` (issue #1435, PR #1437, 2026-09-14): reads the two same-named `logTransaction` functions under `src/services/` — `financialTransactionService.js:239` (all six live call sites resolve here) and `financialPressureService.js:190` (zero callers in `src/` or `tests/`) — and records how they differ (parameter shape, table written, error handling, return shape), plus a future-miscall hazard shape it states and does not act on. **This file does not restate that comparison or the hazard observation as its own finding** — same treatment §6.5 already gives the other note-filing rows above. Mints nothing (no FD-70, no XK-4, no PE #69), rules nothing, recommends no rename/deletion/consolidation; touches none of F-Sec-3's open cold items (the `character_state` key-canonicalization work tracked separately, per the note's own Closing section). | `F-Sec-3_LogTransaction_NameCollision_Read_2026-09-14.md`; issue #1435; PR #1437 |
| **DONE.** `F-AUTH-1_AuthIssuanceSurface_Read_2026-09-15.md` filed (issue #1441, PR #1442, 2026-09-15): records the repo's token issuance and verification surface — the shared `verifyToken` algorithm-routing function in `src/middleware/auth.js`, the HS256 issuance/verification surface in `src/services/tokenService.js`, the sole (unreachable) production mint site in `src/routes/auth.js`, and the narrower `jwtAuth.js` HS256-only acceptance surface — and carries Evoni's three 2026-09-14 rulings on password-login scope, the HS256 boundary, and FD-65's issuance-half closing criterion as hers, cited, not re-derived. **This file does not restate either the surface read or the three rulings as its own finding** — same treatment §6.5 already gives the other note-filing rows above. Mints nothing (no FD-70, no XK-4, no PE #69), rules nothing, proposes no implementation design for password login; no host, AWS, database, or Cognito contact. | `F-AUTH-1_AuthIssuanceSurface_Read_2026-09-15.md`; issue #1441; PR #1442 |
| **DONE.** `F-AUTH-1_JWTAuth_Cognito_Claim_Finding_2026-09-15.md` filed (issue #1444, PR #1445, 2026-09-15): records that `jwtAuth.js`'s header/comment claim of Cognito support was never accompanied by a Cognito verifier call — present with no such call at the file's own addition commit and unchanged since — what a well-formed RS256 (Cognito) token receives from the nine routes `authenticateJWT`/`optionalJWTAuth` gate (HTTP 401, code `AUTH_INVALID_TOKEN`, indistinguishable from a bad-signature HS256 token), and that `compositions.js:813`'s duplicate `PUT /:id` registration is unreachable dead code behind the first-registered handler at line 476. **This file does not restate either finding as its own** — same treatment §6.5 already gives the other note-filing rows above. Mints nothing (no FD-70, no XK-4, no PE #69), rules nothing, states no sequencing directive for Cognito adoption; no host, AWS, database, or Cognito contact. | `F-AUTH-1_JWTAuth_Cognito_Claim_Finding_2026-09-15.md`; issue #1444; PR #1445 |
| **DONE.** `F-AUTH-1_Fix_Plan_v2.73.md` filed (2026-09-16, PR #1467): rules FD-65's issuance half **REOPENED**, on the trigger `v2.67` §4 itself named — `PR #1457`'s 2026-09-15 re-enablement of `POST /api/v1/auth/login`. Establishes the two-stage discharge (`v2.67` §4's three preconditions; 1 and 2 MET, 3 NOT MET) / closure (live verification, Evoni's ruling (c)) path later completed by `v2.74` and `v2.75`. Supersedes `v2.67`'s CLOSED-BY-REMOVAL standing for the issuance half; the privilege half stays closed on the merits. Mints nothing. | `F-AUTH-1_Fix_Plan_v2.73.md`; PR #1467 |
| **DONE.** `docs/audit/F-Tools-1_AWSReadAccess_Ruling_2026-09-16.md` filed (PR #1469, #1470), with two additive amendments (PR #1471 — `cognito-idp list-user-pool-clients`; PR #1473 — `cognito-idp list-groups`): narrows `CLAUDE.md`'s blanket `aws` prohibition to a bounded read-only allowlist, motivated by FD-65's need for a live pool read. Bars any write, credential-returning call, or long-lived key/secret in a session's environment; barred even where nominally "read" (`get-parameter`, `get-secret-value`). Mints nothing. | `CLAUDE.md`; `F-Tools-1_AWSReadAccess_Ruling_2026-09-16.md` + 2 amendments; PR #1469, #1470, #1471, #1473 |
| **DONE.** `src/services/cognitoPasswordAuthService.js` fixed (PR #1472, `1c578b71`): `SECRET_HASH` was sent unconditionally on every `InitiateAuth` call, an assumption from Task #1456's premise rather than a read of the client's actual configuration. Measured live under the new `aws` allowlist: the pool's one app client (`episode-metadata-api-client-dev`) has no secret (`HasSecret: false`) — the exchange as shipped could not have succeeded against it, for any credential. Fix: `SECRET_HASH` now computed and sent only when a secret is configured; omitted otherwise, with a log line on the secret-less path. Adds the test that would have caught the original defect; 13/13 passing. | `F-AUTH-1_Fix_Plan_v2.74.md`; PR #1472 |
| **DONE.** `F-AUTH-1_Fix_Plan_v2.74.md` filed (PR #1475, 2026-09-16): narrows `v2.73`'s precondition-1 evidentiary basis without re-ruling its MET status — distinguishes claim (1) the exchange is correctly shaped (TRUE, MET, unchanged) from claim (2) the exchange succeeds live (UNKNOWN, not addressed by a code fix). Cites the live reads that found the `SECRET_HASH` defect and `PR #1472`'s fix. Explicitly not asserted as Evoni's ruling until reviewed; reviewed and confirmed same session. | `F-AUTH-1_Fix_Plan_v2.74.md`; PR #1475 |
| **DONE.** `F-AUTH-1_GroupCaseMismatch_MEASURED_2026-09-16.md` filed (PR #1474), with two addenda: a reproduction addendum (`F-AUTH-1_GroupCaseMismatch_Addendum_Reproduced_2026-09-16.md`, PR #1476) and, after the fix, a second addendum (`F-AUTH-1_GroupCaseMismatch_Addendum2_Fixed_2026-09-16.md`, issue #1480, PR #1481). Original finding: the pool's three Cognito groups (`admin`, `editor`, `viewer`) are lowercase; two independent middleware implementations (`auth.js`'s `authorize`/`authorizeRole` alias, `jwtAuth.js`'s separate `requireGroup`) did case-sensitive exact-match comparison — 38 of 51 group-check call sites used uppercase `ADMIN` and would deny a real member of the `admin` group; 13 used lowercase and were already correct. The first addendum reproduced this end to end (real token, `cognito:groups: ["admin"]`, `403 AUTH_GROUP_REQUIRED` on `PUT /api/v1/assets/:id/approve`), recommending no fix among three named shapes. **Fixed** (PR #1479, `fix(auth): make group comparison case-insensitive in authorize and requireGroup`): both functions now lowercase both sides of the comparison (plain `.toLowerCase()`, not `.toLocaleLowerCase()`/`localeCompare`) before the membership check; no call site's literal changed. The second addendum re-reads both sites at the fixed basis, re-runs the 51-site enumeration (unchanged), and records a second reproduction — on Evoni's own account — that the original failure mode no longer occurs. **Per the second addendum's own §6, which this row cites rather than re-derives: the condition the original note measured no longer exists in current code, but neither the original note nor either addendum minted or disposed of anything, so none of the three has an open/closed state for this row to move** — this row does not write that any of them is closed, resolved, or disposed. **Residue** (second addendum §7): the 38 uppercase-`ADMIN` call sites still pass a string naming a group that does not exist in the pool; the fix's normalization makes this harmless, not correct — removing that normalization later, or adding a third unnormalized comparison site, would make the same 38 sites fail again. Independent of FD-65 — authorization, not issuance. | `F-AUTH-1_GroupCaseMismatch_MEASURED_2026-09-16.md`; first addendum; second addendum; issue #1480; PR #1474, #1476, #1479, #1481 |
| **DONE.** `F-AUTH-1_Fix_Plan_v2.75.md` filed (PR #1477, 2026-09-16): closes FD-65's issuance half on live verification Evoni performed herself, outside any agent session — `initiate-auth` returned tokens, `POST /login` returned the five pinned fields, `GET /me` accepted the issued token. Rules closure and discharge as separately evidenced criteria per `v2.73` §3's own text, not a sequence; precondition 3 survived this closure as its own owed item, not mooted by it — **later discharged, on direct evidence, by `v2.76` §4/§5.2** (see the row below). Distinguishes this closure (on the merits, live-verified) from `v2.67`'s superseded CLOSED-BY-REMOVAL standing (reversible by re-enabling the route; this one is not). **FD-65 is CLOSED entire.** Reports the verification as pass/fail and shape only — no token, credential, or field value in the document. | `F-AUTH-1_Fix_Plan_v2.75.md`; PR #1477 |
| **DONE.** `F-Stats-1_CompoundPredicates_Read_2026-09-16.md` filed (issue #1490, PR #1491, 2026-09-16): reads the three compound-predicate sites `F-Stats-1_Fix_Plan_v1.60.md` §63.5 names as unread candidates (`worldEvents.js:570`, `:1973`, `worldStudio.js:3121`) for auth middleware (`requireAuth` on all three), raw-SQL-vs-ORM, predicate shape, and `deleted_at` exclusion, and records a mismatch: `worldStudio.js:3121`'s actual predicate (`id = :relId AND character_id = :cid`) carries no `show_id` term, against §63.5's claim that all three sites are in the `:showId` cohort. **This file does not restate that comparison or the mismatch as its own finding** — same treatment §6.5 already gives the other note-filing rows above. Mints nothing (no FD-70, no XK-4, no PE #69), rules nothing, closes nothing; newest F-Stats-1 authority remains `v1.60`. No host, AWS, database, or Cognito contact. | `F-Stats-1_CompoundPredicates_Read_2026-09-16.md`; issue #1490; PR #1491 |
| **DONE.** `F-AUTH-1_Fix_Plan_v2.76.md` filed (PR #1486, 2026-09-16): rules `v2.67` §4 precondition 3 **discharged, on direct evidence** — a live poisoned-body login request against the real Cognito pool, performed by Evoni outside any agent session, returned only the pool's real group; neither caller-supplied `groups: ['ADMIN', 'superuser']` nor `role: 'ADMIN'` appeared in the response (§5.2). Corrects a reasoning gap in this document's own first draft, which had argued the same purpose was already served by `v2.75` §3's differently-scoped ordinary-login verification (§7). `LOGIN_DISABLED` remains `true` in `tests/integration/f-auth-1-fd65.test.js`; the flip is not authorized. FD-65 untouched, **CLOSED entire** as `v2.75` left it. Names the test file's rewrite as ordinary owed work, no precondition attached (§8). Mints nothing — FD-70, XK-4, and PE #69 remain next-available and unminted (§0). | `F-AUTH-1_Fix_Plan_v2.76.md`; PR #1486 |
| **DONE.** `F-AUTH-1_GroupCase_MintDecision_2026-09-16.md` filed (PR #1487, 2026-09-16): records Evoni's decision that the group-case finding (`F-AUTH-1_GroupCaseMismatch_MEASURED_2026-09-16.md` and its two addenda; the fix, PR #1479) does not mint FD-70 — the finding is fully recorded across four documents (measured, reproduced end to end, fixed the day it was found, and the fix itself reproduced end to end), and the residual 38 uppercase-`ADMIN` call sites are harmless under the `.toLowerCase()` fix, not a live defect (§4). Does not reopen or re-dispose `v2.69` §6 Ruling 5's own, separate FD-70 disposition — the two CP6 counting-error rows, a different matter (§2). **This document mints nothing itself; FD-70 remains next-available and unminted** (§3–§4). | `F-AUTH-1_GroupCase_MintDecision_2026-09-16.md`; PR #1487 |
| **DONE.** `Canon_TableExpectation_Census_2026-09-17.md` filed (issue #1498, PR #1500, 2026-09-17): a MEASURED census recording, table by table, what current `origin/main` code expects to exist (from four static sources: Sequelize models, migrations, request-path `.sync()`/inline `CREATE TABLE`, and raw SQL in routes/services) against whether the 2026-08-29 canon capture shows that table. Prep material for Evoni's schema-fork ruling (v25 item 8 disposition, above). **This file does not restate its counts or findings as its own** — same treatment §6.5 already gives the other note-filing rows above. Mints nothing, rules nothing, recommends no canonical schema. | `Canon_TableExpectation_Census_2026-09-17.md`; issue #1498; PR #1500 |
| **DONE.** `Canon_AbsentTable_Classification_2026-09-17.md` filed (issue #1508, PR #1509, 2026-09-17): classifies the 56 census-absent tables as REAL-ROUTE 30, REAL-SERVICE 6, FALSE POSITIVE 4, and UNUSED 16. It supplies the input to the later schema-fork ruling and does not itself choose a canonical schema or mint a finding. | `Canon_AbsentTable_Classification_2026-09-17.md`; issue #1508; PR #1509 |
| **DONE.** The 2026-09-17 canon capture and evidence note filed (PR #1511; no issue was filed for that PR; its commit body references #1504 in error): Evoni's own console read was ATTESTED outside any agent session; the 2026-09-17 and 2026-08-29 captures have the same normalized record set (2,760 records, no table or column difference). The capture names canon instance `episode-control-dev` and preserves the `pgmigrations` and `SequelizeMeta` row counts in the evidence note; it makes no schema change. | `EvidenceNote_Canon_Capture_2026-09-17_Note.md`; PR #1511 |
| **DONE.** `F-Deploy-1_DeployDev_FreezeStatement_Conflict_2026-09-17.md` filed (issue #1499, PR #1501, 2026-09-17): a MEASURED read quoting, side by side, every repository statement found about whether dispatching `Deploy to Development` is safe under the freeze — the hazard doc's Sec 3 items 9 and 7, `CLAUDE.md`'s freeze bullet, `DEVELOPMENT_WORKFLOW.md`'s Evoni-only table, this file's own §0 item 8 and §7, and `F-Deploy-1_DeployDev_DispatchRead_2026-09-12.md`'s database-target read — and records two separate conflicts (§3.2, §3.2b) plus what would resolve each (§3.4). **This file does not restate the note's conflict analysis as its own finding** — same treatment §6.5 already gives the other note-filing rows above. Takes no position on which statement governs; rules nothing; mints nothing. | `F-Deploy-1_DeployDev_FreezeStatement_Conflict_2026-09-17.md`; issue #1499; PR #1501 |
| **DONE.** `F-AUTH-1_SessionRefresh_Read_2026-09-19.md` filed (PR #1560, 2026-09-19): a MEASURED read of why sessions end on token expiry — traces the end-to-end path from `POST /login` (returns a Cognito-issued `refreshToken`) through the frontend F-Auth-4 interceptor to the old `POST /refresh` (HS256-only against local `JWT_SECRET`, no branch that accepts a Cognito refresh token), concluding every session ended within one request of the access token's expiry. **This file does not restate that end-state finding as its own** — same treatment §6.5 already gives the other note-filing rows above. Mints nothing, rules nothing, recommends no fix design; no host, AWS, database, or Cognito contact. | `F-AUTH-1_SessionRefresh_Read_2026-09-19.md`; PR #1560 |
| **DONE.** `F-AUTH-1_Fix_Plan_v2.77.md` filed (PR #1562, 2026-09-20): lands Evoni's adopted rulings on the `/refresh` defect above — Cognito end to end, HS256 refresh retired for user sessions, response contract pinned, FD-65 not reopened — with the ruling text's out-of-repository drafting provenance disclosed at its own §3 before the text, and Evoni's ATTESTED app-client configuration read at §6. **This file does not restate the rulings as its own** — carried at §0 item 5, §4.1, and §6.1 above. Mints nothing; implements nothing. | `F-AUTH-1_Fix_Plan_v2.77.md`; PR #1562 |
| **STILL OWED, not yet filed.** A read of logout/revocation for Cognito-issued access tokens — surfaced by Task #1563/PR #1564's own E2E test edit (`tests/integration/auth.integration.test.js`, the full-auth-cycle test), whose comment states the Cognito-issued access token "isn't a local HS256 JWT this server can verify/revoke," so `POST /logout`'s blacklist mechanism (`TokenService.tokenBlacklist`, HS256-only) does not cover it. No register document reads this yet. | `tests/integration/auth.integration.test.js` (PR #1564); `src/routes/auth.js` logout handler; `src/services/tokenService.js` |

**Code PRs (small, inside or adjacent to the sequence)**

| Item | Source |
|---|---|
| **DONE (code and recording).** FD-64: fixed — `getRolesForshow` → `getRolesForShow` at `65cbe7013`; `Model.update()` `where` clause added at `AssetRoleService.js:151` at `2e5dbdf28`. **Closed** `v2.69` §6 Ruling 2 (2026-09-03); that ruling's own text is disclosed as session-proposed and Evoni-approved, per `F-AUTH-1_FD67_Branch_Ruling_2026-09-02.md` §1's precedent. Closing also surfaced a third, unminted defect — `AssetRole` missing from `src/models/index.js`'s hand-maintained `models` subset, carried forward at §6.5. | `F-AUTH-1_Fix_Plan_v2.69.md` §6 Ruling 2 |
| **DONE (code).** Task #1563: `POST /api/v1/auth/refresh` rewritten to call `cognitoPasswordAuthService.refreshWithCognito` (Cognito `REFRESH_TOKEN_AUTH`) per `F-AUTH-1_Fix_Plan_v2.77.md` §8(a) — implements the ruling above; does not close it (§8(b), live real-pool verification, remains owed, Evoni-only). | `F-AUTH-1_Fix_Plan_v2.77.md`; PR #1564 |
| **STILL OWED.** FD-66 disposition and a baseline migration sequence (after item 8; Evoni rules, a PR executes). | FD-66 DRAFT |
| **STILL OWED.** 28 `claude/**` branch tips lack `[skip-automerge]` while `auto-merge-to-dev.yml` still carries the push trigger in YAML (re-enable decision is gated; the fix is a branch-cleanup ruling, not a code change). | Amd26 §AB3 |

**Session PE roster (PE #27–#68), carried unchanged — see the roster-hygiene item above for what is stale in it.** Closed: #37, #41, #48, #58, #63, #66. P0 open: #51, #52 (F-AUTH-1 pre-flight inventories). P1 open: #27 (prod smoke test accepts 502/503), #38, #40, #42–#47 (npm audit; prod-RDS column drift), #49 (50% AI error rate), #54, #55, #64. P2 open: #31, #39, #50, #53, #56, #57, #59–#62, #65, #67, #68. PE #1–#26 and #28–#36 live in F-AUTH-1's Track 8 roster, not here.

Declined or bounded non-actions (do not reopen without a reason): fourteen in-place-amended documents outside the chain; three `.docx`-only handoff revisions; bare `#NNN` in 279 documents; Amd4's ruling that the second-party observation is not an operating assumption.

---

## 7. Infrastructure and the freeze (ASSERTED unless noted)

| Thing | State |
|---|---|
| Prod box `episode-backend` | `i-02ae7608c531db485`, 54.163.229.144, t3.small, Ubuntu 22.04, no IAM instance profile (so no SSM). The working tree was moved to `origin/main` at `91193483` on 2026-09-18; the served frontend was rebuilt and replaced; the API process was restarted. `episode-api-prod-hotfix` runs on :3000 and `episode-worker` remains stopped. |
| Dev box `episode-dev-backend` | `i-016395bb5f7a51a0b`, EIP 54.87.253.45, t3.small, SSM-managed, role `episode-dev-backend-role`; deployed by manual `workflow_dispatch` of `deploy-dev.yml` (OIDC role `episode-gha-deploy-dev`, artifact via S3, DB creds from Secrets Manager `episode-metadata/dev/database`) — `deploy-dev.yml` is `disabled_manually` as of 2026-09-17 and not dispatched until the gated reconciliation session has run, per `docs/audit/F-Deploy-1_Fix_Plan_v1.50.md` §2, §5. |
| Canon RDS `episode-control-dev` | 10.0.20.224, `episode_metadata`, 143 tables, populated. **Misleadingly named: it is the production data.** Master credential lives only in Evoni's password manager (paper + fingerprint custody). FD-61: `StorageEncrypted=false`. |
| Fork RDS `episode-control-prod` | 171 tables, empty; SG still `0.0.0.0/0:5432` per docs; teardown deferred. |
| Cognito | One shared user pool and client for dev and prod (PE #64); three accounts, all Evoni's; Branch B (new prod pool, existing stays dev) ruled 2026-08-28, unexecuted. `docs/cognito-ids.txt` and `docs/COGNITO_USER_POOL_SETTINGS.md` retired as authority (PE #66). |
| Secrets | Secrets Manager: only the dev DB secret. SSM Parameter Store: four parameters, none for this app (Amd30). Static AWS keys in the prod `.env` (AD). Feb 2026 credential blobs remain in git history (rotated). |
| Workflows (MEASURED via API) | `Validate` active; `Deploy to Development`, `Deploy to Production`, and `Auto-merge to Dev` all `disabled_manually` (`Deploy to Development` disabled by Evoni 2026-09-17, outside any agent session, re-measured this session — `docs/audit/F-Deploy-1_Fix_Plan_v1.50.md` §5); a Copilot cloud agent workflow exists with no file in the tree. |
| DNS | `dev.primepisodes.com` A record → prod box (repoint drafted, never recorded executed). Prod behind an ALB; `PE #27`: prod smoke test accepts 502/503. |
| Monitoring | Three CloudWatch billing alarms ($100/$300/$500 → SNS). The ELB 5xx availability alarm `prod-availability-elb-5xx` is the F-Deploy-G1-AJ plan; Handoff v13 records it live from 2026-06-01, the AJ plan's own baseline lists zero availability alarms, and no later document re-verifies it. The June 2026 multi-day prod 502 was found by a manual check. |

**Freeze rules (from the hazard doc Sec 3):** no `pm2 restart/reload/save/delete`, no reboot, no deploy, no `.env` edit, no RDS modification, no data copy between instances, no dev-box deploy pointed at either instance, no SG "fixes", no re-enabling the disabled workflows. Reconciliation is its own gated session with a verified canon backup first. **`docs/audit/F-Deploy-1_Fix_Plan_v1.52.md` (ATTESTED) records that the 2026-09-18 reconciliation session ran and that the split-brain premise did not hold at its start; it does not say the freeze is lifted or rule on the hazard doc's standing.**

**What is safe today:** local development against Docker Postgres; cloud sessions and laptop CLI on branches; PRs to `main`. **Dispatching `Deploy to Development` is not permitted until the gated reconciliation session has run** — hazard doc Sec 3 items 7 and 9 govern, ruled per `docs/audit/F-Deploy-1_Fix_Plan_v1.50.md` §2; see §6.5 for the record. Conflict note §3.4 Q3 (the `DEVELOPMENT_WORKFLOW.md:247` citation correction) stays open, per `F-Deploy-1_DeployDev_FreezeStatement_Conflict_2026-09-17.md` §3.4 Q3 and v1.50 §7.

---

## 8. Register conventions (how to read and write `docs/audit/`)

- **Authority by numeric sort.** Newest `F-<Keystone>_Fix_Plan_v<N>.md` and `Prime_Studios_Audit_Handoff_v<N>.md` by *numeric* version; `ls | sort -V`. Record blob SHAs alongside numbers (an in-place amendment moves the blob, not the number).
- **Per-claim authority.** The newest revision governs only what it closes; older revisions stay authoritative for what they closed.
- **Banners newest-first; banner governs body.** Additive-supersede: never edit a filed document; prepend a dated correction banner or mint an amendment/revision. A banner may point but may not carry.
- **Standings:** MEASURED (repo read, anyone can check; paste command + output), ATTESTED (session record only), RULED (Evoni's decision, quoted), INFERRED (say so). Never upgrade a standing.
- **H1:** a precondition asserted in prose is not asserted; paste the command and raw output. **H2:** read `origin/main` explicitly (`git show origin/main:path`).
- **Skill/prompt prose is not an enforced boundary.** A skill can say "never closes" or "do not push," but that text does not restrict the executing session's tool calls; use Rule 7 as an explicit conversation gate for push, PR create, merge, and branch delete, and use tool-level restrictions where enforcement matters (`F-Tools-1_SkillBodyConstraint_2026-09-08.md`).
- **Evidence:** a claim is not a close; the artifact is the close. Empty stdout is not evidence unless exit 0 (FD-51). Workflow runtime state needs the Actions API (FD-50). Prose about other documents is never authority (FD-49).
- **Identity by function, never by label:** RDS by `SELECT current_user, current_database(), inet_server_addr()`; processes by ingress route then port then PID; hosts are addresses.
- **Commit hygiene:** `docs(audit): <what> [skip-automerge]`; no closing keywords next to `#N`; PR bodies via `--body-file`; explicit-path `git add`; squash-merge; branch delete.
- **Navigation documents outlive their windows:** before following any doc that tells you what you may read, check its subject is still open against the live register tail (onboarding §4 rule 13; the `[3]` cold-entry docs are VOID).
- **Standing labels on humans:** "Evoni-gated" means only she can perform or authorize it; agent sessions record NOT PERFORMED, never infer.
- **Cite only from the read you're writing from.** A citation must come from the specific read this document performs, not carried forward from earlier in the same session — the corrective lesson from an 8-of-23-wrong-citations catch in `docs/LOCAL_DEV_LOGIN.md`'s first draft (issue #1263).
- **Build-absence is not live-behavior.** Proving a route or fragment is absent from a production build does not prove it behaves correctly when actually reached — `F-AUTH-1_Tier5_DevTokenCarrier` PR #1267's own acceptance evidence proved the carrier route was absent from a production build, but nothing exercised the route live; the actual bug (issue #1268, the pre-auth redirect race, fixed by PR #1269) lived entirely in the half #1267 didn't test. A route/feature task needs both proofs, not one.
- **Diff-reading as standing practice.** Paste the raw diff before asking for push authorization — one file per message if a combined paste won't survive the relay — rather than asking on the strength of a summary. Established after a summary missed two real things a diff caught: a string surviving into a production `dist` bundle, and a README port fix that stranded a reader by leaving surrounding prose inconsistent.
- **What "filed" means.** Content-equivalence to `/audit-file`'s eight checklist points counts as filed, full stop — the skill has no mechanism (no index, no banner-generator, no numbering registry) beyond those points. A hand-conformed document matching all eight needs no banner marking it filed, including retroactively for past documents conformed the same way (ruled 2026-09-05) — e.g. because a session's `disable-model-invocation: true` restriction blocked the skill's own invocation, as it did for `XK-2_Extent_Census_2026-09-05.md`.

---

## 9. Stale and dangerous documents (do not follow)

| File | Problem |
|---|---|
| `.github/agents/deploy.agent.md`, `.github/prompts/deploy-dev.prompt.md` | **Retired 2026-09-03 (PR #1201).** Both files still exist and Copilot still resolves them by path (frontmatter untouched), but their bodies now carry a freeze notice — production is FROZEN, deploys are Evoni-only via `workflow_dispatch`, agent sessions never run `ssh`/`scp`/`pm2`/`aws` — in place of the old SSH-to-`54.163.229.144`/`git pull`/`pm2 restart` procedure this table used to flag. Kept as a row rather than dropped so this table doesn't go silent on files it once warned about. |
| `.github/copilot-instructions.md`, old `CLAUDE.md` | Neon DB, `memories.js` single file, `optionalAuth` convention, port 5173, "no Tailwind". |
| `.github/instructions/memories-routes.instructions.md` | `applyTo: src/routes/memories.js` with a line map of a deleted file; `optionalAuth` scaffold. |
| `.github/agents/story-engine.agent.md` | Names deleted `StoryEngine.jsx`; "12K-line memories.js". |
| `README.md` | Jan 2026 plan: ECS, RDS Multi-AZ, Feb 14 launch, `dev` branch flow, Cognito groups. |
| `SESSION_HANDOFF.md` | 2026-04-14; Part 1 built, Part 2 (Screen Layer System) never built; PhoneHub has since changed. |
| `docs/archive/*.md` (334 moved phase reports plus `docs/archive/INDEX.md`; `git ls-tree --name-only origin/main docs/archive/ \| grep '\.md$' \| wc -l` → 335) | All written 2026-01-01 → 2026-03-01: the January metadata-API/CMS phases (110 `PHASE_*`, 8 `WEEK*`) and the February video suite; none describes the Before Lala / LalaVerse product. Treat as history. **Moved from top-level `docs/` into `docs/archive/` at this basis** (issue #1415, PR #1416); `docs/archive/INDEX.md` is the manifest (not a phase report, and the reason the live `ls-tree` count reads 335 against 334 moved) and `docs/audit/Docs_Archive_Census_2026-09-13.md` is the disposition record — see that census for its own reasoning, not restated here. `docs/API_REFERENCE.md` (3 route groups) and `API_QUICK_REFERENCE.md` (4) did **not** move — both stayed at their top-level `docs/` paths — and still carry their own "HISTORICAL — NOT AUTHORITATIVE" banners (PR #1287, dated 2026-09-06 — one day later than the task that produced them specified, a disclosed same-night deviation, no action owed) against **97** distinct `/api/v1` prefixes. **Reconciled at this file's own basis, superseding this row's prior "91":** `grep -c "app.use('/api/v1" src/app.js` → 137 statements; resolved to distinct prefix strings and `sort -u`'d → 97, of which 17 are the bare literal `/api/v1` shared by 17 different routers (an upper bound on route areas, not a count of them) — unchanged from PR #1287's own count at its basis `89acc3d7f`, consistent with source having been static since May 2026. Two further staleness gaps neither banner states: `API_REFERENCE.md`'s own Table of Contents lists six route groups against a body carrying two `## Authentication` sections — a duplicate, not just three missing groups; `API_QUICK_REFERENCE.md`'s "4 route groups" banner omits a fifth `## System` section (`/health`, `/api/v1` info) that reads like a route group by the same casual standard — a judgment call (resource groups only) the banner never states as a rule. |
| `docs/archive/DEPLOYMENT.md`, `docs/archive/DEPLOYMENT_COMPLETE.md`, `docs/archive/DEPLOYMENT_CONTROL_GUIDE.md`, `docs/archive/DEPLOYMENT_GUIDE.md`, `docs/archive/DEPLOYMENT_GUIDE_PHASE_3.md`, `docs/archive/DEPLOYMENT_GUIDE_PHASE_3A_4.md`, `docs/archive/DEPLOYMENT_MONITOR.md`, `docs/archive/DEPLOYMENT_QUICK_REFERENCE.md`, `docs/archive/GITHUB_DEPLOYMENT_SETUP.md`, `docs/archive/AWS_SETUP.md`, `docs/archive/CLOUDSHELL_MIGRATION_GUIDE.md`, `docs/archive/MIGRATE_NOW.md`, `docs/archive/RDS_READY_FOR_MIGRATIONS.md`, `docs/archive/00_NEXT_STEPS_ROADMAP.md` | Describe ECS / staging-branch / push-to-dev pipelines that never existed or are disabled, and contain copy-paste `aws` and migration commands against RDS. Following any of them recreates the May 30 incident path. **Moved from top-level `docs/` into `docs/archive/` at this basis** (issue #1415, PR #1416); content unchanged. |
| `docs/archive/README_AUDIT_FEBRUARY_2026.md`, `docs/archive/SECURITY_AUDIT_FINDINGS.md` | The May 11 redaction (#665) missed a staging RDS password literal that is still printed in both files' rotation lists. Redact (a docs PR), do not quote. **Moved from top-level `docs/` into `docs/archive/` at this basis** (issue #1415, PR #1416); content unchanged. |
| `.github/prompts/new-endpoint.prompt.md`, `new-context-loader.prompt.md`, `.github/MERGE_CONFLICT_RESOLUTION_CHECKLIST.md`, `.github/agents/endpoint-validator.agent.md` | Line ranges and scaffolds into the deleted `memories.js`; the validator nudges toward PM2 logs on the frozen box. |
| `package.json` `deploy:production` / `health:production` scripts | Call a root `deploy-production.sh` that does not exist. |
| `docs/cognito-ids.txt`, `docs/COGNITO_USER_POOL_SETTINGS.md`, `docs/connect-to-ec2.txt`, `docs/rds-endpoint-dev.txt` | Retired as authority; carry identifiers; do not paste their contents into prompts. |
| `docs/audit/F-Deploy-1_[3]_*` cold-entry docs | VOID (terminal banners); would lead a session to mint a duplicate close and a freeze-lift. |
| `backups/staging/*.sql` | Database dumps committed to the repo (Jan 2026). Read-denied in `.claude/settings.json`. |

---

## 10. Suggested work for the loop (Evoni chooses; nothing here is ruled)

Seven re-check passes are recorded below, one row per pass, oldest first. Each row states its own basis and comparison window independently of the others, so adding the next pass's row does not require re-reading prior rows. Item dispositions (DONE, STILL OWED, etc.) live in the numbered list below the table, unchanged by this table — the table records only which items moved during which pass, and why, not each item's current text.

| Pass | Basis / comparison window | Commits in window (PR → SHA) | Items moved this pass | Notes (disposition detail, disclosures, derivations) |
|---|---|---|---|---|
| **File Basis** (opening re-verification) | `origin/main` at `35745e2adbfcdc0e4a7f38086cc650bd2c52d257` (2026-09-11), compared from `ca10986e9d69b124b03a54082c4af4c61c312e98` — PR #1371's own resulting state, the most recent pass to touch this file at the time. Window: `ca10986e9d69b124b03a54082c4af4c61c312e98..origin/main`. | PR #1373 (`9c6f8650`)<br>PR #1375 (`9f76ebaf`)<br>PR #1377 (`10662c65`)<br>PR #1379 (`35745e2a`) | Item 6 | Item 6's ESLint debt is updated at this basis to reflect PR #1377 and PR #1379: `npx eslint src/ tests/ --format json` reports 16 errors across 10 `src/` files (plus 1 warning in `src/middleware/auth.js`; 17 problems across 11 files total), ATTESTED from local run. Items 1–5 and 7–12 are carried as re-verified at the prior resolved state. |
| **Task #1399** | comparison window `4dbc547c59e5565daf400affafa746c5f2ee88c5..origin/main` | PR #1398 (one commit) | none | `F-Deploy-1_DeployDev_DispatchRead_2026-09-12.md` does not touch ESLint, model IDs, navigation, the Story Engine backend, `docs/archive/`, test discovery, or the `getModels()` pattern — none of items 1–12 move; no item edited this pass. |
| **Task #1421** | comparison window `9c41479d5..origin/main` — **the window since PR #1398, the last commit actually checked against items 1–12, not since Task #1399's own re-check sentence, which only re-verified the one-commit window ending there.** | PR #1400 (`e56bc062a`)<br>PR #1402 (`404d33207`)<br>PR #1404 (`52d3cc761`)<br>PR #1406 (`7259857fb`)<br>PR #1409 (`18d48c649`)<br>PR #1410 (`083921002`)<br>PR #1412 (`83a3683ce`)<br>PR #1414 (`868c5c03b`)<br>PR #1416 (`b37574d53`)<br>PR #1418 (`900d6e16f`)<br>PR #1420's squash (`cb0a03b67`) | Item 12 (moves, DONE); items 6, 7, 8, 9, 10, 11 do not move | Item 6: PR #1402 modifies `src/routes/` files ESLint scans (see item 12), so item 6's count was re-run rather than inferred from the absence of a config change — `npx eslint src/ tests/ --format json` still reports 16 errors across the same 10 `src/` files and 1 warning in `src/middleware/auth.js` (17 problems across 11 files total), ATTESTED from local run, unchanged from the prior recorded count (item 6 does not move).<br>Item 7: no `claude-sonnet-4-2`/`claude-sonnet-4-5` occurrence added under `src/`.<br>Item 8: no `frontend/` file changed.<br>Item 9: `src/routes/storyEvaluationRoutes.js` untouched.<br>Item 10: no `backups/staging/` or the three identifier `.txt` files touched — `docs/*.md` archive movement (PR #1416, item 10's archive half, already recorded DONE) and a new census document (`Docs_Archive_Census_2026-09-13.md`, PR #1410) are the only archive-adjacent changes, and the census proposes, it does not move, remove, or rule anything item 10 names.<br>Item 11: no `jest.config.js` or `tests/` file changed.<br>**Item 12 moves**: PR #1402 (`404d33207`) added the identical `console.error('Failed to load models:', e.message)` logging line item 12's `templateStudio.js` fix already carried, to all five files item 12 named as still silent — `careerGoals.js`, `world.js`, `storyteller.js`, `worldEvents.js`, `wardrobe.js`. Re-derived fresh at this basis by two instruments, not assumed from the commit message alone: `grep -n "^async function getModels\|catch (e)" ` against all six files (the five plus `templateStudio.js`) shows each `getModels()` definition's `catch` clause now reads `catch (e) { console.error('Failed to load models:', e.message); return null; }`, identical across all six; and a widened pattern, `grep -rn -E "catch\s*\(\s*e[a-zA-Z]*\s*\)\s*\{\s*return null" --include='*.js' src/`, covering spacing and identifier variants a literal-string grep would miss, returns zero hits anywhere in `src/`. Item 12 edited below to record this as DONE. |
| **Task #1430** | comparison window `cb0a03b675761ae3d1789823d473aa11baba640b..origin/main` | PR #1422 (`894a39c8e` — Task #1421's own resulting commit, appearing in this window because it postdates the file's previously stated Basis line)<br>PR #1424 (`5b9c8da5c`)<br>PR #1426 (`2457b6e26`)<br>PR #1429 (`5219b594b`) | Item 6 (moves); items 7, 8, 9, 10, 11, 12 do not move | PR #1424 touches `src/routes/evaluation.js`, `shows.js`, and `worldEvents.js` (six unused variables and one dead query removed; none of the three is `storyEvaluationRoutes.js` — item 9 untouched) and updated item 6's ESLint figures in-file to 9 errors / 6 files.<br>PR #1426 touches `src/services/invitationGeneratorService.js` and adds one new test file, `tests/unit/services/invitationGeneratorService.test.js` — not among the uncollected files item 11 names, and no `jest.config.js` change accompanies it, so item 11 does not move — and likewise updated item 6's figures in-file to the same 9 errors / 6 files.<br>PR #1429 added `F-AUTH-1_LoginHandler_UnreachableRegion_Read_2026-09-14.md` under `docs/audit/` only, touching no `src/`, `frontend/`, or `tests/` path.<br>None of the three commits adds a stale model ID under `src/` (item 7), touches a `frontend/` file (item 8), touches `backups/staging/` or the three identifier `.txt` files (item 10), or touches a `getModels()` definition (item 12) — items 7, 8, 9, 10, 11, and 12 do not move this pass.<br>**Item 6 moves**, recorded below: re-derived fresh at this basis rather than carried, `npx eslint src/ tests/` still reports 9 errors, 1 warning, 10 problems, across the same 6 error-bearing files, unchanged from PR #1426's own count — but two of the nine are now recorded as unable to clear by a code fix without a ruling first; see item 6. |
| **Task #1439** | comparison window `5219b594bf6e2bf0b0d59527169eea88ace902d9..origin/main` | `d04075d15e56fb90c93f110d2badec2e0544067f` (PR #1431 — Task #1430's own resulting commit, appearing in this window because it postdates the file's previously stated Basis line)<br>`91e708489a7067b3c4ba45ab2ae5435666ac9eb1` (PR #1434 — `src/services/financialPressureService.js` only, three destructured args renamed `_source`/`_source_id`/`_description`, no logic change)<br>`8c3db0009760ad06a66c49f2aad2453017f2760d` (PR #1438 — `.eslintrc.js` only, scoped a `no-empty`/`allowEmptyCatch` override to `src/migrations/**/*.js`, clearing item 6 blocker (a))<br>`497098c2eafa87d2b56565691454943d15b41804` (PR #1437 — `docs/audit/F-Sec-3_LogTransaction_NameCollision_Read_2026-09-14.md` only) | none among items 1–12 | **Item 6 does not move this pass.** Its current text was already rewritten by PR #1438 to record both PR #1434's and its own change — the "basis `91e708489a7067b3c4ba45ab2ae5435666ac9eb1`" item 6 cites is PR #1434's resulting commit, the state PR #1438 itself started from — and no commit in this window past PR #1438 touches ESLint config, a migration, or `src/routes/auth.js`, so its figures are carried, not re-derived.<br>PR #1434 touches only `financialPressureService.js`, not `storyEvaluationRoutes.js` (item 9 does not move), not a stale model ID (item 7 does not move), and not a `getModels()` definition (item 12 does not move).<br>PR #1437 adds `F-Sec-3_LogTransaction_NameCollision_Read_2026-09-14.md` under `docs/audit/` only, touching no `src/`, `frontend/`, or `tests/` path — cited at §6.5, not restated here.<br>No `frontend/` file changed (item 8); no `backups/staging/` or the three identifier `.txt` files touched (item 10); no `jest.config.js` or `tests/` file changed (item 11). Items 1–5 carried as re-verified at the prior resolved state; no item among 1–12 moves this pass. |
| **Task #1446** | comparison window `497098c2eafa87d2b56565691454943d15b41804..origin/main` | `e940c906b` (PR #1442, `docs(audit): file read of the auth issuance surface`)<br>`2e37f52b5` (PR #1445, `docs(audit): file jwtAuth.js/Cognito claim finding`) | none | `git show --name-only` on both confirms each adds exactly one file, `F-AUTH-1_AuthIssuanceSurface_Read_2026-09-15.md` and `F-AUTH-1_JWTAuth_Cognito_Claim_Finding_2026-09-15.md` respectively, both under `docs/audit/` only — no `src/`, `frontend/`, `tests/`, ESLint config, migration, or `jest.config.js` path is touched by either commit. No item among 1–12 moves this pass. |
| **Task #1458** | comparison window `b9305747c0c450190fc51dc0d5784c83cac0ce93..origin/main` — per the Task #1248 correction, compared from Task #1446's own resulting commit (PR #1447), not from its cited Basis line (`2e37f52b5...`, one commit earlier, PR #1445's own resulting commit rather than PR #1447's). | PR #1449 (`e35998c76`)<br>PR #1452 (`e7311a257`)<br>PR #1457 (`a37174b48`) | Item 6 (moves); item 11 re-derived, count unchanged | PR #1449 touched `PROJECT_CONTEXT.md` only — the restructuring of this very re-check paragraph into the table these rows sit in. PR #1452 touched `src/middleware/jwtAuth.js`, `src/routes/auth.js`, and three test files (one new: `auth-me-contract.test.js`) — none is a stale-model-ID site (item 7), a `frontend/` file (item 8), `storyEvaluationRoutes.js` (item 9), `docs/archive/`/`backups/staging/` (item 10), or a `getModels()` definition (item 12), so none of those items moves; its actual effect (the HS256-only claim narrowing for `/logout`/`/me` only, not `/refresh`/`/validate`) is corrected in prose at §4.1 above. PR #1457 touched `.env.example`, `package.json`/`package-lock.json`, `src/routes/auth.js`, a new `src/services/cognitoPasswordAuthService.js`, and four test files (two new: `auth-login-cognito.test.js`, `cognitoPasswordAuthService.test.js`) — same non-movement reasoning for items 7–10 and 12; its effect is corrected/recorded in prose at §0 item 5, §4.1, §4.6, §5's dev-token-carrier row, §6.1, and §6.5 above. **Item 6 moves:** re-derived fresh, not carried — `npx eslint src/ tests/` now reports 0 errors, 1 warning, down from the 1 error/1 warning the prior pass recorded; both blockers item 6 names are now cleared (detail in item 6's own text below). **Item 11 re-derived, count unchanged:** `find tests -name '*.test.js' \| wc -l` → 166 (up from the 162 the prior recorded basis carried — the three new files above); `npx jest --listTests` → 148 collected, confirmed to include all three new files by name; 166 − 148 = 18 uncollected, the same figure item 11's text already carries. **This file does not re-derive whether the same 18 files as before are still the ones uncollected** — only that the count is unchanged with three known-collected files added on both sides of the fraction. `PR #1455` (`docs(audit): file sweep of unreached exports and shadowed registrations`) was confirmed `state: open, merged: false` at this pass's basis (see the file Basis line above) and is excluded from this window and this row. |
| **Task #1482** | comparison window `a37174b488868924b5dbb43f6dc78bc9b63e3633..origin/main` (per this file's own prior Basis line, which the intervening PR #1478 refresh left unmoved) | `1367e4a15` (PR #1459, Task #1458's own resulting commit)<br>`7f54c3fcf` (PR #1462)<br>`aed0ed809` (no PR number in subject)<br>`531a87fe9` (no PR number in subject)<br>`e01d6faf6` (no PR number in subject; issue #1454)<br>`8de3bbf0b` (no PR number in subject)<br>`c07177d82` (no PR number in subject)<br>`3aa25f0d5` (PR #1469)<br>`fc0342071` (PR #1470)<br>`c6a14e925` (PR #1471)<br>`1c578b71a` (PR #1472)<br>`237da7840` (PR #1473)<br>`950dbd2e5` (PR #1474)<br>`494e95e26` (PR #1475)<br>`4ef0c66e6` (PR #1476)<br>`e2ec91d75` (PR #1477)<br>`9983ef2dd` (PR #1478)<br>`fab882f79` (PR #1479)<br>`b20777ea2` (PR #1481) | none | Nineteen commits, the largest window this table has recorded in one pass. `9983ef2dd` (PR #1478) already refreshed this file's own §6.1 F-AUTH-1 row and §6.5 FD-65 row for `v2.73`–`v2.75` without updating this file's Basis line — that content is carried here, not re-derived by this pass (see the file Basis line above). `531a87fe9`, `aed0ed809`, `e01d6faf6`, `8de3bbf0b`, `c07177d82`, `7f54c3fcf`, and `1367e4a15` are `docs/audit/` filings or an earlier, now-superseded partial context refresh, with no `src/`, `frontend/`, or `tests/` path — none moves items 1–12. `3aa25f0d5`/`fc0342071`/`c6a14e925`/`237da7840` touch `CLAUDE.md`'s `aws` allowlist and `docs/audit/` only. `1c578b71a` (PR #1472) touches `src/services/cognitoPasswordAuthService.js` and its test only. `fab882f79` (PR #1479) touches `src/middleware/auth.js`, `src/middleware/jwtAuth.js`, and adds one new test file, `tests/unit/middleware/auth-authorize-case-insensitive.test.js` (`tests/unit/middleware/jwtAuth.test.js` and `tests/unit/services/cognitoPasswordAuthService.test.js` are extended, not new — both existed at the prior basis, per `git cat-file -e a37174b48...:<path>`). `4ef0c66e6`/`950dbd2e5`/`494e95e26`/`e2ec91d75`/`b20777ea2` (PR #1481) are `docs/audit/` filings only. **Item 6 re-derived, does not move:** `npx eslint src/ tests/ --format json` at this basis reports 0 errors, 1 warning — the same single warning as the prior recorded basis, same file and line (`src/middleware/auth.js:167`, `no-console`); neither the auth-middleware changes nor the new test file introduced a new lint problem. **Item 11 re-derived, count unchanged:** `find tests -name '*.test.js' \| wc -l` → 167 (up from 166 — exactly the one genuinely new file); `npx jest --listTests` → 149 collected, confirmed to include the new file by name; 167 − 149 = 18 uncollected, the same figure the prior basis carried. Items 7, 8, 9, 10, and 12 do not move — no stale model ID added under `src/`, no `frontend/` file changed, `storyEvaluationRoutes.js` untouched, no `docs/archive/`/`backups/staging/` path touched, no `getModels()` definition touched. Items 1–5 carried as re-verified at the prior resolved state. §6.1's stale v2.72 sentence and §6.5's group-case row are updated in prose above by this same pass, per issue #1482 — this table records only whether items 1–12 moved, not that separate prose update. |
| **Task #1492** | comparison window `b20777ea2926bd38406fb6cc65a863e48169a83e..origin/main` (this file's own prior stated Basis line) | `210905604` (PR #1491)<br>`4831d4266` (PR #1489)<br>`5e6b3b469` (PR #1486)<br>`8c9711dcc` (PR #1487)<br>`35d7d1d8c` (PR #1483 — Task #1482's own resulting commit, appearing here because it postdates the file's previously stated Basis line) | Item 6 re-derived, does not move; item 11 re-derived, count unchanged | PR #1489 touches `.github/workflows/pr-validation-block-check.yml`, `scripts/check-pr-validation-block.js`, and adds one new test file, `tests/unit/scripts/check-pr-validation-block.test.js` — the only commit in this window touching `src/`, `tests/`, `.eslintrc.js`, or `jest.config.js`, so both items were re-derived rather than carried. **Item 6 re-derived, does not move:** `npx eslint src/ tests/ --format json` at this basis reports 0 errors, 1 warning — the same single warning as the prior recorded basis, same file and line (`src/middleware/auth.js:167`, `no-console`); PR #1489's own files carry no lint problem. **Item 11 re-derived, count unchanged:** `find tests -name '*.test.js' \| wc -l` → 168 (up from 167 — exactly the one new file); `npx jest --listTests` → 150 collected, confirmed to include the new file by name; 168 − 150 = 18 uncollected, the same figure the prior basis carried. PR #1491 (`docs/audit/F-Stats-1_CompoundPredicates_Read_2026-09-16.md` only), PR #1486 (`docs/audit/F-AUTH-1_Fix_Plan_v2.76.md` only), PR #1487 (`docs/audit/F-AUTH-1_GroupCase_MintDecision_2026-09-16.md` only), and PR #1483 (`PROJECT_CONTEXT.md` only, already reflected — see the file Basis line above) touch no `src/`, `frontend/`, `.eslintrc.js`, or `jest.config.js` path — items 7, 8, 9, 10, and 12 do not move. Items 1–5 carried as re-verified at the prior resolved state. §6.5's F-Stats-1 compound-predicate row and its new note-filing row are updated in prose above by this same pass, per issue #1492 — this table records only whether items 1–12 moved, not that separate prose update. PR #1486 and PR #1487 change content this file states outside §6.5/§10/Basis (§0 item 5, §6.1, §6.5's precondition-3 text; see the file Basis line above) — not addressed by this pass, named under "Out of scope, found" in this pass's own PR body. |
| **Task #1494** | comparison window `210905604fcf144f64456acd163f8c0cb1b6ffe4..origin/main` (this file's own prior stated Basis line) | `a12410bbb` (PR #1493, `PROJECT_CONTEXT.md` only — Task #1492's own resulting commit, appearing here because it postdates the file's previously stated Basis line; already fully reflected in this file, since it produced the content this Basis line is now compared from) | none among items 1–12 | The single commit in this window touches `PROJECT_CONTEXT.md` only — no `src/`, `frontend/`, `tests/`, `.eslintrc.js`, or `jest.config.js` path — so items 6 and 11 are carried, not re-derived; items 1–5, 7–10, 12 carried as re-verified at the prior resolved state. §6.1's F-AUTH-1 row and §6.5's two FD-65 rows are updated in prose above by this same pass, per issue #1494, to cite `v2.76`'s discharge of `v2.67` §4 precondition 3 in `v2.76`'s own terms, and §6.5 gains two new DONE rows for `v2.76`'s filing (PR #1486) and the group-case mint decision (PR #1487) — this table records only whether items 1–12 moved, not that separate prose update. §0 item 5 is not touched this pass — see the file Basis line above for why, and "Out of scope, found" in this pass's own PR body. |
| **Task #1496** | comparison window `a12410bbb35439dfb8a18727f9d983f61d4522e6..origin/main` (this file's own prior stated Basis line) | `93e1f099d` (PR #1495, `PROJECT_CONTEXT.md` only — Task #1494's own resulting commit, appearing here because it postdates the file's previously stated Basis line; already fully reflected in this file, since it produced the content this Basis line is now compared from) | none among items 1–12 | The single commit in this window touches `PROJECT_CONTEXT.md` only — no `src/`, `frontend/`, `tests/`, `.eslintrc.js`, or `jest.config.js` path — so items 6 and 11 are carried, not re-derived; items 1–5, 7–12 carried as re-verified at the prior resolved state. **§0 item 5 is rewritten by this same pass, per issue #1496** — this table records only whether items 1–12 moved, not that separate rewrite. Item 5's stale "implementation filed, live verification owed" framing is replaced, citing `F-AUTH-1_Fix_Plan_v2.75.md` §3 (FD-65 CLOSED entire, live verification by Evoni, ATTESTED) and `F-AUTH-1_Fix_Plan_v2.76.md` §4 (precondition 3 discharged on direct evidence, `LOGIN_DISABLED` unflipped); the `PR #1472`/`v2.74` `SECRET_HASH` fix and the HS256 `tokenService`/ruling-(b) narrowing are kept. The "§6.1 tension" pointer is dropped — §6.1's own text states the tension "is resolved, not merely re-described," so item 5 no longer points to it as standing. |
| **Task #1502** | comparison window `93e1f099d3907a7b34f8435f6103cd037f829184..origin/main` (this file's own prior stated Basis line) | `4b173440d` (PR #1497, `PROJECT_CONTEXT.md` only — Task #1496's own resulting commit, appearing here because it postdates the file's previously stated Basis line; already fully reflected in this file, since it produced the content this Basis line is now compared from)<br>`33cbf0bd0` (PR #1500, `docs/audit/Canon_TableExpectation_Census_2026-09-17.md` only)<br>`af166d146` (PR #1501, `docs/audit/F-Deploy-1_DeployDev_FreezeStatement_Conflict_2026-09-17.md` only) | none among items 1–12 | None of the three commits touches `src/`, `frontend/`, `tests/`, `.eslintrc.js`, or `jest.config.js` — items 6 and 11 are carried, not re-derived; items 1–5, 7–12 carried as re-verified at the prior resolved state. **§0 item 8, §6.5, and §7 are updated in prose above by this same pass, per issue #1502** — this table records only whether items 1–12 moved, not those separate prose updates: §0 item 8 gains a pointer to the new conflict-read note without changing the MEASURED API state it records; §6.5 gains two new DONE rows (the census, the conflict-read note) and one new STILL OWED row (the open ruling both leave); §7's "What is safe today" sentence no longer calls dispatching `Deploy to Development` settled, and its "Freeze rules" line gains the hazard doc's Sec 3 item 7, previously omitted from that summary. |
| **Task #1506** | comparison window `af166d1466aabb4fcb94098e827bbd09385220a0..origin/main` (this file's own prior stated Basis line) | `e7227a800` (PR #1503, `PROJECT_CONTEXT.md` only — Task #1502's own resulting commit, appearing in this window because it postdates the file's previously stated Basis line; already fully reflected in this file, since it produced the content this Basis line is now compared from)<br>`402326766` (PR #1505, `docs/audit/F-Deploy-1_Fix_Plan_v1.50.md` only) | none among items 1–12 | Neither commit touches `src/`, `frontend/`, `tests/`, `.eslintrc.js`, or `jest.config.js` — items 6 and 11 are carried, not re-derived; items 1–5, 7–12 carried as re-verified at the prior resolved state. **§0 item 8, §6.5, and §7 are updated in prose above by this same pass, per issue #1506** — this table records only whether items 1–12 moved, not those separate prose updates: §0 item 8 now records `Deploy to Development` `disabled_manually`, re-measured this session, and not dispatched until reconciliation, per `F-Deploy-1_Fix_Plan_v1.50.md` §2; §6.5's open-ruling row becomes DONE citing v1.50, gains one new STILL OWED row (conflict note §3.4 Q3, unaddressed by v1.50), and gains one new DONE row for v1.50's own filing; §7's Dev-box and Workflows rows record the disabled state, and its "What is safe today" sentence no longer calls the question unresolved — dispatch is stated not permitted until reconciliation. |
| **Task #1516** | comparison window `402326766..origin/main` (this file's own prior stated Basis line) | `938216c3` (PR #1507, `PROJECT_CONTEXT.md` only)<br>`5e9411cf` (PR #1509, `Canon_AbsentTable_Classification_2026-09-17.md` only)<br>`2b4207ab` (PR #1511, four canon capture/evidence files)<br>`8f482497` (PR #1515, `F-Deploy-1_Fix_Plan_v1.51.md` only) | none among items 1–12 | `git show --name-only` confirms the changed paths above. No `src/`, `tests/`, `.eslintrc.js`, or `jest.config.js` file changed, so items 6 and 11 are carried, not re-derived. **§4.5 and §6.5 move by this pass:** the 2026-09-17 capture is added with its unchanged-since-2026-08-29 statement; v25 item 8 is replaced by v1.51's schema-fork ruling, with only the Evoni-gated reconciliation session remaining; PR #1509's classification and PR #1511's capture are recorded DONE. |
| **Task #1522** | comparison window `8f482497e556711377e8e93f92162027a2e7cc50..origin/main` (this file's own prior stated Basis line) | `33cabcb8a8dc9824ceae5e4b9b4441ef172e541a` (PR #1521, `docs/audit/F-Deploy-1_Fix_Plan_v1.52.md` only) | none among items 1–12 | No `src/`, `tests/`, `.eslintrc.js`, or `jest.config.js` file changed, so items 6 and 11 are carried, not re-derived. **§0 item 5, §7, and §6.5 move by this pass:** the prod-box row and freeze summary are updated to v1.52's ATTESTED reconciliation record, and the v1.52 filing plus the two still-owed rows it names are recorded. |
| **Task #1565** | comparison window `33cabcb8a8dc9824ceae5e4b9b4441ef172e541a..origin/main` (this file's own prior stated Basis line) | `ab1ac90b7` (PR #1523 — Task #1522's own resulting commit)<br>`91940e489` (PR #1526)<br>`7e9f91d1d`<br>`15abdbafb`<br>`13a6c5449` (PR #1531)<br>`7001e31f5` (PR #1533)<br>`3002cdb81` (PR #1535)<br>`e92610563`<br>`8d4a96162` (PR #1540)<br>`5667d9a87` (PR #1542)<br>`ce54a1427` (PR #1544)<br>`34b3da26f` (PR #1546)<br>`071a740ae` (PR #1548)<br>`97639979e` (PR #1550)<br>`c59492840` (PR #1552)<br>`23887d44a` (PR #1554)<br>`a7fe6206f` (PR #1556)<br>`b791439bc` (PR #1558)<br>`2964fce5d` (PR #1560)<br>`bf60ab1f7` (PR #1562)<br>`213edbab1` (PR #1564) | **Item 6 re-derived, does not move; item 11 re-derived, does not move (count unchanged).** None of items 1–5, 7–10, 12 moves. | Only one commit in this window touches `src/`, `tests/`, `.eslintrc.js`, or `jest.config.js`: `213edbab1` (PR #1564), which edits `src/routes/auth.js`, `src/services/cognitoPasswordAuthService.js`, and three test files (one new: `tests/unit/routes/auth-refresh-cognito.test.js`) — so items 6 and 11 were re-derived rather than carried, per this pass's own §4.8 re-check (above). **Item 6 re-derived, does not move:** `npx eslint src/ tests/ --format json` at this basis reports 0 errors, 1 warning — the same single warning as the prior recorded basis, same file and line (`src/middleware/auth.js:167`, `no-console`); neither of PR #1564's two `src/` files introduced a new lint problem. **Item 11 re-derived, does not move:** `find tests -name '*.test.js' \| wc -l` → 169 (up from 168 — exactly the one new file); `npx jest --listTests` → 151 collected, confirmed to include the new file by name; 169 − 151 = 18 uncollected, the same figure the prior basis carried. None of the other nineteen commits touches a stale model ID under `src/` (item 7), a `frontend/` file in a way this file's numbered items track beyond what §9's stale-doc table already carries (item 8 — the nine IA/navigation/Production-Checklist commits in this window, eight of them touching `frontend/` directly and one a same-day companion doc, are product work, not the stale-nav-target/orphan-file items 8 names), `storyEvaluationRoutes.js` (item 9), `docs/archive/`/`backups/staging/` (item 10), or a `getModels()` definition (item 12). **§0 item 5, §4.1, §4.6, §4.8, §6.1, §6.3, §6.5, and §10's own suggested-item list move in prose by this same pass, per issue #1565** — this table records only whether items 1–12 moved, not those separate prose updates; §10 gains two new suggested items (logout/revocation for Cognito-issued access tokens; re-deriving §4.6's IA/Sidebar line and frontend counts against the eight `frontend/`-touching commits this pass's own Basis line names but does not check) and marks no existing item 1–12 obsolete, since none of them concerns `/refresh`, `/login`, `/logout`, or §4.6's IA/Sidebar content. |

Inside the locked sequence and safe for agent sessions:
1. **DONE (confirmation sweep, exceptions consolidated, and ratification).** ~~Limb 1 per-CP confirmation passes (repo-only, decomposable, cannot-tell first-class).~~ All 12/12 CPs confirmed (`F-AUTH-1_Limb1_CP1…CP12_Confirmation_2026-09-02.md`, CP10 committed 2026-09-01, the rest 2026-09-02): 127 recorded dispositions, 120 agree / 2 disagree / 5 cannot-tell. The 2 disagree + 5 cannot-tell rows are consolidated with re-verified code reads at `F-AUTH-1_Limb1_Exceptions_Consolidated_2026-09-03.md`. See §6.1. **A handoff had the chance and declined**: `Prime_Studios_Audit_Handoff_v26.md` Sec 8 explicitly stated it does not ratify limb 1's discharge, naming `F-AUTH-1_Fix_Plan_v2.69` as Evoni's to rule instead. ~~Ratification now requires `F-AUTH-1_Fix_Plan_v2.69`.~~ **v2.69 §6 Ruling 1 (2026-09-03) discharges limb 1**, accepting the five cannot-tells as unconfirmed under this method rather than pending under it; an itemized audit that could settle them under a different instrument is carried forward, Evoni-gated, at §6.5.
2. ~~FD-64 fix: rename `getRolesForshow` → `getRolesForShow` and the `Model.update()` without `where` in `AssetRoleService.js:152`.~~ **DONE** — `65cbe7013` and `2e5dbdf28`; ~~close not recorded in a Fix Plan revision~~ **closed** `v2.69` §6 Ruling 2 (2026-09-03).
3. Register hygiene tasks — **DONE, all three sub-items.** Place the owed banner on Amd28 §AD3 — **DONE** (`v25_Owed_Index_Amd28_2026-09-01.md` now carries a second banner, added 2026-09-03, correcting §AD3.3's status line per `Amd30` §AF3.3's ruling — PR #1217; confirmed present at this basis); reconcile v25's FD-68 status — **DONE** (§6.1 already carries "FD-65 and FD-68 CLOSED (v25 wrongly lists FD-68 open)"); PE tail correction — **DONE** (§6.3 already carries "PE tail PE #68").
4. **DONE** — PR #1201 (2026-09-03). Both files still exist (Copilot resolution requires the path) and still listed in §9, now noted there as carrying freeze notices instead of the SSH/pm2 procedure.
5. **DONE**, in two passes — a reader tracing this item should find both, not one PR with no explanation for the other. `PR #1221` (2026-09-03) redacted the label but was **incomplete**: its check confirmed only that the redaction marker was present on each line, not that nothing else remained inside the delimited span, and the original values each contained an embedded backtick that caused the replacement to stop early, leaving a trailing fragment of the real credential exposed on `main`. `PR #1224` (2026-09-04) completed the redaction, verified by counting backticks and asserting the delimited span's contents equal the marker exactly — confirmed at this basis in both `docs/README_AUDIT_FEBRUARY_2026.md` and `docs/SECURITY_AUDIT_FINDINGS.md`. **Redaction removes the literal from `main` going forward; it does not remove it from git history**, where it remains readable. Whether the underlying credential has been or needs to be rotated is not stated here — outside repo-derivable scope.
6. **Blocker (a) cleared at this basis; blocker (b) still standing.** No `eslint` reference in `.github/workflows/validate.yml` at this basis. **Blocker (a) — resolved by issue #1436** (basis `91e708489a7067b3c4ba45ab2ae5435666ac9eb1`): all five `no-empty` errors in the four migrations' `down()` were confirmed at source to be `try { await queryInterface.removeColumn(...) } catch {}` blocks — idempotent rollback (the column may already be gone; there's nothing to do about it), not a `describeTable` schema-existence guard on `up()`. `.eslintrc.js` gained a new override scoped to `files: ['src/migrations/**/*.js']` only, setting `'no-empty': ['error', { allowEmptyCatch: true }]` — not an eslintignore entry, not the rule turned off. **This permits any future migration's `down()` to also silently swallow a rollback failure, not just these four files' already-flagged blocks** — defensible for teardown specifically (the failure mode is "already gone, nothing to do"), but that is what the override actually grants, not something narrower. No migration file was edited. `src/seeders/**/*.js` was confirmed unaffected (`npx eslint src/seeders --format json`: 0 errors, 14 files) and remains outside the new override's scope. Re-run after the config change: `npx eslint src/ tests/` reports 1 error, 1 warning, 2 problems, across 2 `src/` files — down from 6 errors across 5 files, a drop of exactly five, all `no-empty`, with no new errors or warnings anywhere (`ESLINT_ERROR_COUNT:1`, `ESLINT_ERROR_FILE_COUNT:2`, ATTESTED from local run). **Blocker (b) is untouched by this pass, exactly as previously stated:** one error is `no-unreachable` at `src/routes/auth.js:57`, in a region the file's own comment states was retained deliberately pending a ruling on whether password login should exist at all — read at `F-AUTH-1_LoginHandler_UnreachableRegion_Read_2026-09-14.md` (issue #1428, PR #1429), carried here and not restated as this file's own finding. **"Clear the remaining errors, then add ESLint to CI" still cannot complete as written while blocker (b) stands.** This entry records what changed; it rules nothing, per this section's header.

**Blocker (b) cleared at this basis (Task #1456/PR #1457, 2026-09-15).** The `no-unreachable` region at `src/routes/auth.js:57` no longer exists — the whole handler was rewritten to implement a real Cognito `InitiateAuth` exchange, per Evoni's 2026-09-14 ruling (a) waiving the fix-cycle scope restriction for password-login implementation (`F-AUTH-1_AuthIssuanceSurface_Read_2026-09-15.md`). Re-run at this basis: `npx eslint src/ tests/` reports **0 errors, 1 warning** — the warning is `src/middleware/auth.js:167`, `no-console`, pre-existing and unrelated to any of this pass's three commits — ATTESTED from local run. **Both blockers this item names are now cleared. "Clear the remaining errors, then add ESLint to CI" can now complete as written.** This entry records that fact; it does not itself add the `eslint` reference to `.github/workflows/validate.yml` — that remains a separate, now-unblocked task this entry does not perform.

Product-adjacent, small, and reversible (would need Evoni's "scope creep" waiver, since the onboarding doc forbids feature work during the fix cycle):
7. **STILL OWED** — 61 occurrences of the stale model IDs remain under `src/` at this basis. Normalize model IDs (`claude-sonnet-4-20250514` → `claude-sonnet-4-6`; delete the invalid `claude-sonnet-4-5-20250514`; route `iconCueGeneratorService` through the SDK so it is budget-tracked).
8. **STILL OWED** — §4.6 still lists the same stale nav targets and orphan files, unchanged. Fix stale navigation targets and delete orphan frontend files listed in §4.6.
9. **STILL OWED** — `src/routes/storyEvaluationRoutes.js` still present, no disposition document found. Decide the Story Engine backend's fate (retire, keep as API, or rebuild UI).
10. **Archive half DONE; removals half STILL OWED, unruled.** Archive `docs/*.md` phase reports into `docs/archive/` with an index, keeping the few live guides — **DONE** (issue #1415, PR #1416, merge `b37574d530f88e3050b5f7922b40b571ae0758c5`): 334 files moved from top-level `docs/` into `docs/archive/`, plus `docs/archive/INDEX.md` added as the index; the live guides kept at their top-level `docs/` paths are `API_REFERENCE.md`, `API_QUICK_REFERENCE.md`, `COGNITO_USER_POOL_SETTINGS.md`, `LOCAL_DEV_LOGIN.md`, and seven others (see §9). **Remove `backups/staging/*.sql` and the three identifier `.txt` files from the tree — STILL OWED**; not touched by PR #1416 and not ruled here.
11. **DONE for the named #1260/#1276/#1313/#1356/#1358 slices; residual test-discovery and local-DB gaps remain.** Issue #1260 fixed the 5433/5432 mismatch (`README.md:107`) and named the then-20 uncollected backend test files in `jest.config.js:22-54` (`npx jest --listTests` collected 142 of 162 at that basis). PR #1291 lifted the `TEST_DATABASE_URL` hard gate in `tests/setup.js`: missing `TEST_DATABASE_URL` now warns, and `DATABASE_URL` is assigned only when the test URL exists. Issue #1276 was manually closed after Evoni read its six-item checklist against PR #1291 and the 2026-09-07 evidence-run comment: runs (a) `TEST_DATABASE_URL` unset and (b) set against a real, migrated Postgres both reported 2519 total tests, so run (c)'s purpose held even though no second basis existed. PR #1314/issue #1313 then fixed the five sibling `amazonaws.com` predicates (`auth`, `episodes`, `f-auth-1-fd65`, `scenes`, `wardrobe-money`) to skip on unset/empty `DATABASE_URL` while preserving optional chaining and `episode_metadata_test` clauses. PR #1357/issue #1356 widened `jest.config.js` collection from 142 to 144 of 162 files by adding `tests/api/endpoints.test.js` to `testMatch` and removing `tests/unit/controllers/fileController.test.js` from `testPathIgnorePatterns`; both added files passed in CI. Eighteen files remain uncollected. Four of those have DB-independent missing-module failures recorded in PR #1357's body/comment: `tests/integration/jobs.test.js` (`JobProcessor`), `tests/integration/search.test.js` (`OpenSearchService`), `tests/unit/controllers/searchController.test.js` (`OpenSearchService`), and `tests/unit/models/job.test.js` (`models/job`). The other fourteen classifications from #1357 are **not settled here** because PR #1357 now carries a comment stating they were measured against a failing local DB path and need rerun with confirmed host-to-Docker DB auth. PR #1359/issue #1358 filed `CI_LocalSuite_Divergence_2026-09-11.md`: CI's Tests job passed all 144 suites at `089cfa85`, including the five that failed locally, and the local failures traced to host-to-Docker Postgres auth failure; the note rules nothing, proposes no remedy, and does not establish those five suites as broken. **Still not done:** ordinary `npx jest` does not discover `scenes.integration.test.js` because `jest.config.js` still ignores it; #1314 disclosed its suite-run acceptance item as 4/5 met and verified that file's predicate in isolation instead of via a live suite. Issue #1313 remains open at this basis even though PR #1314 is merged; `/close-loop` should catch it as an Agreement row, but closing is Evoni's Rule 7 step.
12. **DONE.** The `getModels()` helper pattern (`async function getModels() { try { return require('../models'); } catch (e) { return null; } }`) used across route files swallowed the require failure with no logging, `e` unused (one of the `no-unused-vars` errors §4.8 already counts). Fixed in `templateStudio.js` as part of issue #1280/PR #1281 (`console.error('Failed to load models:', e.message)` added). The five other files this item previously named as still silent — `careerGoals.js`, `world.js`, `storyteller.js`, `worldEvents.js`, `wardrobe.js` — were fixed identically at PR #1402 (issue #1401, 2026-09-12). Re-derived fresh at this basis by two instruments: direct reads of all six `getModels()` definitions (the five plus `templateStudio.js`) confirm each `catch` clause now logs before returning null, identical in form; and a widened pattern search, `grep -rn -E "catch\s*\(\s*e[a-zA-Z]*\s*\)\s*\{\s*return null" --include='*.js' src/` (covering spacing and identifier variants — `catch(e)`, `catch (err)`, and similar — that a literal-string match on the original one-line form would miss), returns zero hits anywhere in `src/`. Not assumed to be the pattern's full scope beyond `src/` — not re-swept against `frontend/` or elsewhere.
13. **STILL OWED, suggested by Task #1565.** A read of logout/revocation for Cognito-issued access tokens. `POST /logout` blacklists a token via `TokenService.tokenBlacklist` (`src/services/tokenService.js`), which is checked only inside `TokenService.verifyToken` — the HS256-only path (§4.1). A Cognito-issued (RS256) access token, the one `POST /login` now returns and the frontend now bearer-sends (§4.6), is verified by `verifyViaCognito` in `src/middleware/auth.js`, a code path that never consults `tokenBlacklist`. Task #1563/PR #1564's own E2E test edit surfaces this directly, in-line comment: the Cognito-issued access token "isn't a local HS256 JWT this server can verify/revoke." Whether logout leaves a live Cognito access token usable until natural expiry — and whether that is a defect, an accepted design point, or something Cognito's own token-revocation API would need to close — is not read here. No register document reads this yet; filing one is ordinary agent-session work once authorized through the task loop, not Evoni-gated.
14. **STILL OWED, suggested by Task #1565's own reconciliation fix.** Re-derive §4.6 against the eight `frontend/`-touching commits this pass's own Basis line names but does not check (`c59492840`, `34b3da26f`, `ce54a1427`, `5667d9a87`, `8d4a96162`, `7001e31f5`, `13a6c5449`, `15abdbafb` — `Sidebar.jsx` changed twice, `App.jsx` changed twice, plus a new `EpisodeProductionChecklist.jsx`/`.test.jsx` pair and `EpisodeAssetsTab.jsx`/`EpisodeDetail.jsx` edits). §4.6's IA (Sidebar) bullet and its source/CSS/Vitest file counts (560 source files, 132 CSS files, 99 Vitest files, ~786 cases) were last re-derived before this window and are carried unchecked through it. Not performed here — this pass's own scope is limited to §4.6's F-Auth-4 interceptor sentence (§0 item 5, §4.1).

Evoni-only (never an agent task): everything in §6.5 marked Evoni, and the prod reconciliation session.

---

## 11. Where to look

| Need | Path |
|---|---|
| App composition, mounts, middleware | `src/app.js`, `src/server.js` |
| Auth | `src/middleware/auth.js`, `src/middleware/jwtAuth.js`, `src/middleware/rbac.js`, `src/routes/auth.js`, `src/services/tokenService.js` |
| DB config / models / migrations | `src/config/sequelize.js`, `src/models/index.js`, `src/migrations/` |
| Story/AI | `src/routes/memories/*.js`, `src/routes/storyteller.js`, `src/services/textureLayerService.js`, `src/routes/storyEvaluationRoutes.js` |
| World/Feed | `src/routes/worldEvents.js`, `worldStudio.js`, `socialProfileRoutes.js`, `characterRegistry.js`, `franchiseBrainRoutes.js`, `src/services/feedScheduler.js`, `src/seeders/` |
| Production | `src/routes/episodes.js`, `shows.js`, `sceneSetRoutes.js`, `wardrobe.js`, `assets.js`, `uiOverlayRoutes.js`, `src/controllers/*` |
| Frontend | `frontend/src/App.jsx`, `components/layout/Sidebar.jsx`, `pages/WorldAdmin.jsx`, `pages/WriteMode.jsx`, `pages/UIOverlaysTab.jsx`, `components/SceneStudio/` |
| CI / validation | `.github/workflows/validate.yml`, `scripts/validate-routes.js`, `scripts/lint-silent-catches.sh`, `scripts/audit-cost-exposure.sh`, `scripts/check-root-junk.js`, `.githooks/` |
| Deploy | `.github/workflows/deploy-dev.yml` (live path), `deploy-production.yml` (disabled), `ecosystem.config.js`, `nginx/` |
| Register entry points | `docs/audit/NEW_CHAT_ONBOARDING.md`, newest `Prime_Studios_Audit_Handoff_v*.md`, newest `v25_Owed_Index_Amd*.md`, `Cross_Keystone_Register.md`, `Session_PE_Roster.md` |
| Freeze | `F-Deploy-1_PROD_SplitBrain_HAZARD.md`, `F-Deploy-1_INCIDENT_2026-05-30_prod-autodeploy.md` (repo root) |
| Workflow for humans and agents | `DEVELOPMENT_WORKFLOW.md`, `.claude/skills/`, `.claude/desktop/` |
